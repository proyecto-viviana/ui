User-agent: *
Allow: /

# The admin dashboard is dev-only — in a production build its beforeLoad
# redirects to /. Crawling it costs a request and yields a duplicate of the
# home page, so it is excluded here and from the sitemap.
Disallow: /admin

# Keep this origin in step with SITE_URL in src/seo.ts — test:seo asserts it.
Sitemap: https://ui.proyectoviviana.org/sitemap.xml
