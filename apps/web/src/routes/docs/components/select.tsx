// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/select.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/select")({
  head: () =>
    seo({
      title: "Select props",
      description:
        "Every prop Select accepts in @proyecto-viviana/ui — 77 documented from the package's own types.",
      path: "/docs/components/select",
    }),
  component: () => <ApiReference page={page} />,
});
