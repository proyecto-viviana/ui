// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/divider.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/divider")({
  head: () =>
    seo({
      title: "Divider props",
      description:
        "The 13 props declared for Divider in @proyecto-viviana/ui and the packages under it — props inherited from outside them are not listed.",
      path: "/docs/components/divider",
    }),
  component: () => <ApiReference page={page} />,
});
