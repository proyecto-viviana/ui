// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/link.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/link")({
  head: () =>
    seo({
      title: "Link props",
      description:
        "The 34 props declared for Link in @proyecto-viviana/ui and the packages under it — props inherited from outside them are not listed.",
      path: "/docs/components/link",
    }),
  component: () => <ApiReference page={page} />,
});
