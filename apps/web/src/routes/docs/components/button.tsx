// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/button.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/button")({
  head: () =>
    seo({
      title: "Button props",
      description:
        "The 40 props declared for Button in @proyecto-viviana/ui and the packages under it — props inherited from outside them are not listed.",
      path: "/docs/components/button",
    }),
  component: () => <ApiReference page={page} />,
});
