// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/colorslider.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/colorslider")({
  head: () =>
    seo({
      title: "ColorSlider props",
      description:
        "The 25 props declared for ColorSlider in @proyecto-viviana/ui and the packages under it — props inherited from outside them are not listed.",
      path: "/docs/components/colorslider",
    }),
  component: () => <ApiReference page={page} />,
});
