// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/picker.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/picker")({
  head: () =>
    seo({
      title: "Picker props",
      description:
        "Every prop Picker accepts in @proyecto-viviana/ui — 81 documented from the package's own types.",
      path: "/docs/components/picker",
    }),
  component: () => <ApiReference page={page} />,
});
