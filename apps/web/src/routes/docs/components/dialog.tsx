// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/dialog.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/dialog")({
  head: () =>
    seo({
      title: "Dialog props",
      description:
        "Every prop Dialog accepts in @proyecto-viviana/ui — 73 documented from the package's own types.",
      path: "/docs/components/dialog",
    }),
  component: () => <ApiReference page={page} />,
});
