// Generated by `vp run api:extract`. Do not edit; change the package's types instead.
import { createFileRoute } from "@tanstack/solid-router";
import { ApiReference } from "@/components/docs";
import page from "@/data/api-reference/pages/statuslight.json";
import { seo } from "@/seo";

export const Route = createFileRoute("/docs/components/statuslight")({
  head: () =>
    seo({
      title: "StatusLight props",
      description:
        "The 14 props declared for StatusLight in @proyecto-viviana/ui and the packages under it — props inherited from outside them are not listed.",
      path: "/docs/components/statuslight",
    }),
  component: () => <ApiReference page={page} />,
});
