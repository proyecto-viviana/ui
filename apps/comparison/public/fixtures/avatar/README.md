# Avatar comparison fixture

Save the docs-parity Avatar image as:

```text
apps/comparison/public/fixtures/avatar/docs-avatar.png
```

The comparison route references it as `/fixtures/avatar/docs-avatar.png`.
Use a square PNG, at least 256x256 pixels. WebP is also fine if the source path is updated in `apps/comparison/src/data/avatar-demo.ts`.
