export const comparisonSiteName = "Solid Spectrum";

export const comparisonSiteDescription =
  "React Spectrum S2 and Solid Spectrum, side by side. A parity harness for @proyecto-viviana/solid-spectrum.";

export const comparisonCoveragePath = "/coverage/";

export const comparisonInternalRobots = "noindex, nofollow";

export function comparisonComponentPath(slug: string): string {
  return `/components/${slug}/`;
}
