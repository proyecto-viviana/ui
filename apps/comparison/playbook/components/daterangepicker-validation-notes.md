# DateRangePicker Validation Notes

Date: 2026-05-19
Status: parity expanded; assistive-technology rows remaining

## Gate Outcome Summary

| Gate                                     | Outcome | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Blockers/owner                                                                   |
| ---------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Official Docs And Viewer Parity          | covered | Official S2 page checked 2026-05-19 through the S2 docs MCP. Docs sections inventoried: Value, Forms, API. The comparison route mounts React and Solid styled stacks with controls for date/date-time range values, granularity, hour cycle, hidden time zone state, Provider locale, custom calendar creation, form owner/name props, validation behavior, min/max, unavailable dates, non-contiguous ranges, first weekday, page behavior, read-only, disabled, required, invalid, help text, and visible months. | None for modeled route controls.                                                 |
| Upstream React Source Parity             | covered | React S2 source was checked locally from `apps/comparison/node_modules/@react-spectrum/s2/src/DateRangePicker.tsx`. Solid now mirrors the S2 size names, field shell, slotted start/end `DateInput` segment fields, required/invalid/help text surface, calendar button state, hidden start/end form inputs, RangeCalendar popover prop routing, popup `Start time`/`End time` fields, `createCalendar`, and locale routing.                                                                                        | None for covered prop surface.                                                   |
| Solid Idiomatic Implementation           | covered | `DateRangePicker` supports uncontrolled `defaultOpen`, controlled `isOpen`, `onOpenChange`, hidden `startName`/`endName`/`form` inputs, date-time and zoned range values, slotted start/end segment editing, popup time editing, and forwards range value/min/max/unavailable/non-contiguous/createCalendar/first-day/page-behavior state into the popup RangeCalendar.                                                                                                                                             | None for implementation gates covered here.                                      |
| Accessibility And I18n                   | partial | Existing headless tests cover localized start/end labels, keyboard open, Escape close, disabled/read-only data, validation descriptions, popup rendering, hidden form input values, date-time segment edits, and zoned hidden values. Browser coverage now asserts locale route parity and custom calendar seven-column grids.                                                                                                                                                                                      | Add explicit assistive-technology transcript rows for range entry and selection. |
| Style Source-To-Computed Parity          | covered | Solid styled DateRangePicker uses generated S2 field/popup classes and the S2-styled RangeCalendar popover. Browser evidence asserts exact normalized React-vs-Solid closed-field screenshots, two month grids, seven-column rows, fixed 224px month-grid width, unavailable/invalid rendering, date-time popup TimeFields, locale segment parity, custom-calendar grid geometry, and bounded React-vs-Solid grid screenshots with zero dimension delta and a 1.3% raster threshold.                                | None for automated visual gates covered here.                                    |
| React-Vs-Solid Comparison Harness Parity | covered | React fixture imports real `@react-spectrum/s2` DateRangePicker; Solid fixture imports public `@proyecto-viviana/solid-spectrum` DateRangePicker. Both fixtures expose serialized props/value evidence and route the same interactive controls for range value, time, locale, custom calendar, form owner/name props, calendar constraints, validation, read-only/disabled state, and visible months.                                                                                                               | None for harness parity.                                                         |
| Evidence And Handoff                     | partial | Focused package tests cover the headless/component/styled layers; DateRangePicker browser coverage asserts route props, side-panel controls, date-time hidden values, segment field parity, open calendar grid parity, locale parity, and custom-calendar seven-column geometry.                                                                                                                                                                                                                                    | Component remains partial only for assistive-technology transcript rows.         |

## Covered

- DateRangePicker is now live on both React and Solid styled comparison stacks.
- Side-panel controls cover label, S2 size, range value, max visible months, first weekday, page behavior, min/max range constraints, unavailable dates, non-contiguous ranges, start/end form names, description, error text, disabled, read-only, required, and invalid states.
- Side-panel controls also cover date-time granularity, hour cycle, hidden time-zone state, Provider locale, custom calendar creation, form owner, and validation behavior.
- Headless DateRangePicker open state is controlled/uncontrolled without a mirror effect.
- Solid styled DateRangePicker uses S2 size values and generated field/popup shell styling.
- Solid DateRangePicker now forwards picker state into the popup RangeCalendar instead of rendering an unbound calendar shell.
- Hidden `startName` and `endName` inputs are implemented for form submission parity.
- Closed start/end DateInput segments expose six spinbuttons across both stacks.
- Segment editing updates the Solid range value and hidden start/end form inputs.
- Popup `Start time` and `End time` fields render for time granularities and update the Solid date-time range value without stripping the date.
- Date-time hidden inputs use `datetime-local`; zoned range values stay in hidden inputs with full zone strings.
- Closed segmented field screenshots have exact normalized React-vs-Solid pair-diff coverage.
- Open popover grid geometry and bounded React-vs-Solid grid screenshots are asserted for the deterministic two-month route.
- Locale routes assert matching closed segment text between React and Solid.
- Custom calendar routes assert two seven-column month grids with no extra eighth column.

## Remaining Gaps

- Add explicit assistive-technology rows for range entry and selection.
