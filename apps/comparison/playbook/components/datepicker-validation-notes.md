# DatePicker Validation Notes

Date: 2026-05-18
Status: partial

## Gate Outcome Summary

| Gate                                     | Outcome | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Blockers/owner                                                                                                                                                  |
| ---------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Official Docs And Viewer Parity          | partial | Official S2 and React Aria DatePicker API pages checked through the docs MCP. Route controls now cover label, size, controlled date and date-time values, `granularity`, `hourCycle`, `hideTimeZone`, validation, disabled/read-only, required, help/error text, `maxVisibleMonths`, `firstDayOfWeek`, `pageBehavior`, min/max constraints, unavailable dates, `name`, `form`, `validationBehavior`, Provider `locale`, and `createCalendar`. Browser routes assert date-only, zoned date-time, locale, and custom calendar states against both stacks. | Actual form submit behavior and a source branch ledger still need a dedicated pass.                                                                             |
| External Authority And Standards         | partial | React Spectrum S2 DatePicker documents `value`, `name`, `form`, `minValue`, `maxValue`, `isDateUnavailable`, `granularity`, `hourCycle`, `hideTimeZone`, `validationBehavior`, `maxVisibleMonths`, `firstDayOfWeek`, `pageBehavior`, Provider `locale`, and `createCalendar`; React Aria documents the same headless API shape. Browser tests cover keyboard-opened/portaled popover behavior, Escape/outside dismissal, selection close, disabled state, required/invalid/help text, hidden input value, and theme branches.                           | Full APG/WCAG/ARIA-AT assistive-technology matrix remains planned for the date picker composition.                                                              |
| Upstream React Source Parity             | partial | Solid DatePicker keeps the popup Calendar at React S2 DatePicker medium calendar geometry for all field sizes, maps S2 `maxVisibleMonths` to headless `visibleMonths`, passes calendar state props into the headless DatePicker state, forwards Provider locale, supports custom calendar creation, and connects the popup TimeField to DatePicker state for non-day granularities.                                                                                                                                                                     | Full upstream branch ledger remains open, especially form validation behavior and overlay placement/flipping edge cases.                                        |
| Solid Idiomatic Implementation           | partial | `createDatePickerState` owns `defaultOpen`, controlled `isOpen`, `onOpenChange`, calendar value, time value, and calendar state options. The wrapper follows existing S2 boundaries by normalizing string `firstDayOfWeek`, passing calendar props to headless state, and sharing DatePicker state with the popup TimeField instead of duplicating local time state.                                                                                                                                                                                    | DateRangePicker has the older local open-change effect and should be handled in its own component pass.                                                         |
| Accessibility And I18n                   | partial | Browser tests assert trigger focus restore after Escape, selection close/value update, outside dismissal, disabled styling and ARIA suppression, blank-field focus, label/help/error rendering, hidden `name` and `form` input attributes, min/max disabled dates, unavailable disabled dates, locale spinbutton parity, seven-column custom calendar grids with no extra blank day column, and theme-reactive portaled popover colors. Package tests cover DatePicker labels, required invalid naming rows, and zoned hidden values.                   | Full submit behavior and screen-reader matrix rows remain open.                                                                                                 |
| Behavior State Machine                   | partial | `vp test run packages/solid-stately/test/createDatePickerState.test.ts packages/solidaria/test/createDatePicker.test.tsx packages/solidaria-components/test/DatePicker.test.tsx packages/solid-spectrum/test/DatePicker.test.tsx` passed with 64 tests. `vp run --filter @proyecto-viviana/comparison test:datepicker` passed with 11 browser tests. `vp exec --filter @proyecto-viviana/comparison playwright test e2e/modeled-controls-contract.spec.ts --grep DatePicker --reporter=line` passed with 1 modeled controls test.                       | Cross-browser and AT matrix evidence remain outside this focused component pass.                                                                                |
| Style Source-To-Computed Parity          | partial | Solid S2 DatePicker popup uses a fixed medium calendar surface in the popover, allows Calendar `UNSAFE_style` to override internal root sizing, preserves seven calendar columns with no extra blank day column, and reads hover paint from the actual inner Calendar cell. Browser tests cover field/icon sizing, settled popup width across S/M/L/XL fields, month nav, seven-column grid geometry, hover paint, disabled colors, light/dark popover colors, and the previously observed right-side blank column regression.                          | Strict zero-tolerance field/popup pair diffs are still not achieved; existing comparison remains thresholded and DatePicker style token ledger is not complete. |
| React-Vs-Solid Comparison Harness Parity | partial | React fixture imports real `@react-spectrum/s2` DatePicker; Solid fixture imports public `@proyecto-viviana/solid-spectrum` DatePicker. Both fixtures expose open-state evidence, controlled value evidence, route control props, hidden input values, locale/calendar-system evidence, and settled popup geometry. The side panel now drives docs-modeled DatePicker calendar, time, locale/calendar-system, form, and validation controls into both mounted styled stacks.                                                                            | Actual submit rows remain open.                                                                                                                                 |
| Evidence And Handoff                     | partial | Focused package tests, DatePicker browser comparison, and DatePicker modeled controls contract passed on 2026-05-18 after the route-control expansion.                                                                                                                                                                                                                                                                                                                                                                                                  | Component remains partial until the blockers above are closed.                                                                                                  |

## Official Docs And Viewer Inventory

| Docs item        | Official setting/example                                                                        | Route/control                                                                                                                                          | Status                    | Evidence                                                            |
| ---------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | ------------------------------------------------------------------- |
| Primary example  | `<DatePicker />`                                                                                | Route uses `label="Due date"` plus description for comparable field semantics.                                                                         | deviation recorded        | S2 docs MCP, `datepicker-demo.ts`.                                  |
| Value            | `value`, `defaultValue`, `onChange`, `parseDate`, `parseDateTime`, `parseZonedDateTime`         | `value` route parses date-only, local date-time, and zoned date-time strings; both stacks sync controlled values and selection updates evidence attrs. | covered                   | `datepicker-demo.ts`, `datepicker-visual.spec.ts`.                  |
| Forms            | `name`, `form`, `isRequired`, `minValue`, `maxValue`, `isDateUnavailable`, `validationBehavior` | `name`, `form`, required/invalid, min/max, unavailable date, and `validationBehavior` routes are covered; actual submit behavior remains open.         | partial                   | `component-controls.ts`, `datepicker-visual.spec.ts`.               |
| Open state       | `defaultOpen`, controlled `isOpen`, `onOpenChange`                                              | Headless state covers default/controlled open; both comparison fixtures track `onOpenChange`.                                                          | partial                   | `createDatePickerState.test.ts`, `DatePicker.test.tsx`.             |
| Calendar popover | `maxVisibleMonths`, `pageBehavior`, `firstDayOfWeek`, `shouldFlip`                              | `maxVisibleMonths`, `pageBehavior`, and `firstDayOfWeek` are routed and browser-tested; `shouldFlip` placement edge cases remain open.                 | partial                   | S2 docs MCP, `DatePicker.tsx`, `datepicker-visual.spec.ts`.         |
| Time fields      | `granularity`, `hourCycle`, `hideTimeZone`                                                      | Side-panel controls drive both fixtures; package and browser tests cover non-day granularity, hidden zoned values, and shared popup TimeField state.   | covered for parity routes | `DatePicker.test.tsx`, `datepicker-visual.spec.ts`.                 |
| Calendar states  | Provider locale and alternate calendar creation                                                 | `locale` and `calendarSystem` controls drive both fixtures; browser tests assert locale spinbutton parity and custom calendar seven-column grids.      | covered for parity routes | `component-controls.ts`, `styled.jsx`, `styled.tsx`, browser tests. |

## Covered

- DatePicker open state is owned by `createDatePickerState`, including `defaultOpen`, controlled `isOpen`, and `onOpenChange`.
- Solid S2 DatePicker accepts S2 string `firstDayOfWeek` values, normalizes them for headless state, and routes `maxVisibleMonths` as headless `visibleMonths`.
- Calendar min/max constraints, unavailable dates, Monday-first headers, two visible months, locale, and custom calendar creation are asserted in browser tests.
- Hidden input `name`, `form`, date value, date-time value, and zoned value serialization are asserted through package and comparison routes.
- Solid S2 DatePicker popover uses the React S2 DatePicker medium Calendar geometry for every DatePicker field size.
- Popup TimeField is connected to shared DatePicker state for non-day granularities.
- Calendar popup width is asserted against React after the popover settles for S/M/L/XL DatePicker sizes.
- Calendar hover assertions inspect the inner visible paint node while keeping the outer 32px cell geometry contract.
- Focused package tests, modeled-controls contract, and the DatePicker comparison browser suite passed.

## Remaining Gaps

- Add DatePicker route states for actual form submit behavior.
- Complete a DatePicker source branch ledger across S2, React Aria Components, React Aria datepicker hooks, and React Stately.
- Replace thresholded DatePicker field/popup visual comparisons with strict pair-diff evidence where practical.
- Add explicit assistive-technology matrix rows.
