# DatePicker Validation Notes

Date: 2026-05-19
Status: parity expanded; assistive-technology rows and open-popover strict visual rows remaining

## Gate Outcome Summary

| Gate                                     | Outcome | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Blockers/owner                                                                                              |
| ---------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Official Docs And Viewer Parity          | covered | Official S2 and React Aria DatePicker API pages checked through the docs MCP. Route controls cover label, size, controlled date and date-time values, `granularity`, `hourCycle`, `hideTimeZone`, validation, disabled/read-only, required, help/error text, `maxVisibleMonths`, `firstDayOfWeek`, `pageBehavior`, min/max constraints, unavailable dates, `name`, `form`, `validationBehavior`, Provider `locale`, and `createCalendar`. Browser routes assert date-only, zoned date-time, native associated `FormData`, locale, and custom calendar states against both stacks.            | None for modeled docs controls.                                                                             |
| External Authority And Standards         | partial | React Spectrum S2 DatePicker documents `value`, `name`, `form`, `minValue`, `maxValue`, `isDateUnavailable`, `granularity`, `hourCycle`, `hideTimeZone`, `validationBehavior`, `maxVisibleMonths`, `firstDayOfWeek`, `pageBehavior`, Provider `locale`, and `createCalendar`; React Aria documents the same headless API shape. Browser tests cover keyboard-opened/portaled popover behavior, Escape/outside dismissal, selection close, disabled state, required/invalid/help text, hidden input value, associated form data, and theme branches.                                          | Full APG/WCAG/ARIA-AT assistive-technology matrix remains planned for the date picker composition.          |
| Upstream React Source Parity             | covered | Source rows below map S2 DatePicker, React Aria Components DatePicker, React Aria date picker hooks, and React Stately date picker state to Solid owners. Solid keeps the popup Calendar at React S2 DatePicker medium calendar geometry for all field sizes, maps S2 `maxVisibleMonths` to headless `visibleMonths`, passes calendar state props into the headless DatePicker state, forwards Provider locale, supports custom calendar creation, connects the popup TimeField to DatePicker state for non-day granularities, and preserves hidden input/form owner behavior.               | None for the covered source branches.                                                                       |
| Solid Idiomatic Implementation           | covered | `createDatePickerState` owns `defaultOpen`, controlled `isOpen`, `onOpenChange`, calendar value, time value, and calendar state options. The wrapper follows existing S2 boundaries by normalizing string `firstDayOfWeek`, passing calendar props to headless state, and sharing DatePicker state with the popup TimeField instead of duplicating local time state.                                                                                                                                                                                                                         | None for DatePicker-owned implementation gates.                                                             |
| Accessibility And I18n                   | partial | Browser tests assert trigger focus restore after Escape, selection close/value update, outside dismissal, disabled styling and ARIA suppression, blank-field focus, label/help/error rendering, hidden `name` and `form` input attributes, native associated `FormData`, min/max disabled dates, unavailable disabled dates, locale spinbutton parity, seven-column custom calendar grids with no extra blank day column, and theme-reactive portaled popover colors. Package tests cover DatePicker labels, required invalid naming rows, form data participation, and zoned hidden values. | Screen-reader matrix rows remain open.                                                                      |
| Behavior State Machine                   | covered | `vp test run packages/solidaria-components/test/DatePicker.test.tsx` passed with 32 tests after adding associated form data coverage. Earlier focused state/hook/styled package tests cover open state, controlled values, popup time changes, and calendar state routing. `vp exec --filter @proyecto-viviana/comparison playwright test e2e/datepicker-visual.spec.ts --reporter=line` passed with 12 browser tests on 2026-05-19.                                                                                                                                                         | None for covered DatePicker state gates.                                                                    |
| Style Source-To-Computed Parity          | partial | Solid S2 DatePicker popup uses a fixed medium calendar surface in the popover, allows Calendar `UNSAFE_style` to override internal root sizing, preserves seven calendar columns with no extra blank day column, and reads hover paint from the actual inner Calendar cell. Browser tests cover strict normalized closed-field React-vs-Solid pair diff, field/icon sizing, settled popup width across S/M/L/XL fields, month nav, seven-column grid geometry, hover paint, disabled colors, light/dark popover colors, and the previously observed right-side blank column regression.      | Strict open-popover pair diff and a full style token ledger remain tracked.                                 |
| React-Vs-Solid Comparison Harness Parity | covered | React fixture imports real `@react-spectrum/s2` DatePicker; Solid fixture imports public `@proyecto-viviana/solid-spectrum` DatePicker. Both fixtures expose open-state evidence, controlled value evidence, route control props, hidden input values, associated form data, locale/calendar-system evidence, and settled popup geometry. The side panel drives docs-modeled DatePicker calendar, time, locale/calendar-system, form, and validation controls into both mounted styled stacks.                                                                                               | None for harness parity.                                                                                    |
| Evidence And Handoff                     | partial | Focused package tests, DatePicker browser comparison, DatePicker modeled controls contract, strict closed-field pair diff, form data evidence, and this note are updated on 2026-05-19.                                                                                                                                                                                                                                                                                                                                                                                                      | Component remains partial only for AT transcript rows and strict open-popover visual/token ledger evidence. |

## Official Docs And Viewer Inventory

| Docs item        | Official setting/example                                                                        | Route/control                                                                                                                                          | Status                    | Evidence                                                                     |
| ---------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | ---------------------------------------------------------------------------- |
| Primary example  | `<DatePicker />`                                                                                | Route uses `label="Due date"` plus description for comparable field semantics.                                                                         | deviation recorded        | S2 docs MCP, `datepicker-demo.ts`.                                           |
| Value            | `value`, `defaultValue`, `onChange`, `parseDate`, `parseDateTime`, `parseZonedDateTime`         | `value` route parses date-only, local date-time, and zoned date-time strings; both stacks sync controlled values and selection updates evidence attrs. | covered                   | `datepicker-demo.ts`, `datepicker-visual.spec.ts`.                           |
| Forms            | `name`, `form`, `isRequired`, `minValue`, `maxValue`, `isDateUnavailable`, `validationBehavior` | `name`, `form`, required/invalid, min/max, unavailable date, `validationBehavior`, and associated native `FormData` routes are covered.                | covered                   | `component-controls.ts`, `DatePicker.test.tsx`, `datepicker-visual.spec.ts`. |
| Open state       | `defaultOpen`, controlled `isOpen`, `onOpenChange`                                              | Headless state covers default/controlled open; both comparison fixtures track `onOpenChange`.                                                          | partial                   | `createDatePickerState.test.ts`, `DatePicker.test.tsx`.                      |
| Calendar popover | `maxVisibleMonths`, `pageBehavior`, `firstDayOfWeek`, `shouldFlip`                              | `maxVisibleMonths`, `pageBehavior`, and `firstDayOfWeek` are routed and browser-tested; `shouldFlip` placement edge cases remain open.                 | partial                   | S2 docs MCP, `DatePicker.tsx`, `datepicker-visual.spec.ts`.                  |
| Time fields      | `granularity`, `hourCycle`, `hideTimeZone`                                                      | Side-panel controls drive both fixtures; package and browser tests cover non-day granularity, hidden zoned values, and shared popup TimeField state.   | covered for parity routes | `DatePicker.test.tsx`, `datepicker-visual.spec.ts`.                          |
| Calendar states  | Provider locale and alternate calendar creation                                                 | `locale` and `calendarSystem` controls drive both fixtures; browser tests assert locale spinbutton parity and custom calendar seven-column grids.      | covered for parity routes | `component-controls.ts`, `styled.jsx`, `styled.tsx`, browser tests.          |

## Source Branch Ledger

| Source layer                 | Branch/behavior                                                                                                      | Solid owner/evidence                                                                                                           | Status  |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------- |
| S2 DatePicker                | Field label, FieldGroup, segmented DateInput, invalid icon, calendar button, HelpText, and generated S2 field styles | `packages/solid-spectrum/src/calendar/DatePicker.tsx`; strict closed-field pair diff in `datepicker-visual.spec.ts`.           | covered |
| S2 DatePicker                | CalendarPopover, fixed medium Calendar surface, `maxVisibleMonths`, `createCalendar`, resolved error message routing | `DatePickerPopup`, `Calendar`, `datepicker-visual.spec.ts` geometry, constraints, locale/custom calendar, and theme rows.      | covered |
| S2 DatePicker                | Popup TimeField for hour/minute/second granularities, hour cycle, hidden time zone state                             | `DatePickerPopup` TimeField wiring; package popup time test and browser date-time route.                                       | covered |
| React Aria Components        | Context props, DatePicker state context, DateField/Button/Calendar/Popover/Dialog contexts                           | Headless `DatePicker` context providers in `packages/solidaria-components/src/DatePicker.tsx`; package label/open/portal rows. | covered |
| React Aria date picker hooks | Group semantics, trigger ARIA, Escape/outside dismissal, selection close, focus restoration                          | `createDatePicker` bridge plus package and browser open/close/focus rows.                                                      | covered |
| React Stately DatePicker     | Controlled/uncontrolled value/open state, calendar value, time value, validation result                              | `createDatePickerState` and DatePicker package/state tests; browser selection and popup time rows.                             | covered |
| Hidden date input            | `name`, `form`, date/date-time/zoned serialization, associated native form data                                      | `HiddenDateInput`, package form data test, and browser associated `FormData` route row.                                        | covered |

## Covered

- DatePicker open state is owned by `createDatePickerState`, including `defaultOpen`, controlled `isOpen`, and `onOpenChange`.
- Solid S2 DatePicker accepts S2 string `firstDayOfWeek` values, normalizes them for headless state, and routes `maxVisibleMonths` as headless `visibleMonths`.
- Calendar min/max constraints, unavailable dates, Monday-first headers, two visible months, locale, and custom calendar creation are asserted in browser tests.
- Hidden input `name`, `form`, associated form data, date value, date-time value, and zoned value serialization are asserted through package and comparison routes.
- Solid S2 DatePicker popover uses the React S2 DatePicker medium Calendar geometry for every DatePicker field size.
- Popup TimeField is connected to shared DatePicker state for non-day granularities.
- The deterministic closed DatePicker field route has strict normalized React-vs-Solid pair-diff evidence.
- Calendar popup width is asserted against React after the popover settles for S/M/L/XL DatePicker sizes.
- Calendar hover assertions inspect the inner visible paint node while keeping the outer 32px cell geometry contract.
- Focused package tests, modeled-controls contract, and the DatePicker comparison browser suite passed.

## Remaining Gaps

- Replace the remaining thresholded open-popover visual comparison with stricter pair-diff evidence where practical.
- Complete a full style source-to-computed token ledger for DatePicker popup and calendar subparts.
- Add explicit assistive-technology matrix rows.
