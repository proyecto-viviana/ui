# #545 class 3 — breadcrumbs measurement signal (2026-09-22)

Main checkout, branch `main`, base `a78bfb91`. One writer seat. Every number
below comes from a command run in this session.

## Defect

`/showcase/navigation` and `/solid-spectrum/docs/components/breadcrumbs` were
the last two red routes, both `Hydration Mismatch. Unable to find DOM nodes for
hydration key`.

Owning branch, as diagnosed in the `ssr-545` worktree at `d6745471`:

- `packages/viviana-ui/src/breadcrumbs/index.tsx:674` and
  `packages/solid-spectrum/src/breadcrumbs/index.tsx:673`, fed by
  `canMeasureOverflow()` at viviana-ui `:186` and solid-spectrum `:185`.
- The element that throws is `packages/solidaria-components/src/Breadcrumbs.tsx:432`
  (`ElementTag`).
- One-command repro:
  `PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright VIVIANA_GATE=1 vp exec --filter @proyecto-viviana/web -- playwright test e2e/route-sweep.spec.ts --reporter=line --grep '/solid-spectrum/docs/components/breadcrumbs renders'`

The server emits the visible `<ol>` and never the hidden measurement copy. The
client emitted both, because `canMeasureOverflow()` reads `window` and that
boolean was the initial value of `canMeasure`. The measurement `<div>` is a
template, so a missing key is cloned; the copy's first `ElementTag` has no
template and no fallback, calls `getNextElement()` for child slot 5 of an owner
whose server child is the `<ol>` at slot 2, and throws.

## Measurement before the fix, worktree `ssr-545` at `d6745471`

- `vp run build` EXIT=0, 69.63s
- `vp run build:web` EXIT=0, 10.92s
- `vp run test:routes` EXIT=1, 82.06s; playwright totals `2 failed` /
  `173 passed (1.4m)` over `175` tests. 174 routes, 172 passing, 2 failing.
- Nothing that was green at `d1c5f4b3` is red there. No new failing route.

## Fix

One expression per file: `const [canMeasure, setCanMeasure] = createSignal(false)`
(viviana-ui `:267`, solid-spectrum `:266`). The `onSettled` block below it
already ran `setCanMeasure(canMeasureOverflow())` after commit, so the defect
was the initial value alone — no new lifecycle hook, no new export. The two
files stay a baselined diverged pair.

Upstream: pinned `@react-spectrum/s2` `src/Breadcrumbs.tsx` renders
`HiddenBreadcrumbs` unconditionally at `:542` and measures in the layout effect
at `:527`. Its first render reads no environment either.

## Proof that fails first

NEW `packages/solid-spectrum/test/Breadcrumbs.ssr.test.tsx`,
NEW `packages/solid-spectrum/test/Breadcrumbs.hydrate.test.tsx`,
NEW `packages/solid-spectrum/test/fixtures/breadcrumbs.tsx`, and a new case in
`packages/viviana-ui/test/Breadcrumbs.hydrate.test.tsx`.

Both hydrate halves stub a Chrome user agent: `canMeasureOverflow` returns false
for jsdom, so the pre-existing case never reached the branch a browser takes and
a test without the stub passes vacuously. Each one also asserts the measurement
copy is absent before hydration and present after.

- pre-fix (`git stash push` of the two source files, run, `git stash pop`):
  `Test Files 2 failed (2)`, `Tests 2 failed | 1 passed (3)`, EXIT=1, keys
  `...05x08051410d0` (solid-spectrum) and `...08051010c0` (viviana-ui), frames
  `getNextElement` then `staticElement`
- post-fix: `Test Files 2 passed (2)`, `Tests 3 passed (3)`, EXIT=0

The solid-spectrum half asserts the trail by text, not by item count: once
`canMeasure` is true the measurement runs for real and every jsdom element is
zero-width, so the trail collapses into the overflow menu there in a way a
browser layout would not. Item-count behaviour stays with the client-only
Breadcrumbs suite, and the test says so.

## Green on this tree

- `vp run build` EXIT=0 — `guard:package-artifacts — PASS: 1053 manifest target(s)`
- `vp run build:web` EXIT=0 — `built in 3.48s`
- `PLAYWRIGHT_BROWSERS_PATH=$HOME/.cache/ms-playwright VIVIANA_GATE=1 vp run test:routes`
  EXIT=0 — `175 passed (1.3m)`. 175 tests = 1 route-list guard + 174 routes, so
  174 routes, 174 passing, 0 failing. `/showcase/navigation` and
  `/solid-spectrum/docs/components/breadcrumbs` pass by name.
- `vp run test:ssr` EXIT=0 — 35 files, 92 tests
- `vp run test:hydrate` EXIT=0 — 31 files, 103 tests
- `vp run guard:layer-boundary` EXIT=0 — 84 diverged, 0 new forks
- `vp run guard:deploy-target` EXIT=0 — Worker `viviana-ui-docs`
- `vp exec tsx scripts/check-changeset-required.mjs` EXIT=0
- `vp lint` EXIT=0
- `vp run docs:generate` EXIT=0

## Red, and the record corrected

`vp run guard:idiomatic-solid` EXIT=1 on four `children()` snapshot-rendered
sites: `packages/viviana-ui/src/searchfield/index.tsx:426` and `:427`,
`packages/viviana-ui/src/textfield/index.tsx:325` and `:326`. These have been
called unrelated to this ticket. They are not: `git log -L` puts all four in
`096776df`, this ticket's own class-2 commit, where resolving each adornment
once with `children()` is what fixed `get suffix`. They want a baseline entry
with a ticket id, not a source change. This commit adds no fifth site.

## Twins — first sweep, call-seeded only

Every `createSignal` in `packages/*/src` seeded from a call, and whether it can
shift a hydration key:

| site | seed | can shift a key |
| --- | --- | --- |
| `solid-spectrum/src/calendar/DateField.tsx:372` | `isGlobalFocusVisible()` | no |
| `solid-spectrum/src/calendar/TimeField.tsx:366` | `isGlobalFocusVisible()` | no |
| `solid-spectrum/src/combobox/index.tsx:662` | `isGlobalFocusVisible()` | no |
| `viviana-ui/src/calendar/DateField.tsx:370` | `isGlobalFocusVisible()` | no |
| `viviana-ui/src/calendar/TimeField.tsx:364` | `isGlobalFocusVisible()` | no |
| `viviana-ui/src/combobox/index.tsx:674` | `isGlobalFocusVisible()` | no |
| `solid-spectrum/src/image/index.tsx:269` | `sourceCacheKey(local.src)` | no |
| `solid-spectrum/src/image/index.tsx:272` | `Date.now()` | no |
| `viviana-ui/src/image/index.tsx:290` | `sourceCacheKey(local.src)` | no |
| `viviana-ui/src/image/index.tsx:293` | `Date.now()` | no |
| `solidaria-components/src/Popover.tsx` | `shouldBeDialogBase()` | no |
| `solidaria-components/src/Tree.tsx` | `flatItems().length` | no |
| both `color/index.tsx` (5 each) | `parseColor(<literal>)` | no |

`isGlobalFocusVisible()` is `isFocusVisible()` in
`packages/solidaria/src/interactions/createInteractionModality.ts:305`, which
reads the module's `currentModality` — `null` on both sides — and each site
feeds only `isFocused() && isFocusVisibleModality()` into a `data-focus-visible`
attribute, never node creation. `sourceCacheKey` is props only;
`shouldBeDialogBase()` is props only; `Date.now()` does differ between the two
sides but is read only inside a `createEffect`, after commit.

That sweep ended: "Breadcrumbs was the only initial value that read the
environment. Nothing else to fix, and nothing dropped." Both halves were wrong.
The sentence is retracted here rather than edited out, and answered below.

## Twins — corrected under review, 2026-09-22, base `748070a1`

Re-run as an AST sweep: every `createSignal(...)` call in `packages/*/src`, via
`ts.createSourceFile`, classified by the shape of its initial value. Throwaway
script, run in this session, not committed. 1753 files, 367 `createSignal`
sites: 254 seeded from a literal, 24 with no initial value, 13 from an array or
object literal, 6 from other pure expressions, and 70 whose seed contains a call
or a property read. Where the first sweep went wrong, and what it owes:

1. **A "seeded from a call" filter cannot see a property read.** Five seeds are
   a bare property read: `props.open` in both `overlays/OpenTransition.tsx:45`
   (props), `flatItems().length` in `solidaria-components/src/Tree.tsx:884`
   (pure, already in the table above), and `mediaQuery.matches` at
   `packages/viviana-ui/src/skeleton/index.tsx:125` and
   `packages/solid-spectrum/src/skeleton/index.tsx:100`. The two skeleton sites
   do read the environment — and cannot shift a key. They sit inside
   `createPrefersReducedMotion()`, which returns `() => false` and never reaches
   the `createSignal` when there is no `window` or no `matchMedia`, so the server
   creates no signal at all; and the accessor is read only as `reduceMotion()`
   inside the `createEffect` in `useLoadingAnimation` (viviana-ui `:145`,
   solid-spectrum `:116`), which drives `Element.animate` after commit and
   creates no node.
2. **The table dropped 25 of its own kind.** 36 seeds are a direct call or
   `new`; the table carried 11. Dropped, all props, context or a fresh
   container, none able to differ between the two sides: `menu/ActionMenu.tsx:248`
   ×2 (`Boolean(local.defaultOpen ?? local.isOpen)`); `actionbar` `:292`/`:304`
   (`normalizeSelectedKeys(props.defaultSelectedKeys)`); `selectedKeySet(headlessProps…)`
   ×8 in `cardview`, `gridlist`, `table` and `tree` in both styled packages;
   `RangeSlider.tsx` `:571`/`:582` (`normalizeRange(local.defaultValue ?? …)`);
   `solidaria-components/src/Color.tsx:2084` and `:2165` (`normalizeColor(props)`);
   `SharedElementTransition.tsx:123` (`untrack` over props);
   `Tooltip.tsx:563` (`resolvePlacement(props.placement, direction())`, props plus
   locale context); and seven fresh `new Map()` / `new Set()` containers
   (`image/index.tsx:163`/`:183`, `Color.tsx:2161`, `Table.tsx:2503`,
   `Tree.tsx:883`, `createDraggableCollection.ts:54` and `:58`).
3. **Ten rows in the table are not code.** "both `color/index.tsx` (5 each) |
   `parseColor(<literal>)`" are JSDoc examples inside comments in
   `packages/{solid-spectrum,viviana-ui}/src/color/index.tsx`; the AST sweep
   finds no such signal in either file.
4. **Composite seeds were invisible too** — 24 of them, a call or a property
   read inside a larger expression. Fourteen pair a property read with a default
   or a comparison (`local.defaultOpen ?? false`,
   `(headlessProps.selectedItemCount ?? 0) !== 0`, `SIZES.length - 1`, and both
   hydration reads: `solidaria/src/ssr/index.tsx:204`,
   `solidaria-components/src/utils.tsx:592`). Ten wrap a call or a conditional:
   `breadcrumbs/index.tsx:820` ×2 (`headlessProps.isCurrent ?? itemContext?.isLast() ?? false`),
   `DatePicker.tsx:745`/`:748`, `Tooltip.tsx:454`, `color/index.tsx:672` ×2,
   `createFocusRing.ts:52`, `createInteractionModality.ts:404`, `refs.ts:25`.

That leaves fifteen seeds that read anything outside props, context, module
constants or a fresh object, and this is the whole list:

| seeds | verdict |
| --- | --- |
| `isGlobalFocusVisible()` ×6 (table above), `createFocusRing.ts:52`, `createInteractionModality.ts:404` | module `currentModality`, `null` on both sides; feeds a `data-focus-visible` attribute, never node creation |
| `image/index.tsx` `Date.now()` ×2 | differs across the two sides, read only inside a `createEffect` |
| `skeleton/index.tsx` `mediaQuery.matches` ×2 | signal exists only on the client; read only inside a `createEffect` |
| `solidaria/src/ssr/index.tsx:204` (`createHydrationState`), `solidaria-components/src/utils.tsx:592` (`useIsHydrated`) | deliberate: these do gate node creation through `<Show>`, and they are the shape breadcrumbs lacked — `isServer \|\| sharedConfig.hydrating` is true on the server and true again while the client hydrates, and a client-source effect flips it after commit, so the first render matches on both sides |
| `solidaria/src/i18n/locale.tsx:126` (`createSignal(currentLocale)`) | reads the environment and the two sides CAN disagree: `getDefaultLocale()` (`:55`) takes `window[localeSymbol]` or `navigator.language` on the client and falls back to `"en-US"` with no `navigator`. Unmeasured: whether any first render branches structurally on it. `grep` finds 55 `direction === "rtl"` sites in `packages/*/src`; the ones read here are class names, keyboard maps and transforms, none a `<Show>` — the rest are unread. Named on #545, not dropped |

Breadcrumbs remains the only initial value that read the environment **and** fed
node creation on the first render.
