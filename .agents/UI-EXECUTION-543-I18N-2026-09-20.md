# UI #543 runtime-owned I18n context — 2026-09-20

Start `0e85148d46338cfe056b608289adc96051aec4ac`, main. Root sole writer;
read-only independent review. The five existing #534 Hover timing paths remain
outside this slice. Exact commands, statuses and negative controls:
`/tmp/ui-execution-543-i18n-result.md`; logs share that basename prefix.

## Bounded repair and proof

The separately named locale.tsx extension replaces one process-global callable
context with a WeakMap keyed by the actual Solid createContext factory. A
context retains its creating runtime's owner state, so a dev restart must not
reuse it in a new generation. Same-runtime product graphs still share context;
legacy callable entries are replaced. No public API or dependency change.

Three actual-runtime owning regressions exercise module-copy sharing, A→B→A
locale/allocation and legacy replacement. Old source fails2/3: fr-FR becomes
en-US, childkey s0300 becomes s03, nextkey s04 becomes s05. Disabling sharing
fails3/3. Restored final source passes in fresh complete SSR78/78 (29files,
27.98s), followed by hydrate98/98 (27files,13.58s), one worker. Existing locale
71/71 also passes, retaining nested/default/direction behavior.

The rebuilt solidaria package is consumed by the actual comparison dev app.
Controlled Vite restart in PID1143034 changes the actual imported Solid factory
and generation1→2. Both D12 visits retain exact connected button/wrapper nodes,
keys s00001l1/s0000800000200020, and mouse/Enter each increment once, with no
browser diagnostics. Optimizer readiness precedes identity sentinels. This
closes the demonstrated cache mechanism, not every possible HMR/restart case
or already-mounted mixed pre-fix/post-fix consumer migration.

Fresh app paired SSR8/8 then client26/26 pass. Full hydrate logs are clean this
run, but app client proof still logs three default-HTML scan JSX parse errors
in unchanged vendored entries. Toolchain debt remains explicitly open.

Root typecheck and focused lint/format pass; root typecheck does not cover the
app-specific40errors from the preceding slice. Docs wrappers fail1 on sandbox
tsx IPC EPERM; direct node --import tsx generation/check pass0. Generated views
change board hashes only. Final scoped formatting and diff checks pass. Parent
audit initially hits sandbox child-process EPERM; reviewed rerun passes38/38.
The preceding external VisualMode finding is no longer reported; no parent or
other-checkout file was changed by this worker. Exact results are in the ledger.

Playwright skill guided actual-browser proof. Offline CLI ENOTCACHED required
the announced installed-library disposable probe; localhost sandbox EPERM
required reviewed escalation. No browser install, provider call or deployment.
Build/tests include separate dirty Hover files: worktree evidence, not exact-SHA
release acceptance. Independent source/test and browser-log review accepts
this bounded result with the above limits.

## Remaining

#543 stays in-progress: app hyperscript/fixture type and lifecycle repairs,
strict warnings, broader routes/interactions and diagnostics remain; web app
TanStack compatibility is separate. No broad app/package-readiness claim.
#531 remaining children/build/attribution, #139 packaging safety, #194 evidence,
#537 live2177same-revision zero-waiver proof, Changesets/security/site/consumer
and publication prerequisites remain. Owner holds and release boundaries are
unchanged. No packaging, full certified run, versioning or release occurred.
