# UI campaign #544, session 1 — 2026-09-20

Evidence and recovery context, not authority. The work lives on
[#544](../.claude/tickets/initiatives/544-cut-the-solid-2-release-candidate-and-its-public-face.md)
and its children #545–#551.

## Owner direction, as given

- About 15% weekly Fable and 23% Opus remain across three five-hour windows.
  Spend them on this repository, not on menial work. The RC is the aim, not a
  deadline. "The most important goal is iterative progress, don't lose
  anything when quota hits."
- RC as `-rc.N` on npm `next`, like Solid. Consumers adopt at once.
- TanStack `2.0.0-rc.8` bump approved.
- Docs scope: site landing and docs, npm READMEs, GitHub front door,
  comparison site.
- Standing for this campaign: push to `origin/main`, publish the RC to `next`,
  deploy the docs site.
- No Codex today. Opus, plenty of AGY, some Grok.
- No Blacksmith. GitHub Actions and local checks only.

## State found at 77f0de27

- Eligibility check passed: `eligibility-view: ok`.
- Three workflows red on main, two causes:
  1. `apps/comparison/scripts/merge-certified-reports.ts` line 12 lost its
     `import {` in `163f4377`. Breaks the format check in Release Readiness
     and Certification Gates, and the "Merge certified reports" step. All
     eight certified shards themselves passed.
  2. `apps/web` still carries TanStack Solid 1 packages that import
     `solid-js/web`. Breaks Site Gate. Now #545.
- All 11 workflow jobs run on `ubuntu-latest`; no Blacksmith reference.
- Five uncommitted #534 Hover files from an earlier session:
  `packages/solidaria/src/interactions/createHover.ts`,
  `packages/solidaria/test/createHover.test.tsx`,
  `packages/solidaria/test/createTooltip.test.tsx`,
  `packages/solidaria-components/test/Button.test.tsx`,
  `packages/solidaria-components/test/Tooltip.test.tsx`.
  The handoff records their timing run as 515/516, not green.

## Harness facts

- `engine doctor` finds all four kinds: claude, codex, grok, agy.
- The runtime's default catalog holds only `repo:viviana-ai`. Start it with
  `VIVIANA_ENGINE_REPOSITORIES` naming `repo:ui` and its root, or a launch
  into this checkout is refused.
- The host's Claude default model is Fable. `engine start` accepts `--model`
  (undocumented; the driver forwards it). Every Claude worker here must pass
  `--model claude-opus-5`, or it spends the Fable budget.
- Keep each launch's `launch_generation` and delivery id. Never start a
  second worker to retry a lost launch.
- A live worker launch into `repo:ui` was not yet proven when this was
  written.

## Recovery after a quota cut

1. Read #544's ordered work and the history of the ticket at the writer seat.
2. `git status`: paths named in a ticket's latest history line belong to that
   slice. Verify it with the ticket's proof, then commit or revert. Touch no
   unnamed path.
3. Audit findings are in `.agents/audit-2026-09-20/`; unverified ones stay
   unverified until reproduced.
4. Any model can resume. AGY and Grok need no Claude quota.
