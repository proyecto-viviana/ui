# UI #543 comparison Solid 2 app baseline — 2026-09-20

Start `5f150d2fbe7e97414632d1e8a0d5a0cd843319f9`, main. Root sole writer;
reviewers read-only. Preserve the five existing #534 Hover timing paths outside
this commit: solidaria createHover source/test and createTooltip test, components
Button/Tooltip tests. Exact commands, statuses and logs:
`/tmp/ui-execution-543-result.md`. Goal/release acceptance remains active/open.

## Repair and proof

The installed Astro Solid adapter uses Solid1 APIs absent from pinned rc.9.
Private comparison integration selects the already-installed Solid2 compiler
and runtime, without dependency/public-package changes. Paired owned props
stores/Loading boundaries preserve IDs, real server-node adoption, slots,
updates, disposal and visible failures. Async SSR is buffered complete output,
not shell-first streaming. Weak per-island event queues preserve delayed and
nested replay; flush older updates before entering another hydration context.

Ten app Solid files migrate forbidden nested cleanup or removed createResource:
the three async panels, DocsTopBar/DocsToc/color-scheme hook and four Button
fixtures/islands. Cleanups keep original listeners/options; panel pending shell,
exact failure identity and late-resolution disposal are proved. Independent
helper/lifecycle and panel reviews accept bounded progress, not ticket closure.

- Fresh adapter SSR8/8 then client26/26, one worker. Sixteen adapter tests plus
  ten actual CSR panel tests; panel tests do not claim SSR identity. Regressions
  cover slots/props/disposal, delayed/reverse/nested replay, old-root pending
  writes, real async payloads and Error/undefined/zero/false hydration failures.
- Production build initially fails on ComponentDetailMeta's createResource;
  after its named app-only migration it passes all91pages.
- Settled dev browser passes exact D12 button/wrapper adoption, connected old
  node and mouse/Enter each once on both Button routes. D12 has no diagnostics;
  manual viewer retains738strict warnings. Existing production-preview site
  suite7/7 and client-router suite1/1 pass, sequentially, one worker.
- Root typecheck, scoped lint/format/diff checks pass. App astrocheck separately
  fails40errors across435files (0warnings,37hints), mainly hyperscript callable
  types plus fixture props/imports and boolean onSettled returns. Not waived.
- Fresh full SSR75/75 (28files,28.36s), then hydrate98/98 (27files,12.64s), one
  worker. Hydrate exits0 but reports a failed default-HTML dependency scan;
  explicit root entry config remains unchanged. Keep toolchain debt visible.
- Docs wrappers fail1 with sandbox tsx IPC EPERM; direct node--import-tsx
  generation/check pass0. Generated changes only add543/update531 count/hash.
- Parent map gate is37/38: an external VisualMode CURRENT.md retired-word finding
  remains; no parent/other-checkout edit or authority expansion.

The Playwright skill guided real browser proof. CLI permission/offline-cache
failures required the announced installed-library disposable probe fallback;
localhost sandbox/browser EPERM required reviewed escalation. No browser install,
new browser spec, external provider or deployment. Earlier failed browser logs
retain Vite-restart key drift, a readiness-free counter timeout, and optimizer
reload invalidating identity sentinels. The final probe uses existing route
readiness and Solid-specific selectors, not weaker assertions.

## Limits and remaining work

#543 remains in-progress. App type errors, strict warnings, remaining fixture
lifecycle/manual disposer migration, automatic restart parity and broader
component interaction gates remain. Arbitrary overlapping unresolved roots,
CSR-only parents and live chunk arrival are outside the adapter proof; existing
navigation checks do not certify those cases. Main web/TanStack compatibility
is separate and still unresolved.

Read-only installed-runtime proof identifies a stale callable global I18n
context mechanism across module generations: owner key s0100 becomes s01 and
fr-FR becomes null; a current-runtime context restores both. A separately named
locale.tsx/owning-test extension and controlled Vite restart proof must precede
product repair/causal closure. Refresh alone was not isolated as the cause.

No product source change belongs to #543 here; proofs include the separate
dirty Hover slice and are worktree evidence, not same-SHA release acceptance.
#531 children/build/attribution, #139 packaging safety, #194 evidence checks,
#537 live2177same-revision zero-waiver gate, Changesets/security/site/publication
requirements and owner holds remain. No packaging, full certified run, dependency
change, versioning, deployment or release occurred.
