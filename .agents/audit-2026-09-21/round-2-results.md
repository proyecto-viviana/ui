# Adversarial audit, round 2 — results (2026-09-21)

Range `4acbc9e4..65254a8c` (16 commits — the ones written after round 1's range
closed). 3 Opus auditors over three slices, each finding >= medium challenged by
an Opus skeptic. No builds or tests were run by the fleet (the checkout is
occupied by the writer seat); `needs-run` means exactly that. Verdicts:
confirmed / partly (core holds, scope or severity corrected — read the skeptic
line) / low-not-challenged. 15 findings survived: 0 critical, 3 high, 4 medium,
8 low.

Owners: [`OWNERS.md`](./OWNERS.md), round-2 block. Round 1:
[`round-1-results.md`](./round-1-results.md).

## Corrections made while transcribing

The fleet's text is kept as written except where a fact in it is wrong. Two are,
and both are load-bearing:

1. **`r2-guards-4` cites `apps/comparison/src/data/parity-strict-baseline.json`.
   That file does not exist.** The JSON is at
   `apps/comparison/scripts/parity-strict-baseline.json`; the test that pins it
   is at `apps/comparison/src/data/parity-strict-baseline.test.ts`. The
   finding's mechanism is unaffected — 27 of 27 entries are there.
2. **`r2-guards-3` mixes local and UTC times**, as its own skeptic says. The
   census `65254a8c` was committed **2026-09-21T13:46:11Z**, not 10:46; 10:46 is
   its recorded `-0300`. Every commit time in that finding is `-0300` and every
   run time is UTC, so the real gap is three hours wider than the finding reads.
   Verified here with `TZ=UTC git log --date=format-local:'%Y-%m-%dT%H:%M:%SZ'`
   over the range: `495582e9` 05:18:08Z, `413b2f23` 05:47:23Z, `a32e6bab`
   05:47:41Z, `f8e5833e` 06:00:12Z, `7e93d238` 06:18:18Z, `6ad3d12d` 06:55:32Z,
   `6e43c436` 07:11:38Z, `56c0751c` 07:20:48Z, `65254a8c` 13:46:11Z. Run
   35558449632 was created 03:42:53Z and finished 04:06:14Z, and
   `git rev-list --count eb75ee0e..65254a8c` is 18.

## CRITICAL

None. The one critical of round 1, `guards-a/eib-test-red` (#587), is untouched
by this range.

## HIGH

### r2-certified-a/r2a-1 — ActionButton bypasses the Form proxy it just adopted, so `<Form isDisabled>` does not disable it
- verdict: confirmed
- truth: `7e93d238` wrapped ActionButton's merge in `useFormProps`, but its `isDisabled` accessor still reads `runtimeProps` → group → context → provider flags and is passed explicitly AFTER the spread, so the proxy is bypassed. Inside a Form with `isDisabled` (or a Skeleton), upstream's ActionButton is disabled and ours is not. Button, LinkButton and ToggleButton all read it through the proxy.
- evidence: `packages/solid-spectrum/src/button/ActionButton.tsx:187-191` (accessor) and `:480` `isDisabled={isDisabled()}` placed after `{...headlessProps}` at `:478`, so it wins. Form publishes it: `packages/solid-spectrum/src/form/index.tsx:222-224`. Upstream disables: `@react-spectrum/s2@1.7.0/src/ActionButton.tsx:334` `props = useFormProps(props as any)` then `:357` `isDisabled={props.isDisabled ?? isDisabled}`. The commit's own new test covers size only: `packages/solid-spectrum/test/Form.test.tsx:208-232`.
- skeptic: `ActionButton.tsx:187-191` chain omits Form and Skeleton; `:480` `isDisabled={isDisabled()}` follows `{...headlessProps}`. Probed Solid 2 merge in node: a later source owning the key wins even with `undefined`. ToggleButton (`:155`) and Button (`:120`) read the proxy; the changeset claims "and the other Form props".
- action: End ActionButton's `isDisabled` chain on the proxy (`?? headlessProps.isDisabled`, as `Button.tsx:120-121` and `LinkButton.tsx:125` do) keeping `runtimeProps` ahead of the group, and add a `<Form isDisabled>` case to the new `it.each`.
- run: `vp run test:run packages/solid-spectrum/test/Form.test.tsx`

### r2-guards/r2-guards-1 — #574's covered-path set omits the certified gate's own runner, merger, waiver logic and pinned oracle, so a postcard survives changes that decide pass/fail
- verdict: partly
- truth: `certifiedSuiteCoveredPathspecs` covers component source and fixtures only. Everything that turns a certified run into a verdict sits outside it: `apps/comparison/scripts/**` (`merge-certified-reports.ts` exits 1, `certified-waivers.ts` decides `waiverGateFails`, `certified-summary.ts` holds the budgets, `report-component-parity.ts` is the gate itself), `playwright.config.ts` (retries, testMatch), `astro.config.mjs`, `apps/comparison/package.json` and `pnpm-lock.yaml` (the pinned upstream oracle), `apps/comparison/vendor/**` and `packages/*/package.json`.
- evidence: `apps/comparison/src/data/certified-suite-evidence.ts:38-44` lists only `packages/*/src/**`, `apps/comparison/src/**`, `apps/comparison/e2e/**`. `git ls-files apps/comparison/scripts` = 17 files, none matched. Real history: `git diff --name-only 9e0df73c^ 9e0df73c -- <those pathspecs> | wc -l` = 0, and `9e0df73c` is "#194: restore the dropped import in the certified report merger" — a commit that repaired `apps/comparison/scripts/merge-certified-reports.ts`, the job whose exit code IS the gate. Same for `apps/comparison/vendor/**` (174 files) and the pins at `apps/comparison/package.json:66-71`.
- skeptic: Verified: `evidence.ts:38-44` covers only `packages/*/src` and `apps/comparison/{src,e2e}`; its own comment says `scripts` cannot move a result, yet `merge-certified-reports.ts:181-186` carries the gate exit and `package.json:66-71` pins the oracle. Latent, not live: the postcard is already 2577 covered-paths stale and certified shards rerun every push.
- action: Widen the list before any fresh postcard is pinned: add `apps/comparison/scripts/**`, `apps/comparison/*.config.*`, `apps/comparison/package.json`, `apps/comparison/vendor/**`, `packages/*/package.json` and `pnpm-lock.yaml`. Safer still, invert the rule — cover everything and exclude a short, reviewed allowlist (`.claude`, `.agents`, docs, `**/*.md`, the evidence file) — so the default for a new directory is "invalidates" rather than "invisible".

### r2-guards/r2-guards-3 — The 65254a8c census is stale-on-arrival: it sends the next writer at work that landed hours earlier and calls a handed-back ticket cheap
- verdict: confirmed
- truth: The census was committed 2026-09-21 10:46 `-0300` (13:46Z, see the correction above) but reports CI run 35558449632 on `eb75ee0e`, created 03:42Z — before every fix in this range. It is written in the present tense ("where every row now belongs", "the largest untracked block left", "this table is the whole distance"), so a reader takes it as current state. It is not.
- evidence: `gh run view 35558449632` → headSha `eb75ee0e`, createdAt 2026-09-21T03:42:53Z. Commit times (recorded `-0300`): `495582e9` #585 02:18, `413b2f23` #584 02:47, `a32e6bab` #583 handback 02:47, `f8e5833e` popover-surface 03:00, `7e93d238` form 03:18, `6e43c436` calendar 04:11, `6ad3d12d` #582 03:55 — all before `65254a8c` at 10:46. Yet the table says form "none — gap" / "never ticketed. The largest untracked block left", calendar "ungraded", and closes with "Closing the three that are ticketed and cheap — #585, #584, #583". At `65254a8c`: #585 `merged`, #584 `in-progress`, #583 `open` because `a32e6bab` handed it back ("the branch it deletes is #484's ruling").
- skeptic: `gh`: run 35558449632 headSha `eb75ee0e`, completed 04:06Z; census committed 13:46Z, 18 commits later. At `65254a8c` #585 is merged, #584 in-progress, #583 open/handed-back over #484, form fixed in `7e93d238` — yet the table calls the three cheap. Finding mixes local and UTC times; the gap is wider.
- action: Re-head the table as a snapshot of run 35558449632 @ `eb75ee0e`, add a "landed since" column naming `495582e9`, `413b2f23`, `f8e5833e`, `7e93d238`, `6e43c436` and `6ad3d12d`, and move #583 out of "cheap" into owner-blocked. Then let the next Certification Gates run on the pushed tip supply the real roster rather than re-deriving it by hand.

## MEDIUM

### r2-certified-a/r2a-5 — The picker trigger renders no `data-pressed`, where RAC's carries it for the whole time the popover is open
- verdict: confirmed
- truth: RAC feeds its trigger Button `isPressed = state.isOpen`, so upstream's button carries `data-pressed="true"` while the listbox is open. Ours computes `isPressed` only into render values and never emits the attribute. #584 audited exactly this element and its Done-when is that the trigger "carries what RAC's does and nothing more"; a missing state attribute is the other direction of the same defect and was not caught.
- evidence: `react-aria-components@1.21.0 dist/private/Select.mjs:157-163` ButtonContext `{...triggerProps, ref, isPressed: state.isOpen, autoFocus}` and `dist/private/Button.mjs:97` `"data-pressed": renderValues.isPressed || undefined`. Ours: `packages/solidaria-components/src/Select.tsx:894-900` puts `isPressed` in renderValues only; the button at `:932-957` emits `data-focused`/`focus-visible`/`hovered`/`disabled` and no `data-pressed`. `data-pressed` is in the compared allowlist (`journeys.ts:78`).
- skeptic: RAC sets ButtonContext `isPressed: state.isOpen` (`Select.mjs:157-163`); `Button.mjs:97` emits `data-pressed`. Our SelectTrigger (`Select.tsx:932-957`) emits none, the styled picker adds none, and `data-pressed` is allowlisted (`journeys.ts:79`). Step-0 focus failure aborts the spec (`journeys.ts:238-275`), so later steps' dom is never compared.
- action: Emit `data-pressed={dataAttr(isPressed())}` on SelectTrigger. Note the D13 evidence is weaker than the receipt reads: the open-arrow step 0 already fails on focus, so later steps — the ones where the popover is open — are never reached, and "neither fails on dom any more" cannot cover them.

### r2-certified-b/F1 — Every gate number in these commits is local-only; nothing is pushed and main's gate is red with Site Gate disabled
- verdict: partly
- truth: None of the audited commits is on `origin/main`. Main's three newest Certification Gates runs are failures, the latest covering origin tip `96376e9a`. Site Gate and Release Readiness are `disabled_manually` — yet #586 names Site Gate as its blocker. So every "114 passed", "11 of 11", "5 passed" is an unreplayed local claim, and a ticket set to merged on such a number is a gate that lies.
- evidence: `git merge-base --is-ancestor <sha> origin/main` → false for all sixteen commits in the range. `git rev-parse origin/main` → `96376e9a` (its Certification Gates run 35560076342, 2026-09-21T04:11:59Z, `failure`). `gh run list --branch main --limit 4` → the three most recent "Certification Gates" runs all `failure`, newest 35560076342 head `96376e9a`. `gh workflow list --all` → "Site Gate disabled_manually", "Release Readiness disabled_manually".
- skeptic: Verified: the commits are unpushed, `origin/main` `96376e9a`, three newest Certification Gates runs failed, two workflows disabled. But `viviana-projects/spec/ticket-scheme.md:75` defines `merged` = "landed in `<sha>`", `verified` = the later transition; #586 is `in-progress` and says its run is owed; the owner disabled the gates (handoff §11). No gate lies.
- action: Before treating any of #582/#586/#576 as closed: push main and let Certification Gates run on the tip, then re-enable and run the disabled workflows (`gh workflow enable "Site Gate"`). Until a run id exists for this tree, the merged statuses stand as `merged`, not `verified`, and every receipt that cites a local count says unverified.
- run: `git push origin main && gh run list --branch main --limit 1`

### r2-certified-b/F2 — #582 reverses an earlier certified fix on the same D2 rows, and neither the oracle nor the unit test can tell the two directions apart
- verdict: partly
- truth: `6ad3d12d` deletes the exact hold that `6383939c` (2026-09-02, "#257 #251") added to turn the same four DatePicker D2 rows green. Both opposite implementations were certified green by the same driver, so D2 discriminates only when a fixture happens to flip. The one unit test of the seam is green on both. A fix proved by a test that fails on the pre-fix code does not exist here.
- evidence: `git log -S "entering ? preferred" --oneline` → `6383939c` adds, `6ad3d12d` removes. `packages/solidaria-components/test/Popover.test.tsx:1234` renders `<Popover placement="bottom start">` and asserts `data-placement === "bottom"`; in JSDOM all rects are 0 so the measured placement is also `"bottom"` — green under the old hold (`entering ? preferred : ...`) and under the new pass-through (`placement: popoverAria.placement() ?? preferred`, `Popover.tsx:789`).
- skeptic: Confirmed: `6ad3d12d` deletes `6383939c`'s hold and adds no test; `Popover.test.tsx:1234` (added by `0ee3511c`) asserts the seed and passes both ways. Refuted: #582 records a mutation (hold restored → D2 18/8, exactly those four), and `4f1c8435` (2026-09-15, after the hold) centres the canvas, explaining the earlier green.
- action: Add a Popover test that forces a flip (stub the measured placement to `"top"` while `placement="bottom start"`) and assert `data-placement === "top"`; confirm it fails on `6ad3d12d^`. Then needs-run both directions of the D2 rows and record in #582 why the earlier certification was wrong.
- run: `cd apps/comparison && vp run test:datepicker   # at 6ad3d12d and at 6ad3d12d^`

### r2-guards/r2-guards-2 — The #574 tests are shaped to the rule and cannot fail on its hole; the temp-repo case pins the hole open
- verdict: partly
- truth: Neither new test ever writes a file that is uncovered-but-decisive. The real-git fixture only creates `packages/p/src/a.ts`, `apps/comparison/e2e/a.spec.ts`, `docs/a.md`, `.agents/receipt.md`, a README and the evidence file — never `apps/comparison/scripts/**`, a `package.json` or a config. Worse, the first case asserts `current:true` after committing that hand-picked uncovered set, so it locks in the blind spot as the expected behaviour rather than probing it.
- evidence: `apps/comparison/src/data/certified-postcard-git.test.ts` (added in `13080aa0`): `beforeEach` commits three files; "passes on the recorded commit, and after the commit that records it" commits the evidence file, `packages/p/README.md`, `docs/a.md` and `.agents/receipt.md` and expects `{current:true}`. `acceptance-schema.test.ts`'s probe stubs `changedCoveredPaths` with a fixed array, so `certifiedSuiteCoveredPathspecs` is never exercised by any test. The receipt's mutations (drop the exclusion → 1 fails; restore equality → 4 fail) only re-test branches the rule already had.
- skeptic: Fixture facts exact: it writes only `packages/p/src`, `e2e`, `docs`, `.agents`, README, evidence file; `acceptance-schema` stubs `changedCoveredPaths`, so the pathspec list is never exercised. But the `current:true` case commits only deliberately excluded paths — it pins intended exclusions, not the decisive hole. Coverage gap, not fake proof.
- action: Add a case to `certified-postcard-git.test.ts` that commits `apps/comparison/scripts/merge-certified-reports.ts` and `apps/comparison/package.json` into the fixture repo and asserts `{current:false}`. Write it before widening the pathspecs so it fails on today's code, then let the widened list turn it green — that is the proof #574 is currently missing.

## LOW

### r2-certified-a/r2a-4 — The invented trigger attributes were stripped at the component, not removed at the hook that ships them
- verdict: partly
- truth: #584 cured `data-open` by deleting it in SelectTrigger's `cleanTriggerProps`. The cause is one layer down: `createSelect`, published in solidaria, still invents `data-open`, `data-disabled` and `data-focus-visible` as raw booleans on `triggerProps`, and a unit test pins that behaviour. Any consumer spreading `createSelect`'s `triggerProps` gets attributes react-aria's `useSelect` never emits. The ticket discloses this in its tail; no ticket files it.
- evidence: `packages/solidaria/src/select/createSelect.ts:450-452` `"data-open": isOpen || undefined,` / `"data-disabled": isDisabled || undefined,` / `"data-focus-visible": isFocusVisible() || undefined`; pinned by `packages/solidaria/test/createSelect.test.tsx:98` ("sets data-open when open"). Masked downstream at `packages/solidaria-components/src/Select.tsx:913-924`. RAC renders `data-open` on the Select root only (`dist/private/Select.mjs:210-215`).
- skeptic: Invented attributes are real: react-aria `useSelect` emits no `data-*` (`dist/private/select/useSelect.mjs`), RAC renders them on the root only. But solidaria `mergeProps` coerces boolean `data-*` to `"true"` (`domAttrs.ts:62-70`), not `""` — which is why the pinned test asserts `toBe("true")`. #254 already names the trigger `data-open`, owner-gated.
- action: Drop the three attributes from `createSelect` and retire the test that pins `data-open`, then delete the strip in `cleanTriggerProps`; the hook is published API, so the divergence ships even though the certified panel is clean. The auditor's "in Solid's empty-string form" is wrong — the skeptic's `"true"` is the shipped value.

### r2-certified-a/r2a-6 — The oracle's `data-*` policy constant is exported, documented and never used
- verdict: low-not-challenged
- truth: `dom-oracle.ts` documents that journey DOM snapshots ignore `data-rac` and that no other `data-*` is ignored, and exports a constant to that effect. Nothing imports it. Journey snapshots in fact use a positive allowlist that happens to exclude `data-rac`, so behaviour is right but the documented mechanism does not exist — a reader reconciling attribute findings against this comment will be reading a policy that is enforced somewhere else, by different rules.
- evidence: `apps/comparison/e2e/drivers/dom-oracle.ts:1-9` (comment plus `export const ORACLE_IGNORED_DATA_ATTRIBUTES = ["data-rac"] as const;`); `grep -rn "ORACLE_IGNORED_DATA_ATTRIBUTES" apps/comparison` returns only that declaration. The real filter is the allowlist at `apps/comparison/e2e/drivers/journeys.ts:74-99`, applied in `journeys-observe.ts:400-403`.
- action: Delete the constant and fold its `data-rac` paragraph into the `RAC_STATE_DATA_ATTRIBUTES` comment, which is where the rule actually lives. Pre-existing, outside the five commits; recorded because every attribute claim in #584 rests on this oracle.

### r2-certified-b/F3 — "as upstream" is half true: we still enter before we are placed, so a flipped popover animates -4px then reverses to +4px
- verdict: partly
- truth: RAC gates `isEntering` on `!!placement` and passes `placement` through as `undefined` while unmeasured, so React never paints an entering frame before placement exists. Ours still reports entering while unplaced and substitutes the preferred axis, so a flipped popover paints the bottom keyframe (`translateY -4`) for a frame, then swaps to `+4` inside a running 200ms translate transition. React plays no such reversal.
- evidence: `react-aria-components 1.21.0 dist/private/Popover.mjs:100` `let enterAnimation = useEnterAnimation(ref, !!placement)` and `:112` `values: {..., placement: placement, isEntering, isExiting}`. Ours: `packages/solidaria-components/src/Popover.tsx:774-776` `isEntering = ... || (isOpen() && popoverAria.placement() == null)` and `:789` `placement: popoverAria.placement() ?? preferred`. Keyframes `packages/solid-spectrum/src/popover/index.tsx:118-130` (top `+4` / bottom `-4`) with `:144-145` `transition: "[opacity, translate]", transitionDuration: 200`.
- skeptic: Upstream citations check out (`animation.mjs:18-37` returns `isEntering && isReady`; `Popover.mjs:100/112/189`). But entering-while-unplaced predates `6ad3d12d` and is documented at `Popover.tsx:760-768`; the commit claims only the placement pass-through, which matches RAC. The painted reversal is unproven: D2 pauses the enter's first frame and passes.
- action: Gate `isEntering` on a resolved placement and let `data-placement` be absent until measured, matching `Popover.mjs:100/112` — or, if the Solid post-paint measurement makes that impossible, record it in #582 as a named local deviation instead of claiming "as RAC's does".
- run: `cd apps/comparison && vp run test:datepicker -g "open-enter"  # record every frame's translate, not just the captured one`

### r2-certified-b/F4 — #576 is marked merged although its Done-when asks for DOM evidence the ticket never records, and the trap it names is documented, not guarded
- verdict: partly
- truth: The Done-when requires the ticket to record which of three candidates it was and the DOM evidence that decided it. The note names the candidate and a dist-rebuild mutation but carries no DOM print from Work step 1, so the choice rests on inference. Separately, `8aedc891` documents the stale-dist trap only as a `vite.config` comment; the analogous trap in `apps/comparison` is held by a guard script.
- evidence: `.claude/tickets/tasks/576-*.md` front-matter `status: merged`, Done-when text vs the appended note (no `[role=region]` / `[role=alertdialog]` output). `apps/web/vite.config.ts` comment added by `8aedc891`: "A web build therefore tests the last package build, not the working tree: rebuild a changed package before a local web gate (#576)." Compare `guard:comparison-atom-css` in the comparison-build gate, which enforces the same class of trap.
- skeptic: Confirmed: no `[role=region]` print in #576, and the stale-dist trap is only a comment. But the note names candidate 1 and records a mutation (`() => fn` + dist rebuild → 2 failed on that exact locator; restored → 2 passed), excluding candidate 2. Guard largely moot: `certification-gates.yml` builds at `:211` before `a11y:full` at `:258`.
- action: Paste the Work-step-1 DOM output into #576, or say on the ticket that the candidate rests on the mutation and not on a DOM print. The scheme has no backward transition from `merged`, so the note is the correction.

### r2-certified-b/F6 — 56c0751c ships a dark-mode landing restyle axe cannot measure and leaves the one hardcoded red it touched
- verdict: low-not-challenged
- truth: The hero gradient reader is `background-image` with transparent text fill, which axe's `color-contrast` rule reports as incomplete — it was never in the 30-instance tally, yet the swap changes dark from `#0a6fef` to `#3dadff`: a visible landing restyle shipped under an a11y ticket with no visual proof. The same commit leaves the new `ErrorFallback` `h2` at a hardcoded `#ef4444`, roughly 3.45:1, passing only as large text.
- evidence: `apps/web/src/components/theme/studio.css:640` `background: linear-gradient(135deg, var(--text-link), #a855f7)` on `.pv-hero h1 span`; `:278` and `:605` likewise moved off `var(--pv-accent-solid, #3b82f6)` and dropped the literal fallback the rest of the file keeps. `apps/web/src/routes/__root.tsx:82` `ErrorFallback`: `<h2 style={{ color: "#ef4444", ... }}>`.
- action: Put the `h2` on a token (`--text-negative` or equivalent) so the boundary is not the one hardcoded colour in the file, and either restore literal fallbacks on the three new reads or drop them consistently across `studio.css`. Note the dark hero change in #586 as a deliberate visual edit, not an a11y fix. (Line numbers here are the auditor's; at `fa0686da` the gradient is `studio.css:640`, the reads `:281` and `:605`.)

### r2-guards/r2-guards-4 — #194's "only shrinks" is a fixed ceiling, not a ratchet: a slug forced out of the baseline can be re-added later with both checks green
- verdict: partly
- truth: `staleBaselineSlugs` only fires when a listed gap is absent, and the growth pin compares against `frozenBacklog`, a hardcoded nine-slug array that never shrinks when the JSON does. So once a gap closes and its entry is deleted, a later regression can be re-baselined for the same slug and section: the gap exists, so it is not stale; the slug is in the nine, so the pin passes. The commit title's claim is unenforced in the direction that matters.
- evidence: `apps/comparison/scripts/parity-strict-baseline.ts` (`7ec2a732`): `staleBaselineSlugs` returns `allowed.filter(slug => !present.has(slug))`. `apps/comparison/src/data/parity-strict-baseline.test.ts`: `frozenBacklog` is a literal array of nine slugs whose only guard is a comment, "Delete a slug here when its gaps close; never add one". `apps/comparison/scripts/parity-strict-baseline.json` (**not** `src/data/…`, see the corrections above) already lists all nine in all three sections — 27 of 27 possible entries — so the pin has no slack left to remove and can only ever refuse a tenth slug.
- skeptic: Mechanism exact: `staleBaselineSlugs` fires only on absent gaps; `frozenBacklog` is nine literals; the JSON holds 27/27. Grep finds no other guard, so a deleted-then-regressed slug re-enters clean. Contained: no slack exists today, and re-adding means editing a constant whose comment forbids it. The title overclaims.
- action: Make the pin shrink with the JSON: record a per-section count (or the section's own committed contents) in a separate frozen snapshot and fail when a section's length exceeds it, so deleting an entry lowers the ceiling permanently. Until then, say in the workflow comment that the baseline is capped at #85's nine rather than that it "only shrinks".

### r2-guards/r2-guards-5 — #139's containment is relative to `os.tmpdir()`, which is read from the same untrusted environment as the override it is checking
- verdict: partly
- truth: `scratchDir` refuses anything outside `tmpdir()`, but `tmpdir()` is `TMPDIR`/`TMP`/`TEMP` — the same env object the override comes from. With `TMPDIR=$HOME` and `VIVIANA_CONSUMER_DIR=$HOME/Documents` the path is strictly inside "the temp directory", neither holds nor sits inside the repo, so it is accepted and recursively force-deleted. The guard survives one mistyped variable, not two.
- evidence: `scripts/scratch-dir.mjs` (`80c429ec`): `const tmpRoot = realPath(tmpdir());` then `if (!isInside(path, tmpRoot)) refuse(...)`. Node's `os.tmpdir()` reads `process.env.TMPDIR` per call, which is exactly why the commit's own sandbox tests work — `scratch-dir.test.ts` spawns each script with `env: { ...process.env, TMPDIR: childTmp, [variable]: repo }` to relocate the temp root. The delete then lands at `consume-pack-smoke.mjs:109`, `rmSync(consumerDir, { recursive: true, force: true })`.
- skeptic: `scratch-dir.mjs` anchors on `realPath(tmpdir())`, which Node reads from `TMPDIR` — proved by `scratch-dir.test.ts` relocating it through child env. `TMPDIR=$HOME` plus `VIVIANA_CONSUMER_DIR=$HOME/Documents` passes all three checks. But it needs two misconfigured variables where pre-#139 one sufficed, so the guard is strictly stronger.
- action: Constrain the victim as well as the location: require the resolved path's last segment to be one of the names these scripts own (`viviana-ui-packs-chain`, `viviana-ui-consume-smoke`, `viviana-ui-pack-stage-*`), so an override can move where the scratch directory lives but never point the delete at an unrelated directory that happens to sit under a relocated `TMPDIR`.

### r2-guards/r2-guards-6 — pack-local-chain now leaks a fresh stage directory holding seven package source trees on every run
- verdict: low-not-challenged
- truth: With `VIVIANA_PACK_STAGE` unset the stage is `mkdtempSync`'d, so each run gets a unique path. The script `rmSync`s and recreates that brand-new directory at the top and never removes it at exit — it only prints it. The old pid-named path was at least reused and cleared by a later run; the new one accumulates, one full copy of the seven packed packages per invocation.
- evidence: `scripts/pack-local-chain.mjs` (`80c429ec`): `mkdtempSync(join(tmpdir(), "viviana-ui-pack-stage-"))` at `:21-23`; `rmSync(stageRoot, {recursive:true, force:true})` then `mkdirSync(stageRoot, ...)` at `:130-133`; the last mention is `process.stdout.write(\`\nStage directory: ${stageRoot}\n\`)` at `:168`. No cleanup path exists.
- action: Remove the now-redundant `rmSync`/`mkdirSync` pair for a freshly `mkdtemp`'d stage and delete `stageRoot` at exit (keep it only behind a `--keep-stage` flag or when `VIVIANA_PACK_STAGE` was supplied, since that is the case where the operator asked for a stable path to inspect).

## Refuted by the skeptics (not findings)

- r2-certified-a: **#584's receipt claims a clean twin survey; Menu and Link still emit the same raw-boolean `data-*`** — Both cited sites flow through solidaria `mergeProps`, which coerces boolean `data-*`/`aria-*` to `"true"` (`mergeProps.ts:196-199`, `domAttrs.ts:68`). Menu's `:1620` value is additionally shadowed by `{...dataAttrs()}` spread last (`Menu.tsx:1704-1716, 1766-1779`). No empty string reaches the DOM; the survey note holds.
- r2-certified-a: **The picker trigger's focus-visible still comes from the root's within-ring, so the two focus attributes now disagree** — `Select.tsx:511-512` is `childRenderValues`, not the context. `SelectContext` (`:842`) passes `selectHook`'s `isFocusVisible = isFocused() && ring` (`createSelect.ts:509`), whose `focusProps` sit on the trigger (`:423`) chained with the trigger's own ring, so `data-focused` and `data-focus-visible` clear on the same blur.
- r2-certified-b: **The calendar color-scheme diagnosis contradicts CSS inheritance; the right change may have shipped for the wrong reason** — The cited file refutes it: `dist/styles.css` opens `@layer _.a{:where(:root,:host){--s2-color-scheme:light dark`, so the atom resolves to a literal `light dark`, not IACVT/inherit. Theme is pinned via localStorage (`visual-diff.ts:125`), not `prefers-color-scheme`. The receipt records mutation D3 11/11; CI run 35560076342 lists those rangecalendar dark rows red.

## Checked and held

- r2-certified-a: **#585/`495582e9` fixed the COMPONENT, not the fixture or the oracle**, and correctly overrode a wrong conductor brief: S2 styles Picker's list with ComboBox's `listbox` (`Picker.tsx:71` import, `:515` `className={listbox({size})}`), not the local `menu` (`:231`, exported and unused), and `ComboBox.tsx:251-261` has no padding and the identical `overflowY`/`overflowX` split — so `pickerListBox` already matched upstream and is untouched. The real defect (SelectListBox never consumed the parent Virtualizer's `CollectionRoot`) is a genuine port gap, fixed as the ComboBoxListBox twin, with a row-exact mutation run recorded.
- r2-certified-a: **`f8e5833e` is a revert, not a fixture hack**: `git show e4c3b26b` proves the eager `createComponent(Keyed, …)` form was introduced there and the file previously held a thunk. The pair is legitimate because both sides read the scheme from context the same way (`solid-spectrum/src/popover/index.tsx:27,81,241` `useTheme`/`setColorScheme` vs s2 `Popover.tsx:199,255` `ColorSchemeContext`); only Solid can lose it by creating the component outside the Provider's owner, so React cannot mirror the bug. Both portaling fixtures (popover, breadcrumbs) were fixed and the receipt carries a defect-in/defect-out pair.
- r2-certified-a: **`7e93d238` matches upstream ordering exactly**: `useFormProps` is applied AFTER the provider/context/runtime merge and defaults stay read-time (`Button.tsx:109-111`, `LinkButton.tsx:100-102`, `ActionButton.tsx:207`, `ToggleButton.tsx:201`), mirroring s2 `ActionButton.tsx:334`, and our Form context supplies the same seven keys as s2 `Form.tsx:43-60`. The new `it.each` cannot pass on pre-fix source: the removed `size: "M"` literal made the proxy see size as set.
- r2-certified-a: **The widened proxy does not leak S2-only props onto the DOM.** `useFormProps`' `ownKeys` now adds `labelPosition`/`labelAlign`/`necessityIndicator` to `{...headlessProps}` (`ActionButton.tsx:478`, `LinkButton.tsx:205`, `ToggleButton.tsx:394`), but our headless Button filters with `filterDOMProps` before spreading (`solidaria-components/src/Button.tsx:420, :650`) exactly as RAC does (`dist/private/Button.mjs`, `DOMProps`), so no invented attribute reaches the element.
- r2-certified-a: **`a32e6bab` is an honest hand-back backed by primary evidence**, not an avoided task: the reduced-motion branch #583 ordered deleted was added deliberately by `1af6eb71` to close #484 under a recorded owner ruling, and `actionbutton.certified.spec.ts:71-87` already carries the per-stack split while the two toggle specs do not. Nothing was edited, both exits are named, and the conflict is escalated rather than silently resolved.
- r2-certified-b: **Calendar parity claim is TRUE.** S2 1.7.0 `src/Calendar.tsx:94` `calendarStyles` carries no `colorScheme`, and our `setColorScheme()` in `packages/solid-spectrum/src/s2-internal/style-utils.ts:158` is character-identical to S2's `src/style-utils.ts:148`. Removing it from `calendar/index.tsx` and `RangeCalendar.tsx` leaves it on exactly the surfaces S2 sets it on (`popover/index.tsx:81`, `DatePicker.tsx:420`, `DateRangePicker.tsx:422`, `Dialog.tsx:252`, `provider/index.tsx:158`, `table/index.tsx:907`, `tooltip/index.tsx:61`).
- r2-certified-b: **No token shadowing from `56c0751c`.** `git grep -nE "^\s*--(text-link|text-secondary|interactive-fill|text-on-accent|white|blue-500|blue-600)\s*:" -- apps/web` returns nothing: `apps/web` only consumes names declared in `packages/viviana-ui/src/viviana-tokens.css`, and both `--white` (dark `:277`, light `:675`) and `--text-on-accent` resolve in both schemes. The rule that burned us before (an app `:root` block silently shadowing library token names) is not violated here.
- r2-certified-b: **The #586 contrast arithmetic is right.** `--text-link` dark `#3dadff` on `--surface-raised` `rgb(32,38,48)` = 6.235:1; light `#0b5dc2` on `#ffffff` = 6.251:1, matching the claimed 6.24 / 6.25 against the 4.5:1 floor. The ticket status was left `in-progress`, not closed, and the message says "full contrast run still owed" — an honest partial.
- r2-certified-b: **#576's factual core holds.** `apps/web/tsconfig.json` `paths` has only `@/*` and `~/*` and is byte-identical across `c8ae8538`, `8751e5a8`, `e652cb81`, `ca1a0d82`, so it never mapped workspace packages; package `exports` point at `./dist`, so a local web build does test the last package build. And CI was never stale: `.github/workflows/certification-gates.yml:213` runs `pnpm run build` (root `package.json:8` builds every package) before `:258 pnpm run a11y:full`. `d2f94530` really did fix `startViewTransition(() => fn)` → `(() => flush(fn))` in both toast twins, with a test in each package.
- r2-certified-b: **`66d73f80` (#575) and `870bed7c` (#580) are honest work.** Both leave `status: open`; #575 appends a diagnosis and claims no fix, #580 states the seam needs an owner call and that both options mint a package or a dependency, deferring instead of inventing a decision. No fabricated owner ruling, no status the Done-when does not justify.
- r2-guards: **#139 is wired at every deleting call site**, not just one: the three env paths all pass through `scratchDir`, and the only recursive deletes left are `pack-local-chain.mjs:130-131` on `outDir`/`stageRoot` and `consume-pack-smoke.mjs:109` on `consumerDir`, all three guarded. No literal `/tmp` survives in `scripts/` or `apps/comparison/scripts/` (`git grep` at `65254a8c` returns nothing).
- r2-guards: **#139's tests feed genuinely violating fixtures to the real scripts**, not to a mock: each `describe.each` case copies `pack-local-chain.mjs` / `consume-pack-smoke.mjs` into a throwaway repo inside a throwaway `TMPDIR`, points the variable at that repo root, and asserts both the refusal text and a surviving sentinel file. On the pre-fix code `rmSync` would have taken the sentinel, so these three cases do fail on the old code.
- r2-guards: **#139's symlink defence is real**, not decorative: `realPath()` walks up to the deepest existing prefix and `realpathSync()`es it, so a link under tmp pointing at the repo resolves to the repo and is refused; `scratch-dir.test.ts` covers that case, plus `tmpdir()` itself, a `..` walk out of tmp, `/`, the repo root and its parent.
- r2-guards: **#574 fails closed where it matters.** A shallow clone hits `hasCommit` false and returns a named reason mentioning `fetch-depth: 0` rather than degrading to a pass, and `certification-gates.yml:45-49` gives the gates checkout `fetch-depth: 0`, so the step can actually run `merge-base` and the diff in CI.
- r2-guards: **The postcard is not lying today, and the receipt's number is honest.** Replaying the new rule over real history: `git diff --name-only 0f1e1198 65254a8c -- <certifiedSuiteCoveredPathspecs> | wc -l` returns 2577, so `certifiedSuitePostcardCurrency` returns not-current and the gate still blocks, as the #574 receipt says (it quotes 2575, measured two commits earlier).

## Not covered

- r2-certified-a: Nothing was executed: no certified/picker, certified/popover, Select, ComboBox or Form suite was re-run, so every pass/fail count in the commit messages and receipts (60/2, the 21-row mutation, 13→27/0, 216 passed) is unverified; the auditor checked only that the code supports them. The D1/D3 PNG artifacts were not opened, `413b2f23`'s option focus-ring change was not re-checked against react-aria 3.52.0's `useOption`, and whether `VirtualizerItem`'s flat-index lookup (`Virtualizer.tsx:997-1001`) still maps correctly when a Picker has sections/headers was not tested. Nothing outside the five commits was audited.
- r2-certified-b: No execution of anything. So every pass count in these commits and receipts ("114 passed", "86 passed", "11 of 11", "5 passed", "30 instances"), every mutation-proof claim, and the real behaviour of the D2/D3 runs are unverified; only whether the code and the oracle could produce them was checked. The certified scenario fixtures (`apps/comparison/e2e/certified/datepicker.certified.spec.ts`, `calendar.certified.spec.ts`) and the panel wiring that decides whether a DatePicker popover ever flips in the walk were not read — that is what would settle F2 and F3 empirically. The 174-route contrast corpus was not run or sampled, no browser was opened, no published npm artifact was verified. Commits outside the six (including `65254a8c` and `495582e9`), the `.changeset/` entries and the generated board views were not audited.
- r2-guards: Nothing was executed: the claimed pass counts (scratch-dir 8/0, postcard 15, baseline 5) and every mutation claim in the receipts are unverified, as is "[pass] Every baselined gap still occurs" and today's `report:parity:strict` exit code. Needs-run: `vp test run scripts/scratch-dir.test.ts apps/comparison/src/data/certified-postcard-git.test.ts apps/comparison/src/data/parity-strict-baseline.test.ts apps/comparison/src/data/acceptance-schema.test.ts` and `vp run comparison:report:parity:strict`. Out of scope for that slice: the other twelve commits in the range and their upstream parity; the shard logs behind the census's 88; and whether `pnpm-lock.yaml` actually resolves the s2 1.7.0 / RAC 1.21.0 / react-aria 3.52.0 pins.
- The whole fleet: no commit in this range has ever been seen by CI — all sixteen are unpushed (`git merge-base --is-ancestor` false for each against `origin/main` `96376e9a`). So no finding here is contradicted or confirmed by a run at the tree it describes.
