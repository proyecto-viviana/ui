# UI #536 real streaming — 2026-09-19

Start `1d0e7149219dd40b666e25e7657c7ac7bc9c84ea`, main, clean tree/index.
Owner-authorized autonomous foundation slice; root sole writer, read-only reviews.

## Scope and result

New components utilsStreaming fixture/SSR/hydrate tests prove an actual unresolved
Loading shell before Promise resolution, followed by separately emitted tail
delivery after hydrate returns while the boundary remains pending. Exact late
template-node adoption and immutable IDs cover Provider, positive-arity render
props/OptionContent, Show, following sibling, server serialization authority,
reactive updates/remount and balanced per-instance cleanup. No product source,
dependency, public contract, config or retained child cache changed.

The existing solidaria hydration helper waits for installed rc.9 onHydrationEnd,
not synchronous hydrating=false; bounded completion precedes deferred verifier
draining. Pending failures dispose before globals are restored. Exact descriptors
isolate eleven bootstrap/script globals, per-attempt runtime maps/callbacks and
console. Tests cover missing tail, real same-key late tag mismatch, all five
falsy seam errors, immutable sentinels after delayed work and next-valid same-ID
adoption. Existing helper negatives remain; added timeout/preflight/async-seam/
restoration failure controls keep cleanup fail closed. No private-map reset or
arbitrary producer timer, module, head-streaming or complete internal-state claim.

Named paths: five helper/fixture/test paths, #536/#531 histories, generated
status/roadmap and this receipt. Exact paths/commands are recorded in
`/tmp/ui-execution-536-streaming-result.md`.

## Proof

Logs use `/tmp/ui-execution-536-streaming-`. One worker; heavy lanes sequential.

| Proof                             | Exit | Result                      |
| --------------------------------- | ---- | --------------------------- |
| focused-ssr.log                   | 0    | 1/1, real shell/tail writer |
| final-focused-hydrate.log         | 0    | 31/31, two files, 3.43s     |
| full-ssr.log                      | 0    | 75/75, 28 files, 30.09s     |
| subsequent full-hydrate.log       | 0    | 98/98, 27 files, 12.62s     |
| final-full-ssr.log                | 0    | 75/75, 28 files, 31.20s     |
| subsequent final-full-hydrate.log | 0    | 98/98, 27 files, 12.95s     |

Earlier focused runs passed 16/16 and 23/23. Expanded diagnostic assertion was
28/29 because captured String(HTMLElement) does not serialize its tag. The test
now requires the exact runtime key/expected span and independently retained EM
tag/key; no diagnostic or existing assertion is weakened. Review additionally
required observed async-seam rejection, restoration-error cleanup, isolated
sharedConfig accessors/maps and immutable ID snapshots; all have regressions.
Independent source and test reviews found no remaining bounded blocker.
Post-ticket typecheck passes. Scoped five-file format/lint is clean after moving
an already-caught descriptor-restoration throw into its own function to avoid a
finally warning; fresh complete lanes pass again after that extraction. Docs wrappers
hit known sandbox tsx IPC EPERM; both direct no-IPC equivalents pass, including
after ticket formatting. Parent ecosystem gate passes 38/38 (two external dirty
files unread by its doc-shape subgate; no parent edits). Attribution exits 1 with
the identical 64-path mismatch list; no hash refresh. Generated diffs are only
board hashes. Full command ledger records these results and final Git scope.

## Remaining

#536 final inventory/acceptance reconciliation, #533/#534/#535, every #531 build
and attribution requirement remain. #139 precedes pack; #194 precedes #537 live
2,177 same-revision zero-failure/skip/waiver proof. Security/toolchain decisions,
exact-SHA release workflows and actual publication/artifact verification remain.
No broad ordinary suite, build, pack, certified, version or publish ran here.
Owner holds, successor #245, skip #254, census/priority decision and Kumo/Geist
experimental first-release boundaries are unchanged.
