# UI #543 honest app types and keyed fixtures — 2026-09-20

Start `fd28239835394c6ac399c40924d2ca5c1e14fc05`, main. Root sole writer;
independent helper/product/test review. Five existing #534 Hover paths stay
byte-identical and outside this slice. Exact commands, failures and scope:
`/tmp/ui-execution-543-types-result.md`; logs `/tmp/ui-execution-543-*`.

## Bounded changes

Checked hyperscript tag/component results are actual callables; hc has its own
component-thunk brand. Seven fixtures use a real keyed one-argument callback,
materialize JSX and defer their shells inside the Provider owner. The generic
context sentinel fails before lazy construction and passes after it; de-DE
RangeCalendar passed already, not a reproduced locale bug. Same-key updates
retain nodes/focus, while structural/default changes intentionally remount.

Seven fixtures and shared theme return exact listener cleanup from onSettled.
Shared SVGs retain live props/default precedence with installed merge.
Six additional callers have type/materialization repairs only; their lifecycle
migration remains explicit debt. Two readiness callbacks now return void.

Separately named product extensions: Geist omits native RDFa prefix to preserve
its existing JSX prefix slot, without runtime/public-name change. Type proofs
retain native forwarding and svgOnly accessible-name requirements. Solidaria
tablist's four ARIA fields become getters, fixing the reproduced live-label
failure without changing role/handlers/allocation. Formatter also normalizes
two pre-existing blocks' indentation/redundant empty statements. Explicit test
cleanup prevents cross-file retained DOM while preserving all 74 owning cases.

The actual browser exposes React's key-prop spread diagnostic. The named
reference-fixture fix passes the same renderKey as jsxs's third argument,
preserving selection/remount behavior. No Checkbox product fix was needed:
its visually hidden input is not the mouse target; the existing visible label is.

## Evidence and limits

Helper/fixture 19/19; owning 74/74; fresh full SSR 78/78 then hydrate 98/98,
one worker; app paired 8 SSR / 45 client. Preserve all earlier negative controls,
test-authoring corrections and initial 120-second fixture timeout as red/incomplete.
CSR fixture tests are not fixture SSR or styling certification.
Both client lanes emit existing default-HTML scan/three JSX parse diagnostics
despite passing tests; do not call toolchain output clean.

App-specific astro check improves 40 errors to zero (435 files, 35 hints); root
typecheck also passes. Final production build passes 91 pages. Existing site
tests pass 7/7 and client-router passes 1/1, sequentially with one worker.
Scoped lint/format and diff checks pass. Docs wrappers fail with sandbox tsx IPC
EPERM; direct no-IPC generation/check both pass. Parent audit first fails 13/38
under sandbox process restrictions, then passes all 38 on the reviewed rerun.
Exact commands and logs are in the ledger; no exact-SHA release claim follows.

Strengthened real dev-browser probe passes Checkbox label/Space original-input
identity, both Solid/React Tabs mouse/ArrowRight original identity followed by
intentional composition-key remount, and German RangeCalendar labels/keyboard.
No console errors/pageerrors. Strict warnings remain 1,353 / 1,553 / 944 respectively.
Reviewer-required post-keyboard identity assertion runs before structural
remount; no warning suppression/no-error assertion weakening. Earlier hidden
input timeout and React diagnostic failures stay in the ledger.

Playwright skill guided browser proof: offline CLI ENOTCACHED required announced
installed-library fallback; localhost sandbox EPERM required reviewed escalation.
No installs/provider calls/deployments; each probe stops its own server/browser.

## Still open

#543 remains in-progress: 73 nested lifecycle registrations in 70 files, six
type-only callers, strict-warning/larger interaction/navigation and toolchain
debt. Web/TanStack compatibility remains separate. #531 siblings/build/
attribution, #139 safety before packaging, #194 evidence before #537 live 2,177
same-SHA zero-waiver gate, security/Changesets/consumer/site/publication remain.
Owner holds, successor #245, skip #254 and #87 census remain. No release occurred.

## Fresh-session checkpoint

The owner requested a complete remaining-work plan and a fresh-session handoff
before this slice was staged or committed. HEAD remains the starting revision;
the index is empty. Independent review found no bounded source/proof blocker.
All 37 intended slice paths remain uncommitted, separate from the five unchanged
#534 paths. Additional handoff-only ticket/receipt edits are identified in
`.agents/UI-SESSION-HANDOFF-2026-09-20.md`. Resume from #87's dated plan, inspect
the exact working tree, and complete a scoped commit before new source work.
