# ui seat, 2026-09-17

An AGY seat ran on this checkout from 12:33 to 14:29 and stopped on a
usage quota, not at a stopping point. It left eight unpushed commits and
had recorded only the first of them on the board. This receipt names what
landed and what was off-scheme.

## Commits

| Commit     | Subject                                                                               | Recorded on                                                           |
| ---------- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `1a7af7f7` | combobox: land Solid journey protocol and register CB-OC-02                           | #243, #245, in its own commit                                         |
| `ba2c1648` | hygiene: format workspace, regenerate work views, and record local attribution hashes | unticketed hygiene; prettier reflow only, verified no behavior change |
| `4ade6b3c` | numberfield: port upstream NumberParser and NumberFormatter                           | #538                                                                  |
| `226a5d4e` | web: purge register, parity, and stale test counts from landing page                  | #539                                                                  |
| `c9419d5f` | comparison: disable dev toolbar and fix geist button fixture hydration                | #540                                                                  |
| `28539460` | button: purge register from viviana-ui and docs                                       | #539                                                                  |
| `95d30443` | comparison: balance preview canvas alignment and eliminate empty space                | #540                                                                  |
| `16acc9c7` | comparison: preserve Vite dev styles across ClientRouter navigations                  | #540                                                                  |

## Off-scheme

1. **Closure was not in the implementation commit.** Ticket execution
   wants proved closure in the same commit as the source. Seven of the
   eight commits carried none. #538, #539, and #540 are filed after the
   fact and say so in their first history entry. The commits are not
   amended.
2. **A dependency landed without approval.** `4ade6b3c` added
   `@internationalized/number ^3.6.7` to `packages/solid-stately`, a
   releasable public package, against `AGENTS.md` line 47. The owner
   ratified it on 2026-09-17 after the fact. The changeset the bump needs
   was missing and is filed with this receipt.
3. **Seven commits ran with no owning ticket.** The board could not say
   what the seat was doing while it was doing it.

## Not done, not claimed

Nothing was pushed by the seat. No certified suite was run: the postcard
is still `0f1e1198`, 496 commits behind HEAD, and today's local certified
run produced zero cells. #194 still owns that.

## Proof

Working directory: `ui`.

| Kind  | Command                                             | Result                                                |
| ----- | --------------------------------------------------- | ----------------------------------------------------- |
| local | `pnpm run check`                                    | exit 0; 4258 files formatted, 0 lint findings in 3111 |
| local | `vp run typecheck`                                  | `tsc --noEmit -p tsconfig.typecheck.json` clean       |
| tree  | `git status --porcelain .agents .changeset .claude` | 5 untracked files, all filed by this seat             |

The certified postcard is untouched: `lastFullCertifiedSuiteRun` is still
`0f1e1198`, older than HEAD. No suite ran this session.
