# UI #536 bounded retirement receipt — 2026-09-19

The owner's autonomous planning/execution instruction authorizes ordinary,
justified named-path progress without asking at each step. Root is the only
writer; three independent agents reviewed inventory, runtime semantics and
test coverage read-only. Prior commit/push authorization remains; no release,
dependency, naming or owner-hold authorization is inferred.

Starting revision: `7b370277da951755cb3082477745d2c790210d8a` on `main`.
Initial index and `git status --short` were clean. Exact final status, command
ledger and commit outcome live in `/tmp/ui-execution-536-retirement-result.md`.
The older `/tmp/ui-execution-536-result.md` is preserved as historical #542
evidence; this receipt is the subsequent #536 slice.

## Source correction and scope

`packages/solidaria-components/src/ElementTag.tsx` no longer carries the
Solid 1.9.14 static-tag switch. Installed Solid 2 rc.9's native `dynamic`
separates the tag-source memo from reactive props; `staticElement` adopts via
`getNextElement()` while `sharedConfig.hydrating`, otherwise creates the element.
The old post-hydration missing-template rationale does not describe this
runtime. The wrapper splits only `tag` and uses `dynamic(() => local.tag)`;
unrelated prop updates retain the element, actual tag changes replace it.
Forwarded `component` attributes no longer override the tag on the old fallback
path. The old-source negative control fails on button/output/mark forwarding.
No claim is made that the old Solid 1 crash was reproduced under Solid 2.

Named write scope:

- `packages/solidaria-components/src/ElementTag.tsx`: native helper.
- `packages/solidaria-components/src/VisuallyHidden.tsx`: dependent explanation
  only; accessibility behavior, lazy children and structural tag read retained.
- `packages/solidaria-components/test/utils.test.tsx`: all 30 former switch tags
  plus button/output/mark, props/ref/event forwarding, reactive spread updates,
  focus and intentional tag replacement; existing tests unchanged.
- `packages/solidaria-components/test/fixtures/utils.tsx`, `utils.ssr.test.tsx`,
  `utils.hydrate.test.tsx`: two real SSR fixtures, exact nonempty node-identity
  checks, nested Provider order, generated IDs, render props/zero-arg accessors,
  delayed mounts, conditional fallback/reveal, ref/event/focus and disposal.
- `packages/solidaria-components/test/Link.test.tsx`, `Separator.test.tsx`:
  explicit `afterEach(cleanup)` only. Old-source controls reproduce retained
  prior-test DOM failures; no assertion was weakened. Automatic hook registration
  is conditional in installed testing-library, but its precise missed-registration
  cause is not claimed. Explicit teardown also makes the new hydration file safe
  when an assertion throws.
- `scripts/attribution-local-reviews.json`: ElementTag hash only, reviewed hash
  `c365345e8f6149b265d355ecc017f0648aff333833a34601abd0ac2cd9173756`.
- #536 and #531 tickets: dated progress/history and grouped workaround inventory;
  #536 moves to in-progress, not merged/verified. Generated status/roadmap update
  only through the supported generation command/no-IPC fallback.
- This receipt. No helper/config, dependency, package version or public-name edit.

## Proof

One test worker throughout; full SSR and hydrate lanes run sequentially.
No broad ordinary suite, package smoke or certified lane was run.

| Command / phase                                            | Exit and result                                                                        | `/tmp/` log                                                         |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| Focused new utility SSR, first old-source run              | 1; 1/2, 3.48s; test representation error: serialized text includes comments            | ui-execution-536-utils-baseline-ssr.log                             |
| Same SSR after correcting text comparison, old source      | 1; 1/2, 9.33s; real `component` attribute override                                     | ui-execution-536-utils-control-ssr.log                              |
| New source, focused utility SSR                            | 0; 2/2, 5.10s                                                                          | ui-execution-536-utils-focused-ssr.log                              |
| New source, focused utility hydrate                        | 0; 2/2, 6.08s                                                                          | ui-execution-536-utils-focused-hydrate.log                          |
| Seven owning ordinary files, before cleanup                | 1; 143/183, 4.79s; Link 25 and Separator 15 duplicate-role failures                    | ui-execution-536-utils-owning.log                                   |
| Old-source Link/Separator control                          | 1; 35/50, 4.62s; Separator 15 duplicate-role failures                                  | ui-execution-536-owning-baseline.log                                |
| Old-source seven-file control                              | 1; 155/183, 5.08s; Link 25 duplicate-role failures and three new fallback-tag failures | ui-execution-536-utils-owning-baseline.log                          |
| Restored source, explicit owning teardown                  | 0; 183/183, seven files, 5.06s                                                         | ui-execution-536-utils-owning-final.log                             |
| `vp run test:ssr --maxWorkers=1`                           | 0; 51/51, 25 files, 39.83s                                                             | ui-execution-536-full-ssr.log                                       |
| Then `vp run test:hydrate --maxWorkers=1`                  | 0; 59/59, 24 files, 15.35s                                                             | ui-execution-536-full-hydrate.log                                   |
| `vp run typecheck`                                         | 0                                                                                      | ui-execution-536-typecheck.log                                      |
| Scoped `vp check`, eight TS/TSX files and attribution JSON | 0; formatting/lint clean                                                               | ui-execution-536-check.log                                          |
| `vp run guard:attribution-headers`                         | 1; 64 unchanged-path mismatches (one exact-source, 63 local); ElementTag matches       | ui-execution-536-attribution.log                                    |
| `vp run docs:generate` / `vp run docs:check`               | 1 each; known sandbox tsx IPC EPERM                                                    | ui-execution-536-docs-generate.log; ui-execution-536-docs-check.log |
| `node --import tsx scripts/generate-work-views.ts --write` | 0; expected two views                                                                  | ui-execution-536-docs-generate-direct.log                           |
| `node --import tsx scripts/check-docs-current.ts`          | 0                                                                                      | ui-execution-536-docs-check-direct.log                              |

Full tests use a 120-second timeout; ordinary controls/rerun use 60 seconds.
None reached its deadline or hung. JSDOM reports two unimplemented navigation
messages in owning Link tests without failing expectations. No remaining
hydration key/tag/unclaimed diagnostic is hidden or waived. Old source was
temporarily restored only for the named controls, then restored to the reviewed
implementation before final proof. Generated SSR fixtures remain ignored output.

Independent review found no bounded source/expectation blocker. Review prompted
unconditional hydration-file disposal/DOM teardown and strict ref identity;
both are present in the final full-lane proof. The ticket census explicitly
retains semantic caches: installed rc.9 still allocates owner child IDs and
reserves slots for server effect/settlement registrations.

## Remaining acceptance

A final Markdown-only `vp check` formats all three ticket/receipt files
successfully but exits 1 because its lint stage has no code files to analyze.
This is an inapplicable lint invocation, not a passing check. Markdown formatting
and the dedicated docs checker provide that proof; the nine-path code/JSON
`vp check` above exits 0.

The parent ecosystem gate was also run. In the sandbox it exits 1 with 13/38
failures including `spawnSync git EPERM`. The authorized outside-sandbox rerun
exits 1 with 37/38 passing: two real findings remain in parent `AGENTS.md`
(96 counted lines over the 80-line entry-doc cap, and retired wording at line
69). That parent file is outside this UI slice and was not edited. Logs:
`/tmp/ui-execution-536-ecosystem-gate.log` and
`/tmp/ui-execution-536-ecosystem-gate-unrestricted.log`. This is a failed gate,
not a sandbox-only failure or a waiver.

#536 remains in-progress: migrate stale `sharedConfig.context` guards in
ClientOnly/useIsHydrated/ScrollView with owning proof, cover server early-return
allocation suspects, update stale One-Read guidance and prove real asynchronous
streaming. Synchronous renderToString plus delayed client mounts is not streaming
coverage. Then rerun all SSR/hydrate suites; do not delete semantic caches merely
to satisfy the ticket title. #533/#534/#535 and #441 retain their separate work.

#531 retains every child/build requirement. #139 precedes environment-selected
packaging; #194 precedes #537's live 2,177-case, same-revision, zero-failure/skip/
waiver evidence. The attribution gate remains red and requires its owning
review, not mass hash refreshing. Exact-revision release-readiness, site/a11y,
Changesets and publication evidence remain. No package is declared release-ready.

#87's ordered census and explicit foundation-first sequencing remain; #136's
#251/#257/#252/#256 holds, successor #245 and skip #254 are unchanged. No new
named-path permission request is needed for this bounded autonomous slice;
subsequent source work must still name and justify its own paths.
