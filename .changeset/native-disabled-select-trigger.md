---
"@proyecto-viviana/solidaria-components": patch
---

A disabled Select/Picker trigger uses the native `disabled` attribute, matching RAC `useButton` on a `<button>`. The control leaves the tab order.
