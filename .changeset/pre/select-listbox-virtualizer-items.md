---
"@proyecto-viviana/solidaria-components": patch
---

`SelectListBox` under a `Virtualizer` wraps its rows in `CollectionRoot` and `VirtualizerItem`, as `ComboBoxListBox` does, so ListLayout's padding and row geometry reach the options.
