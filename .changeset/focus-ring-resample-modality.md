---
"@proyecto-viviana/solidaria": patch
---

Re-sample global interaction modality on every `createFocusRing` focus change, matching React Aria `useFocusRing`, so a FocusScope contain-restore after pointer hover does not keep a stale keyboard focus ring.
