---
"@proyecto-viviana/solidaria-components": patch
---

Publish `--trigger-width` from the trigger's layout `offsetWidth` so a Picker/ComboBox list still matches the button when S2 `pressScale` has shrunk `getBoundingClientRect` on the pressed open frame.
