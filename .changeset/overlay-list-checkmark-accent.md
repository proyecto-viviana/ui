---
"@proyecto-viviana/solid-spectrum": patch
---

ComboBox and Picker list selected checkmarks and labels match S2 `baseColor` stops when the focused row is not `isFocusVisible` after a pointer open.
