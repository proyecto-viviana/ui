---
"@proyecto-viviana/solid-spectrum": patch
---

Give a focused ComboBox option upstream's focus-visible answer. `useOption` reads the global interaction modality at render; ours ANDed a per-element focus ring that virtual focus never sets, so the row ink and the selected checkmark each sat one color stop below S2.
