---
id: 578
type: task
title: "The certified suite is 27 red at `d6745471` on GitHub's runners, concentrated in five components"
created: 2026-09-20
parent: 544
status: in-progress
history:
  - {
      state: open,
      at: 2026-09-20,
      note: "found by the conductor on 2026-09-20 while checking #194's stage-3 obligation, by reading CI rather than by running anything. `Certification Gates` is **active**, not disabled, and has been running on every `main` push; almost every run is cancelled by the next push, and run 35546816816 at `ef7d4c4d` is the only recent one where all eight certified shards completed. Its merged summary: `Run status: failed`, `Totals: 2004 passed, 169 failed, 4 skipped, 0 waived, 0 flaky`, against a recorded postcard of 2170 passed / **0 failed** at `0f1e1198` (2026-08-21). The `certified report` job failed at `Merge certified reports`, which is #194 slice 1 working exactly as built - a non-pass status the summary does not explain is now a hard failure instead of a green merge of zeros. Receipts `.agents/certified-2026-09-20/merge-ef7d4c4d.log.txt` and `unwaived-failures.txt`, 169 named lines",
    }
  - {
      state: next,
      at: 2026-09-20,
      note: "handed to the close-gates writer after #573 merged (`2b444a89`), ahead of #574, #575 and #576, on this ticket's own argument: those three are steps of the `gates` job and each is smaller, while this is the `certified` job and it is the evidence #547 asks for. The hand-over asks for steps 1 and 2 only - one failure reproduced and understood, then the 169 grouped by proven cause with the ungraded remainder named as ungraded - and a report back before any fixing at scale. Four rules were given priority in the brief, in the order they are easy to break: reproduce before theorising, no waiving, one cause per commit with its re-run in the message, and never update a screenshot baseline to make a test pass. The count in this ticket is from run 35546816816 at `ef7d4c4d`; the run in flight when this was handed over was `1d7551cb`, whose gates job failed at step 36 - the gate #573 has since closed - so the first revision that can walk past step 227 is this commit",
    }
  - {
      state: in-progress,
      at: 2026-09-20,
      note: "taken by the close-gates writer. Steps 1 and 2 only, per the hand-over: one failure reproduced against its own diff image, then the 169 graded by proven cause with the ungraded remainder named as ungraded, and a report before any fixing at scale. Local runs use `COMPARISON_CHROMIUM_ARGS=--disable-software-rasterizer` (`tooling.md` host note: Chrome 151 on this WSL2 host never issues a compositor frame through SwiftShader), so a local red needs that switch ruled out before it is called a real failure",
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: "2026-09-21 round-1 audit, receipt `.agents/audit-2026-09-21/round-1-results.md`. Three findings land here. `578-census/toast-browser-branch-stub-sync`: the new toast tests stub `startViewTransition` synchronously, so the branch they claim to cover is not exercised the way a browser runs it. `578-census/attr-namespace-unguarded`: nothing stops the `attr:` namespace from returning - the fix that closed #581 is two per-component assertions, not a guard. `board-truth/581-merged-below-its-bar`: #581 is merged although its Done-when demands zero failures and four remain; two of the four are #584's. The census this ticket owns is the place those rows are accounted for.",
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: "2026-09-21 round-2 audit, receipt `.agents/audit-2026-09-21/round-2-results.md`, finding `r2-guards/r2-guards-3`, high, confirmed: the census commit `65254a8c` is stale on arrival. It reports CI run 35558449632, head `eb75ee0e`, created 2026-09-21T03:42:53Z and finished 04:06:14Z, but was committed at 13:46:11Z with 18 commits in between (`git rev-list --count eb75ee0e..65254a8c`), and it is written in the present tense - where every row now belongs, the largest untracked block left, this table is the whole distance - so a reader takes it for current state. It is not: at `65254a8c` the form block it calls never ticketed was fixed by `7e93d238` at 06:18:18Z, the calendar block it calls ungraded by `6e43c436` at 07:11:38Z, and the three it closes with as ticketed and cheap are #585 merged, #584 in-progress and #583 open because `a32e6bab` handed it back over #484's owner ruling. The finding's own times mix local and UTC and the gap is three hours wider than it reads; the UTC figures here were re-measured with `TZ=UTC git log --date=format-local`. Fix: re-head the table as a snapshot of run 35558449632 at `eb75ee0e`, add a landed-since column naming `495582e9`, `413b2f23`, `f8e5833e`, `7e93d238`, `6e43c436` and `6ad3d12d`, move #583 out of cheap into owner-blocked, and let the next Certification Gates run on the pushed tip supply the roster instead of re-deriving it by hand",
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: "placed at stage S0-e of #544's path, which had named this ticket only in a history note although the job it owns is blocking. Re-measured at the last run, 35560076342 at `96376e9a`: the `certified report` job failed, its `Merge certified reports` step reported `Run status: failed`, `2085 passed, 88 failed, 4 skipped, 0 waived, 1 flaky`, and exited 1. The step carries no `continue-on-error` (`certification-gates.yml:691`) while the eight shards do (`:646`), and the job's own summary row calls it Blocking (`:402`), so the release condition cannot be met without it. The path now states what makes the job exit 0, because `certification-debt.md` on its own does not: `merge-certified-reports.ts` exits 1 while `waiverGateFails` is true, which it is while any failure is unwaived (`apps/comparison/scripts/certified-waivers.ts:269-271`), and no workflow, script or `package.json` entry reads the debt file. So a named-debt failure ships as a `certified-waivers.json` entry - `[]` today - with an open board ticket and a future `expires`, plus its debt entry; the owner decision on #544 authorises the debt, the waiver is what the gate reads. Work item 3 is still the default: fix by cause, waive only what an owner call names as deliberate. This ticket also supplies the revision #574's postcard pin needs, so S0-d's widened rule lands first",
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: "the roster this ticket carries is out of date and the correction is measured, not guessed. Against the 88 failures of run 35560076342 at `96376e9a`, the first complete run under the new shard shape - 35638122333 at `45714230`, all eight shards run and merged - reports `Totals: 2146 passed, 27 failed, 4 skipped, 0 waived, 1 flaky`. Its 27 unwaived failures, counted from the report job's log (job 106467836508): `combobox-list` 22 (D1 6, D3 6, D7 2, D9 6, D10 2), `picker-trigger` D13 2, `tabs` D4 1, `togglebutton` D2 1, `togglebuttongroup` D2 1 - five components, not thirteen. Run 35646778662 at `d1c5f4b3` fails the same four shards. Which of these are crash-class is the census's call and is not decided here; by the owner's soft-launch cut on #544 the crash-class subset is in the RC and the rest is deferred, and nothing is waived - `certified-waivers.json` is `[]`",
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: 'the owner''s call on waivers, ~17:10, recorded here because this ticket owns the census the rule applies to. The conductor put four open calls to the owner - quoting its own message, "the dist-tag (my default is `rc` alone), re-enabling Release Readiness and Site Gate, the waiver list once the census produces it, and the publish" - and the owner answered, verbatim: "can you handle all those? your default is fine", then "also you''re the only one working on this, the previous session was superseded by yours, so you can handle everything, don''t say "oh the other session is running" or whatever". The call is therefore delegated to the conductor, and what follows is the conductor''s stated default that the owner accepted, not the owner''s own wording: waivers cover BEHAVIOUR-class certified reds only, each entry ticket-backed and expiring at the next release, CRASH-class is fixed and never waived, and the conductor writes the entries once the census is reviewed and reports the list back. Two limits follow from that and are worth writing down: a waiver is not a grade, so the grading stays in this census and nothing may be waived that the census has not graded; and the rule does not cover a row that merely passes on retry, which is why the tooltip flake became #608 and not an entry',
    }
  - {
      state: in-progress,
      at: 2026-09-21,
      note: "re-headed onto the measured count and the waiver list written. (Corrected 2026-09-22 by the note below: the patterns are now anchored at both ends, one entry per case rather than per ticket, `expires` is `2026-10-21`, and the local inventory is 2177 cases, not 3065.) The title and the tables below said 169, and the note above already corrected the body to 27 without moving the head; both now say 27, and the 169/88 tables are kept as dated snapshots because the ordering argument they carry was true when it was made. Measured again at the newest complete run, 35668806426, head `b22a44eb` (= `origin/main` at the time of writing), merge job 106565094355: `Run status: failed`, `Totals: 2146 passed, 27 failed, 4 skipped, 0 waived, 0 flaky`. Same 27 as run 35638122333, and the one flaky row of that run - the certified tooltip, now #608 - did not recur. The 27, counted from that job's own `Unwaived failures` list and its per-component table: `combobox-list` 22 (D1 6, D3 6, D7 2, D9 6, D10 2), `picker-trigger` D13 2, `togglebutton` D2 1, `togglebuttongroup` D2 1, `tabs` D4 1. Five components out of the 107 in that job's own per-component table; the other 102 are green, counted from the table rather than remembered. Class, which is this ticket's call and is made here: **none of the 27 is crash-class**. Nothing throws, nothing fails to render, and every one of them is a compared-behaviour difference against the pair oracle - 22 screenshot/contrast/forced-colors/RTL rows on one ComboBox list defect, 2 Picker journey rows, 2 reduced-motion rows, 1 event-sequence row. So the RC carries no known crash from this suite. Of the 27, the 5 non-ComboBox rows are behaviour-class **and deliberate for this release** under the rule above, and they are now waived by ticket in `apps/comparison/e2e/certified-waivers.json`, which was `[]`: #584 for the two `picker-trigger` D13 rows (`open-arrow-enter-reopen-scroll-escape` and `keyboard-only`), #583 for the two D2 `default · hover-transition` rows on `togglebutton` and `togglebuttongroup`, and the new #609 for `tabs` D4 `horizontal-regular · arrow-next-from-selected`. Each entry carries the ticket's real board state (`in-progress`, `open`, `open`), `expires: 2026-12-31` standing for the next release, and a `reason` written as what a user sees rather than as a test name. The patterns are anchored on the case id with `$` and were checked against the full local case inventory (3065 cases): 2, 2 and 1 matches, nothing else - an over-broad pattern is the real hazard here, because `evaluateCertifiedWaivers` has no unused-waiver problem kind and would silently swallow a future red. Proof of the list, run at this commit: `vp run comparison:test:certified-waivers` -> 3 files, 52 passed, exit 0, and `vp run comparison:guard:certified-waiver-tickets` -> `3 waiver(s) ... agree with the board`, exit 0, both with the non-empty list. A wrong entry is refused three ways, each driven from a scratch fixture through the mechanism that owns it, since they are not the same mechanism: the guard exits 1 on an off-board ticket (`ticket-missing: waiver ticket #9999 is not on the board`) and on a recorded state the board has moved past (`ticket-stale: waiver ticket #583 records in-progress; the board says open`), while a closed ticket and a case id that matches nothing are the verdict's to refuse - `evaluateCertifiedWaivers` returns `ticket-closed: waiver ticket #609 is verified; remove the waiver` with `waived 0, unwaived 1` for each of `verified`, `merged` and `closed`, and leaves a renamed case (`arrow-next-from-last`) unwaived with the gate failing. An expired date does the same. Worth writing down because the guard alone would pass a waiver on a closed ticket whose record matches the board. **The remaining 22 are not waived and must not be**: they are one defect, the ComboBox list checkmark, owned by #497, which is still `status: next` - the writer that was to land it died before making an edit. So `waiverGateFails` stays true and the blocking `certified report` job still exits 1 at this revision; it goes green when #497 lands, not when this bookkeeping does. This is also a disagreement with the brief that produced this note, which described the ComboBox rows as fixed: the tree says otherwise and the tree wins",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "a review of the landed waiver list raised five problems. Four were real as stated, one was real in substance with a wrong suggestion, and all five were measured against the tree before anything was changed. (1) The #584 entry named one cause and covered three. Its reason described only the pointer-open focus placement, while the same two D13 rows also carry the popover's entry motion (opacity 0.41, dy 34 against React's 1 and 36, #582's family) and a trigger that emits no `data-pressed` where RAC's does - `packages/solidaria-components/src/Select.tsx:890-957` computes `isPressed` into render values and the button emits none, and `data-pressed` is in the journey's compared allowlist (`journeys.ts:78`). Fixed by splitting one entry per case and naming every cause on the row, and #584's Done-when now covers all four so removing the entries is safe. (2) The breadth claim was unguarded: nothing ran the 'matches its own rows and nothing else' check, and `evaluateCertifiedWaivers` has no over-broad problem kind, so a pattern one segment too wide would swallow a future red in silence. Fixed with a test that runs Playwright's own discovery - `playwright test e2e/certified --list`, no browser and no web server, the same call `guard:certified-case-floor` makes - and fails unless every entry matches exactly one case. It is proved by mutation: widening the tabs pattern's last segment to `.*$` gives `expected 1, received 3`, exit 1. (3) The patterns were anchored only with `$`. Real, but the suggested anchor could not have worked: the haystack is `${failure.file} ${failure.title}` and `file` is the DECLARING file, so a driver-declared case reads `e2e/drivers/events.ts certified/tabs.certified.spec.ts › …` and a pattern starting `^certified/…` matches nothing. Measured, and now pinned by its own test. Every entry is anchored `^…$`, and the loader refuses a pattern that is not anchored at both ends or that names no `.certified.spec.ts` file. (4) `expires: 2026-12-31` did not implement the recorded rule, which is that a waiver stands until the next release. There is no release date anywhere in this tree (`rg '2026-1[12]|releaseDate|release_date' apps/comparison/scripts apps/comparison/e2e .changeset package.json` -> no matches), so the honest half landed here - `MAX_WAIVER_HORIZON_DAYS = 60`, a new `expires-too-far` problem kind, and the five entries re-dated to `2026-10-21` - and #610 owns binding expiry to the release itself. (5) The census was measured at `b22a44eb` and is five commits behind `90297632`; the head above now says so. It matters, and not only as bookkeeping: `8361daba` and `298f8e7c` (#608) changed `e2e/comparison-page.ts` and `e2e/drivers/journeys-steps.ts`, which declare the two waived D13 rows, and `d3ccc7cc` (#545) changed `ContextualHelpTrigger` in both published packages plus a comment in `certification-gates.yml`. So the 27 is a reading of `b22a44eb`, not of HEAD, and the obligation this ticket now carries is to re-read the roster from the first `certified report` job at or past `90297632` and correct the table from that job rather than from this one. Nothing on this host can supply it: the whole certified suite needs both stacks built and eight shards, and this seat does not push. The list also made `reason` required - an entry without one is now an `invalid-entry` - because an undisclosed cause is what problem 1 was. Proof at this commit: `vp run comparison:test:certified-waivers` exit 0, 3 files, 58 passed (was 52; the pre-fix list against the new loader is exit 1, 5 failed | 53 passed, which is the fix failing first); `vp run comparison:guard:certified-waiver-tickets` exit 0, `5 waiver(s) ... agree with the board`; scoped `tsc` over the two changed TypeScript files exit 0; `vp lint` and `vp check` exit 0 on the three changed files",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "#497's 22 `combobox-list` rows are fixed in this tree and all 22 turn green locally. The defect was one value, not a token: `ComboBoxOption` handed the row and checkmark atoms an `isFocusVisible` our headless layer computes from a per-element focus ring that ComboBox virtual focus never sets, where `useOption` reads the global interaction modality; `packages/solid-spectrum/src/combobox/index.tsx` now recomputes upstream's answer and both atoms are untouched. Proof under the heavy lock, cwd /home/emoporemilio/projects/viviana-hub/ui with `COMPARISON_CHROMIUM_ARGS=--disable-software-rasterizer`, after `VIVIANA_GATE=1 vp run comparison:build` (exit 0): `cd apps/comparison && VIVIANA_GATE=1 npx playwright test e2e/certified/combobox.certified.spec.ts --grep 'ComboBox list' --reporter=line` -> **26 passed (1.8m), exit 0**, against 22 failed and 4 certified passed, exit 1, on the pre-fix tree. So 22 of the 27 are accounted for by cause and the other 5 stay waived by ticket; `certified-waivers.json` is unchanged and `certified-case-floor.json` untouched. This is a local measurement on one component's shard, not a roster re-read: this seat does not push, so the head count still belongs to the `certified report` job at the pushed sha, and the obligation above to re-read the roster from the first report job at or past `90297632` stands. Title left as it is.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "the five waivers waived nothing. Measured, not inferred: run 35689146611 at `d6745471` - the first `certified report` job at or past `90297632`, so the re-read this ticket owed - reports `Totals: 2146 passed, 27 failed, 4 skipped, 0 waived, 0 flaky` (job 106625669687, failed), and all five waiver candidates sit in its `Unwaived failures` list. Its 27 are the same rows as `b22a44eb`: `combobox-list` 22 (D1 6, D3 6, D7 2, D9 6, D10 2) owned by #497, plus the five candidates - `picker-trigger` D13 2, `tabs` D4 1, `togglebutton` D2 1, `togglebuttongroup` D2 1. 107 components in that job's per-component table, 102 green (this note said 106 and 101 when it landed; corrected by the note below, which re-derives both from the job log). Cause: the matched haystack is `${failure.file} ${failure.title}`, and `title` is the reporter's `test.titlePath()` minus its empty head, so it **opens with the Playwright project** - `chromium › certified/picker.certified.spec.ts › …`. Every pattern stepped from the declaring file straight to the spec path, so not one could match. What should have caught that is the breadth test written on 2026-09-22: it walked Playwright's `--list` report with a private title builder of its own, and in that report the project is not a suite level - it sits on `spec.tests[].projectName` - so the walk built projectless titles and the entries were proved against a second builder rather than against a report. Fix, in this commit: one builder. `certifiedCaseTitle` is exported from `apps/comparison/scripts/certified-summary.ts` and the reporter calls it; `certifiedCasesFromListing` beside it rebuilds each listed case's titlePath with its `projectName` at the head; the test's private copy is deleted and both tests read the shared one. Confirmed against a real record rather than by reading: the haystack the listing now reconstructs for `picker … keyboard-only` is character-for-character the record shard 7 wrote, and the old shape matches none of the five. Proof, both directions, over the 27 failure records read out of that run's own `certified-shard-{2,5,7,8}` artifacts and fed through `evaluateCertifiedWaivers`: with the landed waivers file `WAIVED 0, UNWAIVED 27`; with this commit's `WAIVED 5, UNWAIVED 22, problems 0, waiverGateFails true`, the 22 being exactly #497's `combobox-list` rows. The new contract test carries six of those real records as its fixture - the five candidates and one ComboBox row that must stay unwaived - and fails on the pre-fix waivers file (exit 1, `expected [] to deeply equal [ …(5) ]`), exit 0 after. Ran at this commit: `vp run comparison:test:certified-waivers` exit 0, 3 files, 59 passed (was 58; the contract test is the 59th); `node scripts/test-ci-guard-contracts.mjs` exit 0; `vp run comparison:guard:certified-waiver-tickets` exit 0, `5 waiver(s) … agree with the board`; `vp exec tsx scripts/check-changeset-required.mjs` exit 0; `vp check` exit 0 over 4456 formatted and 3204 linted files. One more thing rides in the same commit because it is the same file family: `4ac92800` made `reason` required and left the `certified-shard-gate.test.ts` fixture without one, so `vp run @proyecto-viviana/comparison#typecheck` exited 1 on `ts(2741)` at line 71 - measured both ways here, exit 1 without the line and exit 0 with it. What this does not settle is the head count. This seat does not push, so the RC's roster still belongs to the `certified report` job at the sha this commit lands on; the expectation there - an expectation, not a measurement - is 5 waived and the 22 ComboBox rows green from `d997d01f` and `6031691e`. #578 stays in-progress until that job says so.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "a review of `cd38e04e` raised two problems. Both are real, both are bookkeeping - no source, test, waiver or fixture file is touched by this commit - and both were measured against the run's own log before anything was changed. (1) The head count was wrong in the one passage that says `Measured, not inferred`. The note above said `106 components … 101 green`; the body had said 102 and this commit changed it to 101. Re-derived here, not carried forward: `gh api repos/:owner/:repo/actions/jobs/106625669687/logs` (exit 0), the `## Certified suite — component × driver` table sliced between its header row and `### Unwaived failures`, then split on `|` - **378 data rows, 107 distinct components, 5 with a failing row, 102 with none**, failures summing to 27 and passes to 2146, which agrees with the `Totals` line. 106 is what a `D\\d+` driver regex returns, because two rows carry a named driver - `listbox-drag-and-drop | D-reorder` and `virtualizer | D-scroll` - and one of those components appears nowhere else. Both places now read 107 / 102, and the `### Unwaived failures` list counts 27 bullets, 22 of them `combobox-list`. (2) `certification-debt.md` cited run 35689146611 in one paragraph while the section around it was still headed `Certification Gates 35668806426 at b22a44eb, merge job 106565094355` and still carried `Treat the table below as a reading of b22a44eb until a certified report job at or past HEAD replaces it; #578 owns that re-read` - the re-read the same commit records as done. The section is re-headed onto 35689146611 at `d6745471`, merge job 106625669687, which the evidence allows because the two jobs' per-component tables are identical: `diff` of the two sliced tables, fetched separately from jobs 106565094355 and 106625669687, is empty. The discharged obligation is replaced by the one actually owed - a `certified report` job at a sha carrying both `cd38e04e` and #497's `d997d01f` and `6031691e` - and group B's `run 35668806426 predates the commit` now names `d6745471`, which predates it too. Two residues, neither dropped: the same page said `Three of the five components are waived` where four are - `picker-trigger`, `togglebutton`, `togglebuttongroup` and `tabs`, five entries under three tickets - and that is corrected here; and #544's body still calls 35668806426 the newest complete certified run, which is left as a dated note on #544 rather than an edit to the conductor's roster prose. Ran at this commit: `vp check` exit 0 over 4456 formatted and 3204 linted files; `vp run docs:generate` exit 0; `vp run docs:check` exit 0. The head count still belongs to a `certified report` job at the pushed sha - this seat does not push - so #578 stays in-progress.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "a review of this ticket's landed work (`90297632`, `4ac92800`, `cd38e04e`) raised four problems. Three are real and fixed here; the fourth is not, and is left in place with the measurement that says so. (1) `CLOSED_TICKET_STATES` was `{verified, merged, closed}` and `ticketStatus` was any non-empty string. Scheme v1 (`viviana-projects/spec/ticket-scheme.md` 75-85) is `open -> next -> in-progress -> merged -> verified` with side exits `parked` and `dropped`, and legacy `done` parsing as `merged`: so the set named a word the board never writes and missed the one it writes for a ticket closed without merging, and a recorded state outside the lifecycle matched no closing state at all. Census of the 592 board files now: open 228, in-progress 39, merged 232, verified 86, parked 7, and zero `closed`, `done` or `dropped` - no tracked entry was hit, the rule was simply not the board's. Fixed by exporting `TICKET_STATES`/`isTicketState`, setting `CLOSED_TICKET_STATES` to `{merged, verified, dropped}`, refusing a `ticketStatus` the lifecycle does not have, and normalizing `done` to `merged` in `readTicketStatus`. Measured pre-fix against post-fix over both module versions: a `dropped` entry gave problems `[]`, 1 waived, gate false, and now gives `ticket-closed`, 0 waived, gate true; `closed`, `done` and `wontfix` each loaded as a valid waiver and now load as `invalid-entry` with 0 waivers; a board file reading `status: done` read back `done`, which made a correct `merged` entry look `ticket-stale`, and now reads `merged` with the reconciliation clean. (2) the tracked-file cases graded on `new Date()`, and they run in `certified verdict unit tests`, the first step of `comparison-build` (`.github/workflows/certification-gates.yml` 444, 472), which `comparison-floors-pair`, `comparison-floors-contract`, `certified` and `certified-report` all `needs:` (516, 579, 648, 722). Measured on the tracked list: `[]` problems on 2026-09-22 and on 2026-10-21, five `expired` on 2026-10-22 - so on the day the waivers lapse the step the whole certified ladder hangs off goes red and the eight shards, the report and both floors never run, instead of the intended red certified report naming the five. Fixed by pinning `Date` to 2099-01-01 for that describe, grading the tracked list on its own earliest `expires`, and adding the case that asserts what the real clock past the list gives: five `expired`, nothing waived, every reported row unwaived, gate red. (3) `applyWaiverCounts` overwrote each cell's `waived` with the waived rows still sitting in that cell's `failures`, and it runs twice - once per shard in the reporter, once in the merger over cells the first pass already emptied. Measured over the real modules with two shards and the tracked list: pre-fix `FINAL totals { passed: 15, failed: 0, skipped: 0, waived: 0, flaky: 0 }` under a waived list of length 2; post-fix `waived: 2` with cells `picker-trigger/D13=1 tabs/D4=1`. That is a merged report printing `0 waived` above a list of waived rows, which is what run 35689146611 printed for a second reason. `merge-certified-reports.ts` also re-typed the `${file}\\0${title}` key `certified-summary.ts` exports; it imports `failureKey` now. (4) not real: the claim is that `anchors on the declaring file and on the project the title opens with` passes unchanged on the tree before it. Each revision's own `certified-waivers.json` against the reported haystack: `4ac92800` 5 entries, 0 load problems, 0 matching; `90297632` 0 entries, 3 load problems, 0 matching; HEAD 5 entries, 0 problems, 1 matching - and the case's last assertion is `toBe(1)`, so it fails on the pre-commit tree. The replacement the review asked for, `^certified/picker\\.certified\\.spec\\.ts .*keyboard-only$`, loads clean - problems `[]`, 1 waiver - so the `invalid-pattern` assertion proposed with it could not pass. Left as written. Each fix is proved by a mutant, not by assertion: putting the pre-fix line back turns exactly its own case red and nothing else - `{merged, verified, closed}` reds `fails the job when the waiver records a ticket state that ends a waiver`; any-non-empty-string reds `rejects a ticketStatus the board's lifecycle does not have`; the raw `done` reds `reads a legacy done ticket as merged`; the tracked cases back on the wall clock red all three tracked-file cases under the pinned clock; the overwritten count reds `keeps a shard's waived count when the merge applies the same waivers again` with `waived: 0` against an expected 2. All five mutant runs exit 1 and the three files restore byte-for-byte. Ran at this commit: `vp test run apps/comparison/src/data/certified-waivers.test.ts` 31 passed, exit 0; `vp check` over the four changed files exit 0; `tsc --noEmit` over the test file and the merger reports no error in either changed path; `vp run comparison:guard:certified-waiver-tickets` exit 0, 5 waivers agree with the board; `node scripts/test-ci-guard-contracts.mjs` exit 0; `vp exec tsx scripts/check-changeset-required.mjs` exit 0 - `apps/comparison` publishes nothing, and `TICKET_STATES`, `isTicketState` and the now-exported `CLOSED_TICKET_STATES` are app-internal script exports, not a public name. The head count is still a pushed `certified report` job's and this seat does not push, so #578 stays in-progress.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "w-578f. Run 35728260733 at `7e7936de` reported 2168 passed, 0 failed, 5 waived, 2 flaky, and the report exits 1 on a flaky row. Both rows were green on the first attempt at `6e790bc9` and passed only on retry there. Neither failed locally, at 2 workers or at 4. The D6 driver `apps/comparison/e2e/drivers/ax.ts` (the announcement expect is line 189) and the D9 harness `apps/comparison/e2e/drivers/forced-colors.ts` (the width expect is line 59) were not the cause, and neither was paint settle: `walkScenario` passes `paintBudgetMs: 0`, and `document.fonts.ready` left the placeholder group at 69px. Both sides announce September and measure 69px whenever both fixture values stay empty. DateField D6 `placeholder · page-up`, before 10/10. 14:14 from `apps/comparison`, `PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright` and `VIVIANA_GATE=1`, `vp exec playwright test e2e/certified/datefield.certified.spec.ts -g page-up --repeat-each=10 --retries=0 --workers=2` exit 0, 50 passed (1.7m), 10 of them this row. 14:16 the same row at `--workers=4` exit 0, 10 passed (16.4s). Mechanism: an explicit `?value=` was coerced to the demo date `2025-02-03` by `||` in the comparison controls, and a later `comparison:controls-change` carrying that date sometimes reached only one stack, so PageUp announced `4 – April` (February plus the month step of 2) on that stack and `9 – September` (the empty month, today's placeholder) on the other. 14:41 probe on the pre-fix build, exit 0: six loads of `?value=` showed the control input `2025-02-03`, both `data-comparison-value` attributes empty, and both live regions announcing `9 – September`. Fix: `apps/comparison/src/data/control-value.ts:17` (`controlValueFromSearch` keeps an explicit empty param) and `:33` (`controlValueFromField` keeps a submitted empty string; a missing field, null, stays the demo default). Call sites `apps/comparison/src/scripts/component-controls.ts:45` (`initializeForm`) and `:81` (`readControls`), `apps/comparison/src/components/solid/ComponentExampleControls.tsx:130` (`initialControlValues`), and `apps/comparison/src/data/example-source.ts:26` so the generated source uses the same read. A wait that let both stacks settle on the demo date would certify February, so the drivers were left as they are. After: 14:51, on the 14:50 build (`cd apps/comparison && VIVIANA_GATE=1 vp run build` exit 0, 91 pages), the row `--repeat-each=10 --retries=0 --workers=2` exit 0, 10 passed (23.7s). 14:57 the full `datefield.certified.spec.ts --retries=0 --workers=2` exit 0, 69 passed (4.1m); the runner reported 1 worker. TimeField D9 `placeholder · light`, before 10/10. 14:31 `-g` `placeholder · light` `--repeat-each=10 --retries=0 --workers=2` exit 0, 30 passed (1.1m), 10 of them this D9 row (that grep also matches D1 and D3). 14:33 `-g` `D9 forced colors — TimeField placeholder · light` `--workers=4` exit 0, 10 passed (16.2s). Mechanism: the same coercion. The placeholder group is 69px (`––:–– AM`) and the demo time `09:30:00` is 60px (`9:30 AM`). 14:44 one-load probe, exit 0: `?value=` was 69px on both stacks with the fixture value empty and the control input `09:30:00`; the default URL was 60px on both with the fixture value `09:30:00`. The 14:41 probe's pair around `document.fonts.ready` stayed 69px. CI's React 60px / Solid 69px is that split. Same fix, same lines. After: 14:52 the D9 row `--repeat-each=10 --retries=0 --workers=2` exit 0, 10 passed (22.8s), on the 14:50 build. 15:01 the full `timefield.certified.spec.ts --retries=0 --workers=2` exit 0, 69 passed (4.0m); the runner reported 1 worker. `example-source.ts` was pointed at the helper after those 10/10 runs. 14:56 rebuild exit 0, 91 pages, and both full specs above ran on that build. Checks: 14:48 `vp fmt` on `control-value.test.ts` exit 0, then `vp fmt --check` on the four control paths exit 0, `vp lint` on them exit 0. 14:49 `vp run check` exit 0, 4470 formatted, 3210 lint-clean, tsc pass. 14:55 after `example-source.ts`, `vp fmt --check` and `vp lint` exit 0, `vp test run apps/comparison/src/data/control-value.test.ts apps/comparison/src/data/example-source.test.ts --maxWorkers=2` exit 0, 9 passed, `vp run check` exit 0, 4471 formatted, 3211 lint-clean, tsc pass. No package under `packages/` changed, so no changeset; `apps/comparison` publishes nothing. `certified-waivers.json` and `certified-case-floor.json` untouched. `calendar-demo.ts:228` and `checkboxgroup-demo.ts:160` still use `||`; no certified case passes an empty value through them, so they were left. The head count still belongs to a pushed certified report job, so #578 stays in-progress.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "w-avatar-d1. Certification Gates run 35765667710 at 2b7690ee finished the certified suite 2168 passed, 0 failed, 4 skipped, 5 waived, 1 flaky, and Merge certified reports failed on over-flaky: 1 cases passed only on a retry, budget 0. The case is avatar · over-background · dark · default · image, expect(solidStyles).toEqual(reactStyles) at apps/comparison/e2e/drivers/state-matrix.ts:151-154, transition-property opacity on React and none on Solid. The retry passed. The datefield and timefield placeholder rows from 2b7690ee were green in that same run. Mechanism: both panels request avatarDocsExampleSrc (apps/comparison/src/data/avatar-demo.ts:22), Chromium performs that fetch once, and the later img either joins the in-flight response or, after it ends, is already complete from the memory cache (Image.mjs:132 and packages/solid-spectrum/src/image/index.tsx:334), so loadTime stays under 200 and transition-property stays none (Image.mjs:143, index.tsx:289) while the earlier panel stays opacity. Hydration ordering was not observed. Both stacks lose when they assign src second; the algorithms match, so this is not a port divergence. 16:07 probe on the pre-fix build, from apps/comparison with PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright and VIVIANA_GATE=1, vp exec playwright test e2e/_probe-avatar.spec.ts --retries=0 --workers=1 exit 1, 3 failed, 1 passed (10.2s). delay-solid-fixture: Error delay-solid-fixture react=opacity solid=none, Expected opacity, Received none. delay-react-fixture: Error delay-react-fixture react=none solid=opacity, Expected none, Received opacity. One image request each time. The 400ms solid-mount delay assigned Solid's src 184ms into that response with complete false (in-flight; both load events at +308ms, so Solid's loadTime was about 124ms). The 400ms fixture delays assigned the late src after the response, complete true and naturalWidth 256 (memory cache). Fix: comparisonHarnessAvatarSrc (avatar-demo.ts:88) appends ?stack=react or ?stack=solid only for /fixtures/avatar/docs-avatar.png when the page has .js-component-example-section-mount. The stack name can only come from the caller, so the two fixtures pass their own name at the Avatar src and nowhere else; serialized props stay on the canonical path. AvatarGroup still uses /fixtures/avatar-group/*. The certified route glob is **/fixtures/avatar/docs-avatar.png*. 16:12 cd apps/comparison && VIVIANA_GATE=1 vp run build exit 0, 91 pages. 16:13 the same probe on that build exit 0, 4 passed (9.8s): PROBE delay-solid-fixture react=opacity solid=opacity images=2, and PROBE delay-react-fixture react=opacity solid=opacity images=2, each request about 304ms. 16:13 vp exec playwright test e2e/certified/avatar.certified.spec.ts -g over-background --repeat-each=20 --retries=0 --workers=4 exit 0, 180 passed (3.6m): D1 80, D3 80, D6 20, the dark over-background D1 row among them 20/20. 16:17 the full avatar.certified.spec.ts --retries=0 --workers=2 exit 0, 22 passed (1.5m); the runner reported 1 worker; D1 10, D3 10, D6 2. 16:19 vp fmt --check and vp lint on the four source paths exit 0; vp run guard:comparison-atom-css exit 0 (1580 referenced atoms across 231 files); vp run guard:layer-boundary exit 0. 16:20 vp test run apps/comparison/src --maxWorkers=2 exit 0, 19 files, 166 passed. 16:20 vp run check exit 0, 4471 formatted, 3211 lint-clean, tsc pass. No package under packages/ changed, so no changeset. certified-waivers.json and certified-case-floor.json untouched. The head count still belongs to a pushed certified report job, so #578 stays in-progress. 16:26 vp run docs:generate exit 0; .claude/current/roadmap.md and .claude/current/status.md stayed byte-identical. 16:26 vp run docs:check exit 0. 16:27 vp fmt --check on the five write paths exit 0 (5 files). 16:27 vp lint on those paths exit 0 (0 warnings; 4 files, the ticket markdown is not a lint target). 16:27 vp run docs:check exit 0 after that note.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "w-avatar-d2. 19:35 from apps/comparison, PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright vp exec playwright test e2e/avatar-visual.spec.ts --retries=0 --workers=2 exit 1, 3 failed, 1 passed (16.0s); the runner reported 1 worker. The pass is Avatar default state is pixel-identical: the route at line 18, `**${avatarDemoDefaults.src}` with no trailing star, does not intercept `?stack=`, and both panels still painted the static PNG. The three reds, at the lines this file had before the edit: line 167 toHaveAttribute data-comparison-control-props expected the data:image/svg+xml avatarChangedSrc and received /fixtures/avatar/docs-avatar.png; line 193 and line 206 toEqual, imgSrc /fixtures/avatar/docs-avatar.png?stack=react against /fixtures/avatar/docs-avatar.png?stack=solid, every other field equal. Fix is in apps/comparison/e2e/avatar-visual.spec.ts. The docs route is `**${avatarDemoDefaults.src}*`, which is the glob `avatar.certified.spec.ts:22` spells as `**/fixtures/avatar/docs-avatar.png*`. avatarContract drops only a stack search param and compares the rest of the URL as written. The data: expectation is older than 2732f395, and that commit did not touch the sanitizer. 223dfbf0 (2026-05-15) set avatarChangedSrc to the data URL. The spec's commits are 50e3932c (2026-05-14), f07fd926 (2026-05-15), and 223dfbf0, so the expectation was never edited again. dba5fd29 (2026-09-03, #259) added sanitizeDemoHref. Its comment says query-string demo href/src values land on a public origin, and it keeps http(s), same-origin paths, and hashes while dropping javascript:, data:, and protocol-relative URLs. The same commit wired sanitizeDemoSrc into normalizeAvatarDemoProps and pinned the data:image/svg+xml fallback in demo-url.test.ts. git log -L on that comment shows 668845dd (2026-09-03) reflowed the signature and left the rule. The spec now passes src=/e2e/avatar-visual-changed.svg and routes that path, inside this file, to the indigo SVG the data URL carried. No fixture file was added. After: 19:38 the same avatar-visual command exit 0, 4 passed (34.1s), 1 worker. 19:39 from apps/comparison, PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright vp exec playwright test e2e/certified/avatar.certified.spec.ts --retries=0 --workers=2 exit 0, 22 passed (1.6m), 1 worker (D1 10, D3 10, D6 2). 19:41 vp fmt --check apps/comparison/e2e/avatar-visual.spec.ts exit 0 (1 file). 19:41 vp lint apps/comparison/e2e/avatar-visual.spec.ts exit 0 (0 warnings, 0 errors, 1 file). 19:41 vp run check exit 0, 4471 formatted, 3211 lint-clean, tsc pass. apps/comparison/dist was dated 19:13, after 2732f395 at 16:28, and the built avatar-demo still names comparisonHarnessAvatarSrc, so this seat did not rebuild. 19:44 vp run docs:generate exit 0; .claude/current/roadmap.md and .claude/current/status.md stayed byte-identical. 19:44 vp run docs:check exit 0. 19:45 vp run docs:generate exit 0; those two views stayed byte-identical. 19:45 vp run docs:check exit 0. `avatar-visual.spec.ts` runs in no CI job, so its red went unseen. The head count still belongs to a pushed certified report job, so #578 stays in-progress.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: "w-578g. The w-578f note said an explicit ?value= was coerced by || and a later comparison:controls-change carrying that date sometimes reached only one stack, and it pointed at the init dispatch. That dispatch is component-controls.ts:49, inside initializeForm. initializeComparisonControls runs it from component-example-section-mount.tsx:32, synchronously, before Promise.all of the react-mount and solid-mount imports at :36-41. The fixture listeners are registered only once those islands mount: React useEffect at fixtures/styled/datefield.js:37, Solid onSettled (datefield.tsx:36) at datefield.tsx:49. The init dispatch therefore reaches neither stack, which matches the probe that note already recorded (both empty, both September). This seat did not re-run that probe. The dispatch that can land between the two island mounts is the change-driven one: the document input listener at component-controls.ts:152 and the change listener at :164 call dispatchFormControls at :160 and :172; dispatchFormControls is :147 and calls readControls at :149, whose present-field read is :81. ComponentExampleControls synthesizes that change with queueMicrotask at ComponentExampleControls.tsx:79-83. Reached only one stack stays plausible and unproven. The 2b7690ee fix is right either way, because it repaired both readers: controlValueFromSearch at :45 and controlValueFromField at :81. Two fixture readers still disagreed once an explicit empty param dispatches the empty string. badge-demo.ts:92 and link-demo.ts:60 used ||, so ?children= read Published and View project, while normalizeBadgeDemoProps and normalizeLinkDemoProps keep the empty string. Both sites are now ??, the shape datefield-demo.ts:226 and textfield-demo.ts:75 already use. Nothing else in either module. The sweep walks every *-demo.ts module that exports both a PropsFromSearch reader and the matching normalize function: 76 pairs, each asserting that children= equals normalize of children set to the empty string. Before the edit its only mismatches were ./badge-demo.ts (Published vs empty) and ./link-demo.ts (View project vs empty). readControls was private. 20:23 rg scripts/component-controls over **/index.ts exit 1, no match, so it is not re-exported and is now exported from the app script. Its control-value import is the relative ../data/control-value. 20:23 rg @comparison in vitest.config.ts exit 1, no alias, and apps/comparison/vitest.config.ts was not touched. Root vitest include is apps/comparison/src/data/**/*.test.ts, apps/comparison/src/worker.test.ts, and scripts/**/*.test.ts. 20:14 picomatch exit 0: apps/comparison/src/scripts/component-controls.test.ts matches none of those three. demo-empty-param.test.ts imports that test, so vp test run apps/comparison/src collects it. 20:15 vp test run apps/comparison/src/data/demo-empty-param.test.ts --maxWorkers=2 before the ?? edit, exit 1, 3 failed | 1 passed. AssertionError: expected 'Published' to be '' // Object.is equality. AssertionError: expected 'View project' to be '' // Object.is equality. The third failure is agrees across 76 search/normalize pairs, and those two were its only mismatches. The readControls case passed. 20:16 the same command after ??, exit 0, 4 passed. 20:16 the same command with readControls temporarily restored to String(submittedValue || defaultValue), exit 1, 1 failed | 3 passed. AssertionError: expected '2025-02-03' to be '' // Object.is equality, at apps/comparison/src/scripts/component-controls.test.ts. Restored before this commit. 20:18 vp fmt on the five TypeScript paths, exit 0, 5 files. 20:18 vp test run apps/comparison/src --maxWorkers=2, exit 0, 20 files, 170 passed, on a tree that still had a one-line comment later removed. 20:20 vp fmt --check on the five TypeScript paths, exit 0, 5 files. 20:20 vp lint on those five, exit 0, 0 warnings, 0 errors, 5 files, 98 rules. 20:20 vp test run apps/comparison/src --maxWorkers=2, exit 0, 20 files, 170 passed. 20:21 vp run check, exit 0, 4473 formatted, 3213 lint-clean, tsc pass. The heavy lock was taken as w-578g for that typecheck and removed; 20:22 the lock was absent. 20:22 vp run guard:layer-boundary, exit 0, PASS, 524 identical, 84 diverged, 0 new forks. 20:24 vp run docs:generate, exit 0; .claude/current/roadmap.md and .claude/current/status.md stayed unstaged. 20:24 vp run docs:check, exit 0. 20:25 vp run docs:generate, exit 0, those two views still unstaged. 20:25 vp run docs:check, exit 0. 20:25 vp run docs:check once more, exit 0. No package under packages/ changed, so no changeset. certified-waivers.json and certified-case-floor.json untouched. The head count still belongs to a pushed certified report job, so #578 stays in-progress.",
    }
  - {
      state: in-progress,
      at: 2026-09-22,
      note: 'w-ci81. Reported red: Certification Gates run 35803106919 at 81affe37, job 106998461561, shard 3/8, Merge certified reports over-flaky with flakyBudget 0. The case passed only on retry: e2e/drivers/state-matrix.ts:135:9, D1 state matrix — Dialog surface, modal-open · light. First attempt at state-matrix.ts:154, dialog · modal-open · light · default · target, expected React outline-color rgb(41, 41, 41) and outline-width 3px, received Solid rgb(16, 16, 16) and 1px; outline-style none and outline-offset 0px on both. This seat did not re-fetch that run. Before, 22:17 from apps/comparison, PLAYWRIGHT_BROWSERS_PATH=/home/emoporemilio/.cache/ms-playwright and VIVIANA_GATE=1, vp exec playwright test e2e/certified/dialog.certified.spec.ts -g "modal-open · light" --repeat-each=20 --retries=0 --workers=2, exit 1. Running 60 tests using 2 workers. 9 failed, 51 passed (2.1m). Summary revision 23f0719c7117e8df233e7d964d237123ca532cd1: dialog-surface D1 11 passed and 9 failed, D3 20 passed and 0 failed, D7 20 passed and 0 failed, 0 skipped, 0 waived, 0 flaky. The saved diff is that same pair, outline-style none and outline-offset 0px included. 22:38 the same command with --workers=4, exit 0, 60 passed (1.9m), D1 20/0, D3 20/0, D7 20/0. Mechanism: the D1 target is the role=dialog node. Its author outline is outlineStyle none only, dialogInner at packages/solid-spectrum/src/dialog/Dialog.tsx:398 and upstream react-spectrum/packages/@react-spectrum/s2/src/Dialog.tsx:101. The modal WHCM outline is outlineStyle solid, outlineWidth 1, outlineColor transparent at Dialog.tsx:348-350, and that node is the pixel parent, not the D1 target. state-matrix.ts:45-48 records outline-color, outline-style, outline-width, and outline-offset, and :154 asserts solid equals react. walk.ts:156 waits scenario.settleMs, which surfaceScenario sets at dialog.certified.spec.ts:116 to 400, then reads computed style. With outline-style none, an unfocused box keeps the UA initial width medium (3px) and color currentcolor (rgb(41, 41, 41) in this light theme). :focus-visible supplies width 1px and color rgb(16, 16, 16) while style stays none. Both stacks run the iOS VoiceOver blur. Upstream react-spectrum/packages/react-aria/src/dialog/useDialog.ts:65-82 calls focusSafely, then 500ms later blurs and calls focusSafely again when the active element is the dialog or the body. The port packages/solidaria/src/dialog/createDialog.ts:88-108 does that inside runAfterPaint. focusSafely defers only while modality is virtual: packages/solidaria/src/utils/focus.ts:283 and react-spectrum/packages/react-aria/src/interactions/focusSafely.ts:35. The pointer opener uses clickLocator at apps/comparison/e2e/comparison-page.ts:399-401, pointerdown, pointerup, then el.click(), and el.click() is a detail-0 virtual click. 22:35 probe on that walk, --repeat-each=4 --workers=2, exit 0, 4 passed (12.5s). React focusin at 114, 114, 116, and 114 ms; focusout to body at 545, 542, 539, and 542; focusin back at 547, 548, 545, and 547. The settle read was 3px on body with focus-visible false three times and 1px on the dialog once. Solid focusin at 113, 120, 119, and 114; focusout to body at 613, 620, 618, and 613; focusin back at 614, 621, 620, and 614. The settle read was 1px on the dialog all four times. 900ms later every sample on both stacks was 1px on the dialog with focus-visible true. Solid''s focusin-to-focusout span is 500, 500, 499, and 499 ms, so its timer starts with the focus. React''s span is 431, 428, 423, and 428 ms, so its timer is already running when focus lands. The 400ms settle reads React during the body gap and Solid before its blur. Once both have refocused, the outlines match. Fix: openDialogWithPointer at dialog.certified.spec.ts:38 waits, after visibility, for a focusout whose target is that dialog and the following focusin that leaves it active, then expects the dialog focused. No sleep was added. flakyBudget, certified-case-floor.json, and the outline allowlist are unchanged. The keyboard opener still drains the same timer with waitForTimeout(600) at dialog.certified.spec.ts:92. The throwaway probe was deleted before this commit. No package under packages/ changed, so no changeset. After, 22:41 the workers=2 command, exit 0, 60 passed (3.3m), D1 20/0, D3 20/0, D7 20/0, 0 skipped, 0 waived, 0 flaky, revision 23f0719c. 22:44 workers=4, exit 0, 60 passed (1.6m), the same zeros. 22:46 vp exec playwright test e2e/certified/dialog.certified.spec.ts --retries=0 --workers=2, exit 0; the runner reported 1 worker and 18 passed (1.8m), every dialog driver 0 failed. 22:46 vp fmt --check apps/comparison/e2e/certified/dialog.certified.spec.ts exit 0, 1 file. 22:46 vp lint on that file exit 0, 0 warnings, 0 errors, 1 file, 98 rules. 22:49 vp run check exit 0, 4477 formatted, 3214 lint-clean, tsc pass. This seat did not rebuild apps/comparison. 22:51 vp run docs:generate exit 0; .claude/current/roadmap.md sha256 000f09e9a3a6c7ccf7673b941805781d56c2ef91685fb7a882ab4770c505de45 and .claude/current/status.md sha256 d7e9884f988b898ce294c0e52f10760a42a2129f4374ad7f34d3316be5416332 were unchanged. 22:51 vp run docs:check exit 0. The head count still belongs to a pushed certified report job, so #578 stays in-progress.',
    }
---

## Why this is filed above the remaining gate reds

#547 asks for "a certified run whose result is recorded, not waived", and the
owner's decision on 2026-09-20 was that the evidence comes from re-enabling
Certification Gates and running it in CI. That has happened. The answer it
returned on 2026-09-20 was 169 failures; on 2026-09-21, after the fixes the
history above names, it is **27**.

So the gap between here and the release is not four gate tickets. It is four
gate tickets **and this**. Every one of #573, #574, #575 and #576 is smaller
than this ticket, and none of them is on the critical path in the way this is:
a green `gates` job with a red certified suite is not a releasable revision
under #547's own words.

## Where the 27 are, run 35689146611 at `d6745471` (2026-09-22)

Re-read, as this ticket owed, from the first `certified report` job at or past
`90297632`: run 35689146611 at `d6745471`, six commits on, job 106625669687,
failed. It returns the same 27 rows as `b22a44eb`, so the table below stands
unchanged and now rests on a job that ran after #608's and #545's commits.

`Totals: 2146 passed, 27 failed, 4 skipped, 0 waived, 0 flaky`. Five
components out of the 107 in that job's per-component table; the other 102 are
green, re-derived from the job log rather than carried forward. **The
`0 waived` is a defect, not a verdict**: the five entries below were in that
job's `Unwaived failures` list because their patterns matched nothing, which
the 2026-09-22 note above measures and this commit fixes.

**This table is still not a reading of HEAD.** #497's fix (`d997d01f`,
`6031691e`) and the waiver fix land after `d6745471`, and the sha they land on
owes the next reading. It cannot be taken on this host: the suite needs both
stacks built across eight shards, and this seat does not push.

| component         | driver | failures | class     | owner | in the RC                             |
| ----------------- | ------ | -------: | --------- | ----- | ------------------------------------- |
| combobox-list     | D1     |        6 | behaviour | #497  | fixed, never waived                   |
| combobox-list     | D3     |        6 | behaviour | #497  | fixed, never waived                   |
| combobox-list     | D7     |        2 | behaviour | #497  | fixed, never waived                   |
| combobox-list     | D9     |        6 | behaviour | #497  | fixed, never waived                   |
| combobox-list     | D10    |        2 | behaviour | #497  | fixed, never waived                   |
| picker-trigger    | D13    |        2 | behaviour | #584  | waived, 2 entries, expires 2026-10-21 |
| togglebutton      | D2     |        1 | behaviour | #583  | waived, 1 entry, expires 2026-10-21   |
| togglebuttongroup | D2     |        1 | behaviour | #583  | waived, 1 entry, expires 2026-10-21   |
| tabs              | D4     |        1 | behaviour | #609  | waived, 1 entry, expires 2026-10-21   |

No row is crash-class: nothing throws and nothing fails to render. The 22
ComboBox rows are one defect seen through five drivers and are being fixed
rather than waived, so the blocking `certified report` job exits 1 until #497
lands — the five waivers cover the other five rows only, one entry per case so
that each entry's reason names the causes that row actually carries.

The certified tooltip row is not in this table. It passes and turns the job red
only through `flakyBudget: 0`; it is #608, and it was green in this run.

## Snapshot: the shape at 169, run 35546816816 at `ef7d4c4d` (2026-09-20)

Kept because the ordering argument it carries was true when it was made, and
because the families it names are how the 169 were worked down to 27. Every
count below is that run's, not today's.

169 unwaived failures over 37 component × driver rows. By driver:

| driver     | failures | what it is        |
| ---------- | -------: | ----------------- |
| D3         |       61 | pixel diff        |
| D1         |       50 | state matrix      |
| D9         |       24 | forced colors     |
| D7         |       19 | contrast          |
| D2         |        6 | motion (reduced)  |
| D10        |        4 | RTL state matrix  |
| D13        |        2 | journeys          |
| D4, D6, D8 |   1 each | event, ax, target |

By component:

| component       | failures |     | component                                                 | failures |
| --------------- | -------: | --- | --------------------------------------------------------- | -------: |
| toast           |       25 |     | popover-surface                                           |       13 |
| daterangepicker |       22 |     | toast-icon                                                |       12 |
| datepicker      |       22 |     | form                                                      |       12 |
| combobox-list   |       22 |     | calendar                                                  |        6 |
| picker-list     |       21 |     | rangecalendar                                             |        5 |
|                 |          |     | picker-trigger, datepicker-motion, daterangepicker-motion |   2 each |
|                 |          |     | togglebutton, togglebuttongroup, tabs                     |   1 each |

111 of the 169 are D1 and D3, which are both screenshot drivers, and ten of the
thirteen components render their content in an overlay.

## What that pattern is worth, and what it is not

It is worth an ordering, not a diagnosis. **Do not write "one cause" into a
commit until a run proves it.** Two observations cut against the easy story:

- `form` fails D1 and D3 at `size-s`, `size-l` and `size-xl` in both schemes
  and **passes at `size-m`**. A portal that does not render does not pass at one
  size. That is a size-dependent layout change.
- `tabs` D4 is an event-sequence failure and `togglebutton` D2 is motion.
  Neither is a screenshot.

So there are at least three families here, and the overlay screenshot family is
merely the largest. The standing memory that portaled overlays with
`light-dark()` fills need `setColorScheme()` on the portal root is a candidate
for that family and is a hypothesis, not a finding.

These are regressions, not old debt: the same suite was 0 failed at `0f1e1198`,
and the total only moved 2174 → 2177. Everything in this list appeared during
the Solid 2 port window.

## Work

1. ☑ Reproduce one failure locally before theorising about 169. Take
   `toast` D1 `neutral · dark` — the largest component, the cheapest driver —
   and look at the actual diff image, not at the test name.
2. ☑ Group the 169 by proven cause, not by apparent family. The receipt is the
   worklist; a row leaves it only when a run says why it failed. Done by the
   2026-09-21 census; what the grouping left is the 27 table above.
3. Fix by cause. Each cause owes its own commit, its own re-run of the affected
   spec, and a changeset if it touches a published package. What is left is
   #497 (22 rows, one ComboBox list defect), and #583, #584 and #609 after the
   RC.
4. Anything that is a deliberate Solid 2 divergence rather than a defect goes to
   `certification-debt.md` **by name**, with the owner's call recorded. It does
   not go to `certified-waivers.json` without a ticket and an expiry, which that
   file already enforces.

## Done when

The certified suite on one revision reports zero unwaived failures, or every
remaining failure is named in `certification-debt.md` with an owner decision,
and the postcard in `apps/comparison/src/data/certified-suite-evidence.ts` is
re-pinned to that revision — which is #194's Done when, reached from this side.

Concretely, at the RC: `waiverGateFails` false, which means every remaining
failure matched by a waiver whose ticket is open on the board, whose `expires`
is in the future and inside the horizon a release bounds, and whose pattern is
anchored at both ends on one case. All five waivable failures are there now, as
five entries under three tickets — #584 two, #583 two, #609 one — and they are
measured as 5 waived, 22 unwaived against the 27 failure records run
35689146611 itself wrote. The 22 ComboBox rows are not waivable, so the gate
stays red and #497 is what closes them.

One condition is not met yet and is this ticket's, not #497's: a `certified
report` job at a sha carrying both this commit and #497's has to say so. Until
then the 5 waived above is a local reading of a CI run's records, not a CI
count, and the head count is `d6745471`'s.

## Relationship

Child of #544, and the thing stage 3 of
`.agents/CONDUCTOR-RELEASE-PATH-2026-09-20.md` was pointing at without knowing
its size. Blocks #547 directly: this is the "recorded, not waived" evidence.

Closes the open question in #194, which asks for the certified record pinned to
HEAD — it cannot be, because HEAD does not pass. #194's slices 1–3 are why this
was visible at all: slice 1 is what turned a silent green merge into the failing
step that produced this list.

Work-children, each owning rows of the 27 table: #497 (ComboBox list, 22 rows,
fixed not waived), #584 (Picker D13, 2), #583 (ToggleButton and
ToggleButtonGroup D2, 2), #609 (Tabs D4, 1, opened from this census). #608 is
the fifth child and owns no failing row — it is the tooltip case that turns the
same job red through the flake budget.

Bears on #576, which found the playground Toast region missing its
`Notifications` landmark. Toast is also the largest certified cluster at 25.
Whoever takes either ticket should read the other first; if they are one defect,
#576 is its cheapest reproduction and should be fixed first.

Not a duplicate of #574 (`comparison parity (strict)`) or #575/#576
(`a11y:smoke`): those are steps of the `gates` job, this is the `certified`
job, and the two ladders' relationship is #568's subject.
