# Commands

The command policy lives in [`.claude/current/tooling.md`](../current/tooling.md).
Use `vp`. Raw `pnpm` is only for debugging pnpm itself.

```bash
vp install
vp run dev
vp run comparison:dev
vp run test:run
vp run check
```
