---
kind: process
status: current
tasks:
  - id: gridlist-double-toggle
    title: Fix GridList Space double-toggle (browser evidence, then faithful)
    state: in-progress
    roadmap: upstream-parity-loop
  - id: press-path-phase2
    title: Migrate the three item hooks onto createSelectableItem (T-51, T-56)
    state: next
    depends: [gridlist-double-toggle]
    roadmap: upstream-parity-loop
  - id: keyboard-nav-behavior
    title: keyboardNavigationBehavior='tab' collection model (T-34)
    state: open
    depends: [press-path-phase2]
    roadmap: upstream-parity-loop
  - id: table-focus-ring
    title: TableView cell focus-ring --topFocusRing re-architecture (T-59)
    state: open
    roadmap: upstream-parity-loop
  - id: rac-form-field-wrappers
    title: Absorb the 9 unported RAC Checkbox/Radio/Switch Field/Button/FieldContext names
    state: open
    roadmap: upstream-parity-loop
  - id: autocomplete-collection-bridge
    title: Wire SearchField/Menu onto autocomplete contexts (Bucket D)
    state: open
    depends: [port-list-keyboard-delegate, port-context-slots]
    roadmap: upstream-parity-loop
    note: >-
      FLAGGED for owner — blocked on headless-spine-port. Scoped 2026-06-21: not
      additive wiring. Needs three keystones our repo lacks — a shared
      useSelectableCollection (no createSelectableCollection; FOCUS/CLEAR-FOCUS +
      autoFocus-on-mount live there upstream), live slot-capable useContextProps
      (currently non-reactive, zero call sites; Provider is a no-op), and filtered
      list state (createListState has no filter). Resume after the spine lands; do
      NOT fake it with bespoke per-widget consumers.
---

# Upstream release audit — RAC 1.14→1.19 / S2 1.0→1.5

Status: live audit.
Update when: a T-NN ticket changes state or a new upstream release is audited.

> **Loop resumed (2026-06-20).** The client-readiness `UC-NN` track landed
> (`ui-client-contract.md`), and **T-57** (focusable-utils port) and **T-58**
> (autocomplete `autoFocusOnMount` + Arrow inline-nav, hook level) are now done.
> The next open work is **T-59** (table focus-ring); the **T-34**/**T-51**
> press-path epics stay gated together — their shared Phase 0 (**T-52**, the
> onSelect layer split) landed 2026-06-20; **T-56** falls out in the epic's
> Phase 2. Note: T-58's
> autocomplete↔collection bridge is still unwired repo-wide — a collection
> consumer honoring `collectionProps.autoFocus` / the focus-clear events is the
> autocomplete consumer-wiring port already tracked in `upstream-sync.md`
> (Bucket D), distinct from the T-51 `replace`-mode cluster.
>
> **`autocomplete-collection-bridge` scoped + FLAGGED (2026-06-21).** The
> overnight pass reached this Bucket-D item and confirmed it is **not** the
> additive wiring the plan assumed — it is **blocked on `headless-spine-port`**.
> `createAutocomplete` is a faithful controller (it dispatches
> `AUTOCOMPLETE_FOCUS_EVENT`/`AUTOCOMPLETE_CLEAR_FOCUS_EVENT` and emits
> `collectionProps.{filter,autoFocus,shouldUseVirtualFocus}`) and
> `Autocomplete.tsx` provides the contexts, but **no collection consumer can
> receive any of it** without three spine keystones we lack: (1)
> `port-list-keyboard-delegate` — upstream wires the FOCUS/CLEAR listeners +
> `autoFocus`-on-mount + virtual-focus nav inside `useSelectableCollection`; we
> have no `createSelectableCollection` (nav is hand-rolled per widget), so the
> bridge has nowhere shared to attach; (2) `port-context-slots` — `useContextProps`
> is a non-reactive zero-call-site merge and `Provider` is a no-op, so
> `SearchField`/`ListBox`/`Menu` can't consume the contexts + merge refs the way
> upstream does; (3) filtered list state — `createListState` has no `filter`
> (upstream's `UNSTABLE_useFilteredListState`). Faking it with bespoke per-widget
> consumers is explicitly out (parity-is-the-rule + the spine-duplication debt).
> Resume after `port-list-keyboard-delegate` + `port-context-slots` land. See
> tech-debt §"Shared headless spine is re-implemented per widget".
>
> **Re-verified 2026-06-21 (owner cleared the breaking-change concern; bridge
> still blocked structurally).** The owner's "breaking doesn't matter, parity is
> priority" resolved the _picker/treeview_ breaking flag but does **not** dissolve
> this item — its blocker is a missing-primitive dependency, not a breaking-API
> decision, and "parity is priority" argues _against_ the per-widget shortcut. A
> ground-truth source read confirmed the verdict with one correction: blockers A
> (no shared `createSelectableCollection`; FOCUS/CLEAR + `autoFocus`-on-mount
> absent from `createListBox`/`createMenu` — `createMenu` even declares an
> `autoFocus` prop it never reads, `createListBox.ts`/`createMenu.ts:89`) and C
> (`createListState` takes no `filter`, `solid-stately/.../createListState.ts`)
> are **hard-confirmed MISSING**. Correction to blocker B: the slot-reading
> infrastructure is **not** inert — `getSlottedContextProps`
> (`solid-spectrum/src/button/spectrum-context.ts:15`) is a real, widely-used
> utility, and `Autocomplete.tsx` already provides `AutocompleteContext` /
> `AutocompleteCollectionContext` correctly. The genuine B gap is narrower: **no
> consumer reads them** — `SearchField`/`ListBox`/`Menu` never call
> `useAutocompleteInput()`/`useAutocompleteCollection()`. Doing the bridge
> faithfully therefore still requires the shared selectable-collection + filtered
> list-state spine (A + C), so the consumer wiring (B) has somewhere upstream-true
> to attach instead of being scattered per-widget. **Verdict stands: cannot be
> built faithfully today; remains gated on the Phase-3 spine port.** Whether to
> pull that epic forward is a sequencing call for the owner.

A backlog of **atomic tickets** distilled from Adobe's release notes across every
train since we started porting (the S2 1.0.0 major) up to our current pin. Each
ticket is a single "do we have this change, or not?" question. Working this list
is how we catch the changes the mechanical gates can't see: the
`guard:upstream-test-parity` oracle diffs ARIA _vocabulary_, so it is blind to
behavioral/focus fixes, new props, and whole new components that don't alter an
asserted role. The release notes are the only signal those happened — see the
"Read the release notes for intent" step in [upstream-sync.md](./upstream-sync.md).

> **Status — pass 1 complete; source-based depth-verify done; web pass 2 done. No 🔍 tickets remain.**
> The tickets below come from the release **highlights** (pass 1, done). A
> **source-based depth-verify** then resolved the greppable list: confirmed-present
> **T-07, T-12, T-13, T-14, T-15, T-20, T-24, T-27, T-28, T-30, T-33** and the
> animated **T-01**; confirmed-gap **T-02** (EditableCell; **now ported ✔**), **T-18** (horizontal
> virtualization; **now ported ✔**), **T-32** (field `prefix`; **now ported ✔**). The **web pass 2** — mining the per-PR
> "Fixed" lists for the genuinely _behavioral_ tickets, then reconciling each against
> the vendored 1.18/1.4 `src` — is now **complete**: **present** — **T-03** (the
> `render` / `DOMRenderFunction` element-swap prop, faithfully wired), **T-09**
> (overlay positioning — `calculatePosition` is a faithful post-#9343 port incl. the
> non-viewport-container `boundaryToContainerTransformOffset`), **T-05**
> (Menu-in-Popover — architecture-immune: our runtime-trigger model avoids the
> React-portal context leak #9549 fixed), and **T-06** (TreeView `disabledBehavior` —
> present; the minor `forcedColors: GrayText` HCM styling is **now ported ✔**); **new gaps** — **T-10**
> (the whole `scrollIntoView` / `scrollIntoViewport` util family + its collection
> wiring was unported; **now ported ✔**), **T-31** (S2 `TableView`
> `selectionStyle="highlight"` — upstream _does_ have it; **now ported ✔**), and
> **T-22** (icon set was 8 of 410 behind; **now regenerated ✔**). Execution of the confirmed ⛔ gaps (leaf-first, no conflicts): **T-29
> SliderFill ✔**, **T-25 Calendar month/year pickers ✔**, **T-23 Calendar
> multi-select ✔**, **T-26 RangeCalendar available-range ✔** — which **completes the
> Calendar 1.18 cluster** — and **T-16 Table expandable rows ✔** (the `UNSTABLE_`
> tree grid, headless React-Aria parity), and **T-31 TableView
> `selectionStyle="highlight"` ✔** (the styled S2 highlight selection, mirroring the
> Tree T-20 port), and **T-02 EditableCell ✔** (the S2 inline cell editor —
> desktop popover + touch dialog), and **T-18 horizontal virtualization ✔** (the
> `orientation` axis through `VirtualizerLayouts` / `Virtualizer`, with GridList
> gaining an `orientation` prop + `data-orientation`), and **S2 Toast animations ✔**
> (the View Transitions API enter/exit/restack port). **No confirmed ⛔ gaps remain.**

> **Train 6 — RAC 1.18→1.19 / S2 1.4→1.5 — backlog filed; execution pending.**
> The single shared 1.19/1.5 train (one minor each, same commit `1c84a49a`,
> published 2026-06-17) was absorbed via the lightweight single-train path in
> [upstream-sync.md](./upstream-sync.md): oracle refreshed, pin bumped, the five
> mechanical gates re-run clean (and two long-broken gates repaired — see below).
> Its changes are filed as **T-34…T-50** below, grouped by feature, oldest-first.
> Initial triage from the `git diff 791377f0..1c84a49a` over `src`+`test`: confirmed
> gaps **T-34** (`keyboardNavigationBehavior`), **T-36** (`UNSTABLE_focusOnEntry`),
> **T-37** (column-reverse `ListKeyboardDelegate`), **T-38** (`onAction(key, value)`),
> **T-39** (overlay `getTargetRect`) — all greppably absent; **T-45** CenterBaseline
> already present internally (✅); the rest 🔍 (behavioral, reconcile per-ticket).
> The bulk of the 496-file diff is mechanical noise with no port concern (removal of
> `// @ts-ignore` before glob-JSON imports tree-wide, plus stories/docs/s2-docs/test
> churn); `react-stately/src` carried only that noise. **First ticket executed: T-38** (Menu `onAction(key, value)`, changeset `menu-onaction-value.md`); the rest pending.
> Two unrelated gate repairs landed with the absorb (tooling, not ports): the RAC
> barrel moved to `exports/index.ts` (`guard:rac-parity` / `guard:rac-export-gap` had
> been silently crashing on the old `src/index.ts` path since ≥the 1.18 pin), and
> `guard:virtualizer-keyboard-parity` had been red since the 2026-04-30 formatting
> baseline on a single-vs-double-quote regex literal (behavior was always intact).
> **Newly surfaced by fixing `rac-export-gap`** (pre-existing, predates this train,
> _not_ a Train 6 item): RAC exports `CheckboxField`/`CheckboxButton`/`CheckboxFieldContext`,
> `RadioField`/`RadioButton`/`RadioFieldContext`, `SwitchField`/`SwitchButton`/`SwitchFieldContext`
> (the `*Field`/`*Button` form-field wrappers) that solidaria-components doesn't — track
> separately as a backlog of unported RAC surface.

## Scope & sources

| Train (date) | RAC    | S2                           | Notes (highlights)                        |
| ------------ | ------ | ---------------------------- | ----------------------------------------- |
| 2025-12-16   | 1.14.0 | **1.0.0** (S2 GA, the major) | rac v1-14-0 · rs v1-0-0                   |
| 2026-02-04   | 1.15.0 | 1.1.0                        | rac v1-15-0 · rs v1-1-0                   |
| 2026-03-04   | 1.16.0 | 1.2.0                        | rac v1-16-0 · rs v1-2-0                   |
| 2026-04-15   | 1.17.0 | 1.3.0                        | rac v1-17-0 · rs v1-3-0                   |
| 2026-05-30   | 1.18.0 | 1.4.0                        | rac v1-18-0 · rs v1-4-0                   |
| 2026-06-17   | 1.19.0 | 1.5.0                        | rac v1-19-0 · rs v1-5-0 ← **current pin** |

Tickets below are built from the **per-package GitHub Release highlights**
(`gh release view '<pkg>@<ver>' --repo adobe/react-spectrum`). Those are summaries;
the exhaustive per-PR "Fixed" lists live on the website
(`https://react-aria.adobe.com/releases/v1-16-0`,
`https://react-spectrum.adobe.com/releases/v1-2-0`, …). Mining those pages is the
**second pass** — this list is the high-value first pass.

## How to work it

- **Oldest first.** Port/verify trains top-to-bottom. Later trains often build on
  earlier ones (e.g. expandable Table rows assume the collection refactor); doing
  them in order avoids porting against a foundation that isn't there yet.
- **Reconcile to the pin, not the intermediate.** Our oracle is pinned at
  1.18/1.4, so the _current truth_ for any surface is its 1.18/1.4 state. When an
  earlier ticket touches a surface a later ticket also changes (flagged
  **⇄ dedup/superseded** below), port the net-current behavior — don't re-port the
  intermediate and then overwrite it.
- **Verify, don't trust the status tag.** Tags are a first-pass signal (grep +
  spot checks); an identifier existing ≠ the feature fully/faithfully ported.
  Confirm each against upstream `src` + our code before closing, the same
  component-wrong-vs-test-wrong way the parity sweep does, and land a changeset
  when published-package `src` changes.

**Status legend:** ✅ have (identifier present — verify depth) · ⛔ gap (confirmed
absent — real port ticket) · 🔍 verify (uncertain: behavioral, or matched only a
generic term) · ✔ done (ported by us this cycle) · ➖ n/a (docs/website/deps/
internal — not a port concern).

---

## Train 1 — RAC 1.14.0 / S2 1.0.0 (2025-12-16, S2 GA)

- [x] **T-01** ✔ RAC — animated **Tab** transitions (animate the selected-indicator/panel between tabs). **Verified present + refined** this cycle (changeset `selection-indicator-reduced-motion.md`): our `Tabs` already animates the selected indicator via a `SelectionIndicator` + a `translate`/`width`/`height` transition (200ms, `out`), matching upstream. The depth-verify surfaced one faithfulness gap — the indicator transition was **not** gated behind `@media (prefers-reduced-motion: reduce): none` the way upstream's is — now fixed, and the **same gap in `SegmentedControl`'s selection pill** was fixed alongside it (the two form the S2 selection-indicator cluster; `Disclosure` already had the gate). **Adjacent finding (separate, larger — now done):** our S2 `Toast` previously implemented **no** enter/exit/restack animations at all (it set `translate`/`opacity` instantly), whereas upstream animates them through the View Transitions API with its own `prefers-reduced-motion` gate. **Now ported** (changeset `toast-view-transitions.md`): a generic `wrapUpdate(fn, action)` hook on `solid-stately`'s `ToastQueue` + a `solid-spectrum` `ToastContainer` that wraps each queue mutation / expand / collapse in `document.startViewTransition()`, with the upstream `Toast.module.css` keyframes + `::view-transition-group()` rules injected at runtime and a `prefers-reduced-motion` fallback (`PRIVATE_forceReducedMotion` test hook). It was a standalone animation-port follow-up, not a reduced-motion tweak.
- [x] **T-02** ✔ S2 — inline **TableView** cell editing. **Was a confirmed gap** (depth-verify); **now ported** (changesets `table-editable-cell.md`, `table-row-modal-editor-support.md`). Upstream ships an `EditableCell` component exported from S2 `TableView.tsx` (the `editableCell` style + `EditableCell`/`EditableCellInner`, ~lines 1480–1834) with a `renderEditing: () => ReactNode` slot that swaps a cell into an edit affordance (TextField, Picker, …) with its own focus + edit-overlay handling; exercised by `EditableTableView.test.tsx`. We now export `EditableCell` / `EditableCellProps` from `solid-spectrum/src/table`, mirroring upstream's two paths: a fine-pointer **Popover** (modal → `role="dialog"`, icon-only save/cancel) and a touch **DialogContainer > CustomDialog** (text buttons), selected by a new `createMediaQuery` primitive (Solid port of `@react-spectrum/utils`' `useMediaQuery`). The supporting `solidaria-components` `TableRow` fix (build children once inside both `TableRowContext`/`ButtonContext` providers; stable `buttonContextValue` getter object) stops the row recreating its subtree when the editor popover toggles, and `Popover` now accepts `AriaLabelingProps`. 6 tests (`EditableCell.test.tsx`).

## Train 2 — RAC 1.15.0 / S2 1.1.0 (2026-02-04)

- [x] **T-03** ✅ RAC — **DOM customization render prop** (new render prop to swap the rendered DOM element — Router `Link`, animation libs). **Verified present** (web pass 2). Upstream added a cross-cutting `render?: DOMRenderFunction<E, T>` — `(domProps, renderProps) => element` — that overrides the rendered DOM element (RAC `utils.tsx` `DOMElement` / `DOMRenderProps`, for router links, animation libs, pre-styled components). Ours exposes the same element-swap `render` prop across `Button` / `RadioGroup` / `Select` / `Menu` / `Checkbox` / `Table` / `TextField` / `Popover` (e.g. `Button.tsx`: `render?: (props: JSX.ButtonHTMLAttributes, renderProps: ButtonRenderProps) => JSX.Element`) and **genuinely consumes it** — `Button.tsx:506` returns `local.render(customRootProps, customRenderValues)` when provided, not just a type. Faithful.
- [x] **T-04** ✔ RAC — **DateField/DatePicker**: constrain dates **on blur**, not as-you-type (their most-upvoted issue). **Behavioral — invisible to the ARIA gate.** **Done by us** this cycle (changeset `datefield-constrain-on-blur.md`): ported upstream's `IncompleteDate` display model into `solid-stately`. A new `calendar/IncompleteDate.ts` (port of `@react-stately/datepicker`) holds the raw segment values, so a momentarily-impossible edit (e.g. month set to February while the day still reads `31`) is held exactly as typed while focused and constrained (Feb 31 → Feb 28) only on blur via `confirmPlaceholder`. `createDateFieldState` was reworked around a `displayValue: IncompleteDate` signal: `setSegment`/`incrementSegment`/`decrementSegment`/`clearSegment` operate on it; a complete + valid edit commits eagerly (fires `onChange`), an incomplete/invalid edit is held without firing `onChange` until blur; `segments` render the raw typed value via `Intl.NumberFormat`. **Divergence removed:** a typed value is no longer snapped to `minValue`/`maxValue` — out-of-range dates are kept and reported through validation (`rangeUnderflow`/`rangeOverflow`), matching React Aria (the 3 old clamp tests were rewritten to assert this). The `DateFieldState` public interface is unchanged, so `createDateSegment` (solidaria) and the `DateField`/`DatePicker`/`DateRangePicker` components (solidaria-components / solid-spectrum) inherit the behavior without API changes. **Solid note:** `confirmPlaceholder` only resets the `displayValue` signal when the value actually changed (a commit or a constrained Feb-31-style value), because blur fires on every focus move and Solid's reference-keyed `<For>` would otherwise recreate the just-focused segment node and drop focus. 13 stately tests (incl. invalid-combo-on-blur + eager-commit); full solid-stately suite green (836).
- [x] **T-05** ✅ S2 — **Menu** renders correctly when opened from within a **Popover** (regression fix; upstream PR #9549 "Render menu popover if MenuTrigger is inside the popover"). **Verified present — architecture-immune** (web pass 2). Upstream's fix adds an `InPopoverContext` + `isPopover = (ctx || isSubmenu) && !inPopover` guard (S2 `Menu.tsx`) to patch a **React-portal context leak**: a `Menu` defined lexically inside a `Popover` inherited `inPopover === true` through the portal and mis-rendered inline. Ours decides popover-vs-inline from the **runtime** trigger type (`usePopoverTrigger()?.trigger === "MenuTrigger" / "SubmenuTrigger"` → `isPopoverMenu()`), which cannot leak through portals, so a MenuTrigger inside a Popover yields `isMenuTriggerPopover() === true` and renders as a popover correctly. The regression is specific to React's lexical-context model. **Follow-up (now done — test-only):** `Menu.test.tsx` → "renders in its own popover when opened from inside a Popover" locks the behavior — a `MenuTrigger` nested in a (non-modal) `Popover` opens its menu as its own `data-trigger='MenuTrigger'` dialog, whose _nearest_ popover ancestor is the MenuTrigger's and never the outer `PopoverTrigger`'s, confirming the popover-vs-inline decision does not leak. **Separately surfaced while writing that test (new, distinct from PR #9549):** a _modal_ outer `Popover`'s `FocusScope contain` collapses a nested _modal_ menu overlay — `onOpenChange` flips the menu open, but the outer focus containment immediately tears the menu's popover back down so it never renders (`role="menu"` count stays 0); the menu only opens cleanly inside a **non-modal** outer Popover today. Upstream supports menus opened from inside modal overlays, so our nested-modal containment needs to become descendant-overlay aware — tracked as its own minor follow-up below.
- [x] **T-06** ✅ S2 — **TreeView** `disabledBehavior` styling updated to latest designs (upstream PR #9472 "Update `disabledBehavior` style"). **Verified present; the minor HCM follow-up is now done** (web pass 2). We have `disabledBehavior` + disabled tree styling; our disabled row-content color uses the `disabled` semantic token (→ `disabled-content-color`, S2's `gray-400`), matching upstream's literal `gray-400`. **Divergence (now closed — changeset `tree-disabled-forcedcolors.md`):** upstream pairs that disabled color with `forcedColors: 'GrayText'` on the row content (`treeCellGrid`) and the `expandButton` chevron; we ported the full upstream color block onto our merged `treeViewItem` (`isDisabled: { default: 'disabled', forcedColors: 'GrayText' }`, `forcedColors: 'ButtonText'`, and the `selectionStyle.highlight` `forcedColors: 'HighlightText'`), added `forcedColors: 'GrayText'` to the disabled description color, and changed the chevron default from `neutral-subdued` to `inherit` (with `forcedColors: 'GrayText'` on its disabled color). Porting the row color also fixed a latent gap where a disabled label kept the enabled `neutral-subdued` color (the label `inherit`s a row color that previously had no disabled branch). Regression-locked structurally (the `style()` macro hashes the color tuple into one atomic class and jsdom loads no macro CSS, so the test asserts the disabled row + description + chevron share the disabled-only color class, absent when enabled); tree suite 15 green, `tsc` validates the system-color tokens.

## Train 3 — RAC 1.16.0 / S2 1.2.0 (2026-03-04)

- [x] **T-07** ✅ RAC — **multi-select ComboBox** (`selectionMode="multiple"`, `ComboBoxValue`, TagGroup display). **Verified present** this cycle: `ComboBoxValue` exported + used in `solidaria-components/ComboBox.tsx`. (Deep tag-display parity is a component-validation concern, not this audit's "do we have it" question.)
- [x] **T-08** ✔ RAC — **Tree sections** (`TreeSection` + `TreeHeader`). **Done by us** this cycle — item 3b, commit `8341b2c3` (rowgroup/row/rowheader port).
- [x] **T-09** ✅ RAC — overlay **positioning** improvements (upstream PR #9343 "Fix overlay positioning for overlays positioned in non-viewport containers"). **Verified present** (web pass 2). Our `solidaria/src/popover/calculatePosition.ts` is a faithful port of the **current (post-#9343)** upstream file: same 16 functions (`getContainerDimensions` / `getDelta` / `computePosition` / `getMaxHeight` / `getAvailableSpace` / `calculatePositionInternal` / …), and the fix-specific token `boundaryToContainerTransformOffset` appears **4/4** — `getMaxHeight` applies `isContainerDescendentOfBoundary ? containerOffsetWithBoundary.top : 0` to the bounding rect exactly as upstream, i.e. the non-viewport-container handling is in.
- [x] **T-10** ✔ RAC — **scroll-into-view** behavior (1.16 refinements: PR #9146 `scrollMargin` + inline/block alignment, PR #9634 scrollbar-width + RTL). **Done by us** (changeset `scroll-into-view.md`). Ported the full `@react-aria/utils` scroll-into-view family — `scrollIntoView(scrollView, element, opts)` + `scrollIntoViewport(target, opts)` (faithful to the post-#9146/#9634 source: `scrollMargin`/`scrollPadding`/border/scrollbar math, block/inline `nearest`/`start`/`center`/`end`, RTL+iOS scrollbar offset, deterministic test branch) into `solidaria/src/utils/scrollIntoView.ts`, plus a `getScrollParents` (plural) helper in `utils/dom.ts`. Wired `scrollIntoViewport` into the three hooks that move **real DOM focus** so keyboard navigation reveals an off-screen focused item: `createDateSegment` (on segment focus), `createCalendarCell` (keyboard-modality focus effect), and `createTable`'s "Mirror React Aria's `useSelectableCollection`" focus-by-data-key effect. 4 util tests mirror upstream `react-aria/test/utils/scrollIntoView.test.ts`. **Follow-up — activedescendant collections now done** (changeset `scroll-into-view-activedescendant.md`): the pure-`aria-activedescendant` collections (ListBox / Menu) and the virtual-focus ComboBox now reveal their focused option through a shared `createScrollIntoViewOnFocus` hook (solidaria) — a faithful port of `useSelectableCollection`'s focused-key scroll effect, which upstream runs for _all_ selectable collections (it is **not** gated on virtual focus). On a keyboard-modality `focusedKey` change it `scrollIntoViewport`s the `[data-key]` element into the collection root, deferred one microtask so the virtualizer's force-include commits the focused row first (no fight — the browser owns scroll and our rAF listener re-windows, converging). **Correction:** the earlier note here wrongly grouped **Select** as an activedescendant collection that "manages its own virtualizer scroll" — Select moves _real_ DOM focus via `focusSafely`, so the browser already reveals the option; it's intentionally left unwired. **Still deferred:** grid / gridlist move roving real focus with no programmatic row focus / `aria-activedescendant` and don't port upstream's `useGridCell` focus-mode walker, so there's no focused-row reveal to attach a scroll to yet.
- [ ] **T-11** ➖ RAC — assorted crash fixes. Mine the per-PR list in the second pass.
- [x] **T-12** ✅ S2 — **ListView** GA. **Verified present**: styled `ListView.ts` + `gridlist/` in solid-spectrum (S2 ListView = the styled GridList).
- [x] **T-13** ✅ S2 — **unavailable menu items** (`isUnavailable`). **Verified present**: `isUnavailable` wired in `solid-spectrum/menu/index.tsx`.
- [x] **T-14** ✅ S2 — **ActionBar** support for **TreeView**. **Verified present**: `actionbar/index.tsx` (solid-spectrum) + `ActionBar.tsx` (solidaria-components), referenced by our `tree/index.tsx` (and gridlist/cardview/table). Deep Tree-integration parity is a component-validation concern.
- [x] **T-15** ✅ S2 — **Picker** custom render for the display value. **Verified present**: `renderValue` in `solid-spectrum/picker/index.tsx` + `solidaria-components/Select.tsx`.

## Train 4 — RAC 1.17.0 / S2 1.3.0 (2026-04-15)

- [x] **T-16** ✔ RAC — **Table expandable rows** (`treeColumn` + chevron button in cells; macOS-Finder-style trees). **Done by us** this cycle (changeset `table-expandable-rows.md`): ported the `UNSTABLE_` tree-grid stack headless-first through all three RAC-equivalent layers, keeping the `UNSTABLE_` prefix. **solid-stately** — new `createTreeGridState` (port of `useTreeGridState`) owns the expanded-keys signal (controlled `UNSTABLE_expandedKeys` / uncontrolled `UNSTABLE_defaultExpandedKeys` / `UNSTABLE_onExpandedChange`), rebuilds the collection on expansion change, and layers `toggleKey` + `expandedKeys`/`treeColumn` getters onto the base state via `defineProperty` (spread would break the reactive getters); collapse-from-`'all'` materialises every expandable row first. `TableCollection` gained a tree-grid mode (`buildTreeRows` stamps each node's `level`/`isExpandable`/sibling position; resolves `treeColumn`), and `createTableState` exposes inert `expandedKeys`/`treeColumn`/`toggleKey` so the row hook reads one shape in both modes. **solidaria** — `createTableRow` emits the tree-grid row ARIA (`aria-expanded` only when the row has children, `aria-level`/`aria-posinset`/`aria-setsize`) and a **press-based** `expandButtonProps` (mirrors `useTableRow`: `onPress` toggles + focuses the row, `excludeFromTabOrder` + `preventFocusOnPress` + `data-react-aria-prevent-focus`, `Expand`/`Collapse` label) so the chevron flows through our Button's `createPress` rather than a raw DOM `onClick`; ArrowRight/ArrowLeft expand/collapse (RTL-flipped, leaf → focus parent), stopping propagation so grid navigation doesn't double-fire. **solidaria-components** — `Table` wires `expandButtonProps` to a `chevron` Button slot and threads the render-prop values (`isExpanded` on rows; `hasChildItems`/`isTreeColumn` on cells) so the consumer renders the chevron only in the tree column of rows with children — S2's `TableView` gating. **Behavioral note removed:** no invented `aria-hidden` on leaf chevrons (upstream conditionally renders the chevron instead). Tests per layer (stately `createTreeGridState`, solidaria `createTableRow` tree-grid + press, components `Table` tree grid); full suites green (solid-stately, solidaria 3397, solidaria-components 2075); `guard:upstream-test-parity` exit 0 (the tree `aria-expanded`/`-level`/`-posinset`/`-setsize` show as we-only — confirmed faithful to `useTableRow` source). **Follow-up — now done** (changeset `table-tree-column-chevron.md`): the S2-styled `ExpandableRowChevron` + tree-column indentation now ship in the `solid-spectrum` styled `TableView`. The styled `TableCell` auto-injects the chevron in the tree column of rows with children (`hasChildItems && isTreeColumn`, S2's `Cell` gating) using the **headless** Button on the row's `chevron` slot (wrapped in a flex `<div>` _inside_ the real `<td>` so fixed table-layout column widths survive), rotates it 90° on expand via the `rotate` shorthand (RTL omitted, matching our TreeView idiom), and indents tree-column cells by depth (`--treeColumnPadding` 36 for leaves vs 16, plus `--indent` 16 per `--table-row-level`). 3 new styled tree-grid tests (chevron injection/ARIA, leaf + non-tree-column omission, press-to-toggle expand/collapse); flat tables unchanged. The Table regression snapshot is intentionally left for the branch's batch regeneration (pre-existing branch-wide staleness, not this change).
- [x] **T-17** ✔ RAC — **window scrolling** in **Virtualizer** (collection scrolls with the page; no fixed height). **Done by us** this cycle (changeset `virtualizer-window-scrolling.md`): the `Virtualizer` now computes its visible viewport as the scroll view's height intersected with the window viewport and offsets the visible range by how far the scroll view has been pushed above the viewport by page/ancestor scrolling, all driven by a single document-level capturing `scroll` listener — mirroring upstream `ScrollView`. A new `allowsWindowScrolling` prop (default `true`, matching RAC `CollectionRoot`'s hard-coded value) opts back into element-only scrolling; an explicit `viewportSize` layout option still wins. Behavior-preserving for a fixed-height collection inside the viewport (the `window ∩ element` math reduces exactly to element scroll), so existing collections are unaffected unless they actually scroll with the page; 58 Virtualizer tests (2 new window-scroll tests) plus the downstream collection suites stay green. **Follow-up — `isScrolling` now done** (changeset `virtualizer-isscrolling-pointer-events.md`): while actively scrolling, the content gets `pointer-events: none` (set on the first scroll, cleared 300ms after the last — folded into one debounce since our scroll handler is already rAF-throttled), carried by a new inner content wrapper (`data-virtualizer-content`) that restores upstream ScrollView's two-div structure (the outer div stays the scroll container so wheel/touch events keep flowing). Upstream's other `isScrolling` use (deferring DOM reordering of reused views) and the relayout scroll write-back are both **N/A** to our architecture: we render the visible slice in order on every pass (no recycled, absolutely-positioned views to reorder) and the browser owns the scroll position (read through a signal), so there is no `scrollToItem`/`scrollToRect` write-back to port — the original phrasing of that API was speculative; it does not exist by those names upstream. ⇄ same feature as **T-21**.
- [x] **T-18** ✔ RAC — **horizontally virtualized** GridList + ListBox. **Done by us** this cycle (changeset `virtualizer-horizontal-orientation.md`). Upstream `ListLayout` (`react-stately/src/layout/ListLayout.ts`) takes an `orientation` option (default `vertical`) and offsets items along `x` or `y` (`offsetProperty = orientation === 'horizontal' ? 'x' : 'y'`); RAC ListBox/GridList expose `data-orientation`. **`VirtualizerLayouts`** — new `Orientation` type + an `orientation` option on `DefaultVirtualizerLayoutOptions` (default `'vertical'`); `ListLayout.getLayoutInfo` offsets along `x` with `width = itemSize` and the cross axis (`height`) filling the viewport when horizontal (mirroring `offsetProperty`/`heightProperty`), and `getDropTargetFromPoint` measures `point.x` instead of `point.y`. `getVisibleRange` stays axis-agnostic; `TableLayout` inherits vertical. **`Virtualizer`** — tracks the scroll view's `scrollLeft` and feeds the horizontal scroll axis + measured width into `getVisibleRange` / page nav when horizontal, and exposes the resolved `orientation` on its context. **`GridList`** — gains an `orientation?` prop (default `'vertical'`) threaded into the render props, the drop-target delegate, and a `data-orientation` root attribute; **GridList + ListBox** windowing spacers reserve the windowed-out extent along `width` when horizontal. 7 new Virtualizer tests (ListLayout x-offset / x-axis drop target, context `orientation` + horizontal layout info, GridList `data-orientation`, horizontal width spacers); full suites green (Virtualizer + ListBox + GridList 170); `guard:upstream-test-parity` exit 0. **Follow-up — now done** (changeset `gridlist-horizontal-keyboard-axis.md`): the custom GridList keyboard handler (`createGridList`) is now orientation-aware, mirroring upstream `ListKeyboardDelegate` for a `stack` layout — a vertical stack leaves Left/Right inert (upstream strips `getKeyLeftOf`/`getKeyRightOf`) while Up/Down navigate; a horizontal stack promotes Left/Right to the primary axis (Right=next, Left=prev in LTR, flipped under RTL) with Up/Down still working. `solidaria-components` `GridList` threads its resolved `orientation` + text `direction` (same `getComputedStyle`/`document.dir` resolution as its DnD delegate) into the hook. 3 new nav tests (horizontal inline axis + block axis, vertical Left/Right no-op, RTL flip); GridList 34 + styled ListView green, typecheck + parity guard clean.
- [ ] **T-19** ➖ RAC — dependency-management improvements (internal; not a port concern).
- [x] **T-20** ✅ S2 — **highlight selection** in **TreeView**. **Verified present**: our `tree/index.tsx` has `TreeSelectionStyle = "checkbox" | "highlight"` (default `checkbox`) feeding the style conditions, mirroring upstream's `selectionStyle` + `selectionBehavior={highlight ? 'replace' : 'toggle'}`. ⇄ The **TableView**-highlight half of **T-31** is separate and is **now ported** (`table-selection-style-highlight.md`) — web pass 2 corrected the earlier claim here: upstream S2 `TableView` **does** expose `selectionStyle`, and ours now does too.
- [x] **T-21** ✔ S2 — **window scrolling** in collection components. ⇄ **dedup of T-17** — the S2-styled collections (`ListBox` / `Table` / `Tree` / `Menu` / …) inherit the default-on window scrolling through the shared `solidaria-components` `Virtualizer`; covered by the same changeset (`virtualizer-window-scrolling.md`), and the S2 collection suites (85 tests) stay green.
- [x] **T-22** ✔ S2 — updated **workflow icons** set. **Done by us** (changeset `workflow-icons-regen.md`). Not actually a S2 1.2.0 release-notes item (the 1.2.0 notes mention no icons; the new icons landed across later trains). Copied the 8 missing upstream `_20_N` SVGs — `ArrowCurved`, `ArrowUpSend`, `BookmarkSingleFilled`, `PremiumIcon`, `StopProcessing`, `ZoomFitToHeight`, `ZoomFitToScreen`, `ZoomFitToWidth` — into `assets/s2wf-icons` and reran `scripts/generate-solid-spectrum-icons.mjs` (then `vp fmt`), bringing our s2wf set to **410 of upstream's 410** with a generated `.tsx` + `s2wfIcons` barrel export for each (`PremiumIcon` → `PremiumIconIcon` under our `<Pascal>Icon` scheme). No API/behavioral work.

## Train 5 — RAC 1.18.0 / S2 1.4.0 (2026-05-30, current pin)

- [x] **T-23** ✔ RAC — **Calendar multiple date selection** (changeset `calendar-multiple-selection.md`). `selectionMode="multiple"` ported across `solid-stately` (defaulted `M` generic + `CalendarValueType`, array value, `isSameDay` toggle, `setValue(null)→[]`), `solidaria` (`aria-multiselectable` on the grid), and `solidaria-components` (`Calendar` generic over `M`, threads `selectionMode`). ⇄ same feature as the S2 1.4 Calendar note; the S2 styled `Calendar` (`@ts-nocheck`) already forwards `selectionMode` through to the headless component.
- [x] **T-24** ✅ RAC — **CalendarHeading** component. **Verified present**: `CalendarHeading` in Calendar + RangeCalendar across solidaria-components and solid-spectrum.
- [x] **T-25** ✔ RAC — **CalendarMonthPicker** + **CalendarYearPicker** (jump to month/year). **Done by us** this cycle (changeset `calendar-month-year-picker.md`): ported the `createCalendar{Month,Year}Picker` aria hooks (solidaria) + the headless `CalendarMonthPicker` / `CalendarYearPicker` render-prop components (solidaria-components). Like upstream, each is context-agnostic (reads `CalendarContext` or `RangeCalendarContext`, works in both Calendar and RangeCalendar) — **not** split per-type. Exposed `minValue`/`maxValue` accessors on both calendar states for the year-window clamp. 6 tests.
- [x] **T-26** ✔ RAC — **RangeCalendar**: available dates derived from the first selected date (`firstAvailableDate` validation). **Done by us** this cycle (changeset `rangecalendar-available-range.md`): `createRangeCalendarState` now mirrors upstream `useRangeCalendarState` — anchor-aware `isDateUnavailable(date, anchorDate)`, an available range derived via `nextUnavailableDate` that narrows the effective min/max (feeding `isCellDisabled` / `isDateOutsideAllowedRange` so the prev/next page buttons disable at the span edges / `constrainDate`, unless `allowsNonContiguousRanges`), and `isValueInvalid` extended to flag a committed range whose endpoint is unavailable or out of `[minValue, maxValue]`. 6 stately tests. The `RangeCalendar` surfaces (solidaria-components / solid-spectrum) inherit the widened callback from `RangeCalendarStateProps`; the `DateRangePicker` adapts it to the 1-arg form its text fields expect (null anchor). **Follow-up — now done** (changeset `daterangepicker-isdateunavailable-anchor.md`): the S2 `DateRangePicker`'s internal display helper annotated `isDateUnavailable` as 1-arg though it already forwarded the anchor-aware callback to the embedded `RangeCalendar` at runtime; the annotation is now the 2-arg `(date, anchorDate) => boolean`, mirroring upstream `useRangeCalendarState` / `react-stately` `datepicker/types.ts`. Type fidelity only — the public `DateRangePickerProps` already exposed the 2-arg form by inheritance and a 1-arg callback stays assignable, so no API/behavior change; existing tests already assert an unavailable day renders `aria-disabled` in the popover. (Aside, out of scope: the shadowed, stale `createRangeCalendarState.d.ts` still shows the 1-arg form, but the co-located `.ts` — the 2-arg source of truth — wins resolution, so it's dead.)
- [x] **T-27** ✅ RAC — **TableFooter** component. **Verified present**: `TableFooter` exported from `solidaria-components/Table.tsx` + `solid-spectrum/table`. ⇄ same as the S2 1.4 TableFooter note.
- [x] **T-28** ✅ RAC — **description + error messages** for **Checkbox / Radio / Switch** (forms). **Verified present**: `description` / `errorMessage` / `FieldError` wired in all three (`solidaria-components/{Checkbox,RadioGroup,Switch}.tsx`). ⇄ same as the S2 1.4 note.
- [x] **T-29** ✔ RAC — **SliderFill** component. **Done** (changeset `slider-fill-component.md`): ported to `solidaria-components/Slider.tsx` as `SliderFill` / `Slider.Fill` — single-thumb fill from `offset` (default minValue → 0%) to the current value, orientation-aware (`inset-inline-start`/`width` horizontal, `bottom`/`height` vertical), with `isHovered`/`isDisabled`/`orientation`/`valuePercent` render props + `data-*`, exported with a `SliderFillContext` alias; 12 tests.
- [x] **T-30** ✅ S2 — **drag & drop** for ListView / TableView / TreeView. **Verified present**: `useDragAndDrop` wired across `ListBox` / `GridList` / `Table` / `Tree` / `Menu` in solidaria-components.
- [x] **T-31** ✔ S2 — **TableView highlight selection** + TableFooter. TableFooter ✅ = **T-27**; the **highlight half is now ported** (changeset `table-selection-style-highlight.md`). Our `solid-spectrum/table` now exposes `selectionStyle?: 'checkbox' | 'highlight'` (default `checkbox`), deriving `selectionBehavior={selectionStyle === 'highlight' ? 'replace' : 'toggle'}` for the headless table (explicit `selectionBehavior` still overrides), gating **both** the select-all column and per-row checkboxes on `selectionStyle === 'checkbox'` with `toggle` behavior, and giving selected rows the blue-tinted highlight background (`color-mix(gray-25, blue-900, 10%)`, `15%` on hover/press) with `Highlight`/`HighlightText` forced-colors fallbacks — the style change is scoped to the highlight path so checkbox mode is byte-for-byte unchanged. `data-selection-style` is exposed for parity with Tree. Mirrors the **T-20** Tree port; 2 new tests + typecheck + the parity guard stay green. The virtualizer-only polish (contiguous-selection-block rounded corners + box-shadow row dividers via `isNextSelected`/`isPrevSelected`) is **now done too** (changeset `table-highlight-selection-block-border.md`): a `::before` overlay — injected once as a shared stylesheet, since the `style()` macro can't express a pseudo-element — draws the rounded blue block border from per-row custom properties the macro sets; `isNextSelected`/`isPrevSelected` (computed from the headless `TableState` `selectedKeys` + `collection.getKeyAfter`/`getKeyBefore`) zero the inner edges and corner radii where a neighbor is also selected; and the highlight-mode gray divider moves from the cell's bottom border to the row's `box-shadow` (checkbox mode keeps the cell border, byte-for-byte unchanged). The **only** upstream detail dropped is the overlay's `z-index: 3`, which exists solely to paint above the S2 virtualizer's sticky cells — N/A for our real-DOM `<table>` (which has none). 2 new tests assert the overlay class + injected stylesheet and the neighbor-flag class changes. **Correction (still stands):** supersedes the earlier T-20 note that wrongly said upstream `TableView` exposes no `selectionStyle`.
- [x] **T-32** ✔ S2 — custom **prefixes** for ComboBox / TextField. **Now ported** (changeset `field-prefix.md`). Upstream hosts `prefix?: ReactNode` on the shared `FieldGroup` (`s2/src/Field.tsx`, with a `prefixId` + `aria-labelledby` association) and threads it into **ColorField, ComboBox, NumberField, TextField** (prefix-only; no `suffix`). We have no shared `FieldGroup` — each field composes its own group/input from its headless context — so the port adds a small shared helper `field/prefix.tsx`: `FieldPrefix` renders the prefix in a baseline-centered, icon-styled container (`CenterBaseline` gained an optional `id`) with a stable `id`, and `PrefixInputProvider` re-provides the field's own context through a proxy that appends that `id` to the input's `aria-labelledby` — preserving reactivity and each context's `inputProps` shape (object getter for TextField/NumberField/ColorField, function for ComboBox). All four fields now accept `prefix?: JSX.Element`; with no prefix the render path is byte-for-byte unchanged. 4 new tests (one per field) + typecheck + the parity guard stay green.
- [x] **T-33** ✅ S2 — **LabeledValue** (display non-editable values). **Verified present**: `solid-spectrum/labeledvalue/`.

## Train 6 — RAC 1.19.0 / S2 1.5.0 (2026-06-17, current pin)

Filed from `git diff 791377f0..1c84a49a` over `src`+`test` (the 1.18/1.4 → 1.19/1.5
delta), grouped by feature, oldest-first. Statuses are first-pass triage — reconcile
each against upstream `src` + our code before closing, and land a changeset when a
published-package `src` changes. None executed yet.

- [ ] **T-34** ⛔ RAC — **`keyboardNavigationBehavior='tab'` collection keyboard model** (headline epic). Upstream `useGridListItem` (the big +141 change) gates child-element propagation so that, in `'tab'` mode, arrow keys inside a focusable row child are left to the browser (except Tab), extracts `handleTreeExpansionKeys()`, binds `onKeyDownCapture` only in `'arrow'` mode, wraps `rowProps.onKeyDown` (base `usePress` runs only if propagation wasn't stopped — fixes Space inside a textfield in a tree row), and adds `onPointerDown`/`onMouseDown` guards (stopPropagation when the target is a tabbable inside the row); `useGridList`/`useTree` thread the prop, and RAC `GridList`/`Tree` expose it (`Tree` drops its `Omit<…, 'keyboardNavigationBehavior'>`). **Confirmed absent** — `keyboardNavigationBehavior` greps to nothing in `packages/`. Cross-hook; scope on its own. ⇄ includes the S2 **ListView** / **TreeView** un-omit of `keyboardNavigationBehavior` (the styled surface of the same feature). ⇄ shares the item-hook press-path migration with **T-51** (`replace`-mode action model); sequence the two together. **Scoped 2026-06-20 in `press-path-epic.md`** (Phase 3 there).
- [x] **T-35** ✔ RAC — **`useTypeSelect` 1.19 refactor — ported.** Upstream splits the handler into a dedicated **capture** path (Space-during-search only) and a **bubble** path (character keys), returning _both_ `onKeyDownCapture` + `onKeyDown`; it **bails on `e.altKey`**, `preventDefault`/`stopPropagation` **only** on a successful match, resets the search + clears the timeout + returns when no match remains even after the retry-from-top, and cleans up the reset timeout on unmount. **Ported all four behavioral deltas** into `createTypeSelect` (changeset `typeselect-keydown-faithful-parity.md`, `@proyecto-viviana/solidaria` patch): (a) re-added `e.altKey` to the bubble bail — **reverses `dfd4d37b`'s "allow altKey"** (pinned 1.19 _does_ bail on altKey); (b) `preventDefault()`/`stopPropagation()` on a matching character (previously only on Space-during-search); (c) reset `search=''` + `clearTimeout` + `timeout=undefined` + return on a no-match; (d) `onCleanup` clears the pending debounce on unmount (upstream's `useEffect` teardown). Search/focus + debounce-restart factored into shared helpers mirroring upstream's duplicated body; the bubble retry is now unconditional (`if (key == null)`) matching upstream (equivalent to our old `&& currentKey != null` guard). **Solid-divergent-by-necessity:** the capture/bubble split is faithfully reproduced, but a capture handler delivered through a `{...typeSelectProps}` spread is **inert** in Solid, so the live bubble `onKeyDown` is the working path — and it _also_ covers mid-search Space because upstream's bubble bail only rejects a **leading** Space (`search.length === 0 && ' '`). True capture-phase binding (ref-based `addEventListener` threaded through every consumer) stays deferred; the split here is the faithful realization within Solid's spread model. **Consumer shims N/A:** upstream's `useSelect` drops an `onKeyDown = onKeyDownCapture` shim and `useComboBox` lets Tab `continuePropagation`, but our `createSelect`/`createMenu`/`createListBox`/`createComboBox` only **spread** `typeSelectProps` (no per-consumer shim ever existed — the alias lived solely inside `createTypeSelect`), and our raw DOM events never carry `continuePropagation` (so upstream's `!('continuePropagation' in e)` guards collapse to unconditional `stopPropagation`). Tests: `createTypeSelect.test.tsx` — Alt-bail (rewrote the former "allows Alt" assertion), `preventDefault` on a matching char, and reset-on-no-match (`'x'` then `'a'` → Apple, proving the buffer reset); **all 3 confirmed discriminating** (exactly 3 fail / 12 pass against the pre-port source). 15/15 typeSelect + 213 type-select consumer tests (menu/listbox/select/combobox/autocomplete) green; fmt/lint/typecheck/guard clean. ⇄ **supersedes** the former sweep-deferred "typeahead true capture-phase binding" item — the capture/bubble split is exactly that work, so it is tracked here now (not separately in upstream-sync.md).
- [x] **T-36** ➖ RAC — **`useSelectableCollection` `UNSTABLE_focusOnEntry`** (`'first' | 'last'`) + virtual-focus-first deps change — **N/A by construction / deferred to Bucket D, confirmed.** Two parts: **Part A (`UNSTABLE_focusOnEntry`) — N/A by construction.** Upstream's `useSelectableCollection.onFocus` gains an experimental `UNSTABLE_focusOnEntry: 'first' | 'last'` option that, on keyboard/virtual entry into the collection, **always** navigates to `delegate.getFirstKey()`/`getLastKey()` instead of restoring the last-focused key or detecting tab direction. It is a **private, single-consumer** API for Adobe's internal chat surface (its own comments name "the prompt field's attachment card", the "Thread", and seeing "the new prompt reply") — a consumer we do not have. We also have **no `createSelectableCollection`**: collection entry-focus is inlined per-hook (`createListBox`'s `createFocusWithin` `onFocusWithin` + the navigation hooks), with no strategy-driven navigate-on-entry to extend — `focusOnEntry`/`navigateToKey` grep only to `createAutocomplete` (the autocomplete's own virtual focus, a different layer). Nothing to port. **Part B (the deps change `[manager.collection]` → `[firstKey, manager.collection.size]`) — belongs to the deferred autocomplete↔collection bridge (Bucket D).** That tuple re-keys the `shouldVirtualFocusFirst` `useUpdateLayoutEffect`, which lives in upstream's `useSelectableCollection` but is **driven by the autocomplete `FOCUS_EVENT`** — i.e. the collection-consumer half of the autocomplete virtual-focus-first bridge. In our port the autocomplete **dispatches** `AUTOCOMPLETE_FOCUS_EVENT` (`detail.focusStrategy: "first"`) from `createAutocomplete`, but **no collection consumer listens + refocuses-first-on-update** — that wiring is the open **Bucket D** gap (`upstream-sync.md`), and there is no consumer-side effect in our tree to re-key. When Bucket D is built, the consumer effect should adopt this `[firstKey, collection.size]` deps shape from the start; it is not a standalone port now. User-confirmed lowest-value (experimental private API).
- [x] **T-37** ➖ RAC — **column-reverse `ListKeyboardDelegate`** (+49) — **N/A by construction, confirmed.** Upstream adds reversed-column (chat-log) navigation to its `ListKeyboardDelegate` class: a private `isReversed(key)` that compares `getItemElement(ref, key).getBoundingClientRect().top` of the current vs next/prev item, `getKeyBelow`/`getKeyAbove` flipping `getNextKey`/`getPreviousKey` when reversed, and `getKeyPageAbove`/`getKeyPageBelow` inverting their `pageY` math (`itemRect.y - visibleRect.height` for the negative-y column-reverse case) and their `?? getFirstKey()/getLastKey()` fallback. **None of it has a host in our architecture:** (1) **We have no `ListKeyboardDelegate` class** — list/menu navigation is inlined per-hook over **collection order**, not a geometry-driven delegate: `createListBox`/`createGridList` navigate via `findNextEnabledKey` (`collection.getKeyAfter`/`getKeyBefore`), and `createMenu` via `findNextNonDisabledKey` + a **simplified `clientHeight`-accumulation** page model — none uses a `layoutDelegate`/`getItemRect`/`getVisibleRect`. (2) **The bulk (~40 of the 49 lines — the `getKeyPageAbove`/`getKeyPageBelow` reversed math) has no structural equivalent**: `createListBox`/`createGridList` have **no page navigation at all** (no PageUp/PageDown), and `createMenu`'s page-nav accumulates `clientHeight` rather than comparing `getItemRect.y` to `getVisibleRect`, so the negative-y column-reverse `pageY` arithmetic maps onto nothing. (3) **The `isReversed` flip is unreachable**: `createListBox`/`createGridList` receive **no root ref** (consumers call `createListBox(props, state)`; the `_ref` param is unused), so a `getItemElement(ref, key)` geometry read cannot run there, and `getBoundingClientRect()` is an **all-zero no-op in jsdom** regardless (same real-layout-only constraint as T-47's ButtonGroup overflow). (4) **No consumer exposes a reversed/`column-reverse` list/grid/menu layout** anywhere (grep-empty) — the feature's entire purpose is absent, it is nascent even upstream (its own `TODO: still need to see how this works with virtualizer once there is handling for the reverse layout`), and our virtualizer has no reverse-layout handling either (1D scroll-axis decomposition). **Deferred sliver:** if a reversed-layout list consumer is ever added, the minimal faithful realization would be an `isReversed` flip in `createListBox`/`createGridList` ArrowDown/ArrowUp — but that needs cross-layer root-ref threading + `getItemElement`, is jsdom-untestable, and has zero current consumers, so it is **not** ported now. Same disposition family as **T-49**/**T-50**/**T-41 Part B** (absent-by-architecture, not a behavioral miss).
- [x] **T-38** ✔ RAC — **Menu `onAction(key)` → `onAction(key, value)`** (passes `item?.value`). **Was a confirmed gap** (our `createMenu` typed `onAction?: (key: Key) => void`, `createMenuItem` called `data?.onAction?.(key)`); **now ported** (changeset `menu-onaction-value.md`). Upstream `useMenu` types it `(key, value: T)` and threads `props.onAction` into the shared, type-erased `MenuData` (`menu/utils.ts`: `value: any`), where `useMenuItem` `performAction` fires `onAction(key, item?.value)`. **Two-call-site threading** (our architecture splits what upstream routes through a single `performAction`): `createMenuItem`'s press path now calls `data.onAction(key, item?.value)`, and `createMenu`'s keyboard-activation path passes `collection.getItem(focusedKey)?.value` — `AriaMenuProps` is now generic (`<T>`) and `MenuData.onAction` is type-erased (`value: unknown`) like upstream. **Static-vs-dynamic value decision:** RAC `Menu.tsx` auto-sets `MenuItem.value` **only for dynamic collections**; our static-JSX path uses a synthetic item descriptor with no data `value`, so `solidaria-components/Menu` forwards the real data item for dynamic collections and **suppresses `value` to `undefined` for static children** to match. **Inner `select`/`combobox` `onAction: undefined` is N/A by construction** — our `createSelect`/`createComboBox` never forward an `onAction` into an inner listbox/menu, so there is nothing to null out. Tests: `createMenu.test` (value = the data item, incl. a keyboard-Enter case), `solidaria-components/Menu.test` (dynamic = data item, static = `undefined`), `solid-spectrum/ActionMenu.test` (dynamic items + render-fn children = data item, static JSX = `undefined`); 206 menu tests green across the 4 suites.
- [x] **T-39** ✔ RAC — **overlay `getTargetRect` override — ported.** Upstream's `getTargetRect?: (target) => DOMRect | null` flows a `targetRect` override into `getOffset`/`getPosition` so an overlay positions relative to a virtual target (mouse cursor for a context menu, a text selection) rather than the trigger's own box. **Was confirmed absent**; now ported (changeset `overlay-get-target-rect.md`, `@proyecto-viviana/solidaria` + `@proyecto-viviana/solidaria-components` patch). **Four-layer threading:** (1) `calculatePosition` gains a `targetRect?: Offset | null` option (`PositionOpts`) plumbed through `getElementOffset(node, ignoreScale, overrideRect?)` and `getPosition(node, parent, ignoreScale, overrideRect?)` (both `overrideRect || getRect(...)`, including the `fixed`-position branch) into the `childOffset` computation — omitted/`null` falls back to `target.getBoundingClientRect()` exactly as before (`DOMRect` is structurally assignable to our `Offset`). (2) `createOverlayPosition` adds `getTargetRect` to `AriaPositionProps` and forwards `targetRect: props.getTargetRect?.(targetNode)` into the `calculatePosition` call, called against the live target node and — **matching upstream** — **left out of the position-update dependency tracking** (upstream's deps array excludes it; treated as stable). (3) the headless `Popover` adds `getTargetRect` to its prop allowlist (interface + `splitProps` + `createPopover` getter). (4) `createPopover` (extends `Omit<AriaPositionProps, …>`, spreads `{...props}`) and the S2 `Popover` (extends `Omit<HeadlessPopoverProps, …>`, lands it in `rest` → `{...rest}`) **auto-forward** — no edit needed. **S2 Popover un-omit confirmed N/A:** the pinned `@react-spectrum/s2` `PopoverProps` already does **not** omit `getTargetRect`/`isNonModal` (its `CustomPopoverProps` `Pick` re-includes them), and our `PopoverProps extends Omit<…>` never omitted them either — there was no omit to remove. New `calculatePosition.test.ts` (4 tests, the pure-function path): default = measured rect → `position.top === 0` (jsdom zeros), `targetRect:{top:300,…,height:40}` → `340`, `+offset:8` → `348`, `targetRect:null` → `0` — **discriminating** (the default-vs-override `0`/`340` split fails against the pre-port no-`targetRect` function). 4/4 new + 54/54 overlay+Popover regression green; fmt/lint/typecheck clean. T-09 follow-up (our `calculatePosition` was a faithful post-#9343 port; this is the next overlay delta).
- [x] **T-40** ✔ RAC — **`useAutocomplete` CJK/IME + inline nav** (+54). **Parts A+B (IME forward-typing) — ported.** Upstream reads the input type from a **`beforeinput`** listener (so `lastInputType` is set _before_ the input's `onChange` runs) and treats **`insertCompositionText`** / **`insertFromComposition`** like `insertText` → `focusFirstItem()`; other insert/delete/history types → `clearVirtualFocus(true)`. Our `createAutocomplete` listened on **`input`** (too late) and forward-focused only on exact `insertText`, so composing CJK/IME text fell into the **clear** branch and dropped virtual focus mid-composition. Switched the listener to `beforeinput` (`handleInput`→`handleBeforeInput`) and added the two composition types to the forward-typing condition (changeset `autocomplete-ime-forward-typing.md`, `@proyecto-viviana/solidaria` patch). New `createAutocomplete.test.tsx` "forward-typing virtual focus (IME-aware)" block — 4 tests dispatching a real `beforeinput`+`input` pair and asserting the `autocomplete:focus`/`autocomplete:clearfocus` CustomEvent: `insertText` and the two composition types → `focus` (`detail.focusStrategy === "first"`), `deleteContentBackward` → `clearfocus`; the **2 composition cases confirmed discriminating** (fail under the old exact-`insertText` condition → clear branch). 16/16 autocomplete suite + lint/typecheck/guard clean. **Parts C+D (deferred `autoFocusOnMount` + Arrow inline-navigation) — folded into T-58.** `focusFirstItem` fires the focus event immediately rather than deferring via `setAutoFocusOnMount(true)` when the collection isn't mounted yet (needs `collectionProps.autoFocus` threading + collection-consumer support), and our ArrowLeft/Right path clears virtual focus unconditionally instead of upstream's dispatch-to-focused-item-then-clear inline-nav semantics; both want their own pass — filed as **T-58**.
- [x] **T-41** ✔ RAC — **DnD type/modality refinements** (`dnd/utils.ts` +31). **Part A `DragTypes.has()` — ported.** Our `DragTypesImpl.has` did only an exact MIME match (plus an _any-symbol_ → `GENERIC_TYPE` check); upstream's `has(type: DragType | DragType[])` also accepts an **array** (`some`), the **`*/*`** match-any wildcard, and **`type/*`** prefix wildcards (`image/*` accepts `image/png`), and pins the symbol check to **`DIRECTORY_DRAG_TYPE`** specifically. Without this, `acceptedDragTypes: ['image/*']` never matched a dragged `image/png` over the DataTransfer-backed path. Ported the full upstream algorithm and widened the public contract in `solid-stately` (new `DragType = string | symbol` alias + `has(type: DragType | DragType[])`, exported from the `dnd/` + root barrels, with the `.d.ts` mirrors and the keyboard-mode shim annotation moved to match) (changeset `drag-types-wildcard-array-matching.md`, `@proyecto-viviana/solid-stately` + `@proyecto-viviana/solidaria` patch). New `DragTypesImpl.test.ts`: exact, `type/*`, `*/*`, array, directory-symbol-only (the tightening), and Safari-`Files` cases — 6 tests, **4 confirmed discriminating** (fail on the old exact-only impl; the 2 unchanged-behavior cases stay green). **Part B `mapModality` coarse-pointer — N/A.** Upstream switches `mapModality`'s touch test from `'ontouchstart' in window` → `matchMedia('(pointer: coarse)')`; we have **no** `mapModality`/`useDragModality`/`getDragModality`/`ontouchstart` anywhere (grep-empty) — our DnD is the simplified pointer-only path with no drag-modality mapping (same family as T-44 #4's absent `DragManager`), so there is nothing to convert. Also confirmed the **keyboard** `getDropOperation` path stays a raw `Set<string>` delegate (no wildcards) exactly as upstream (`useDroppableCollection` passes a `Set<string>`), so the inline `getKeyboardDragTypes` shim keeps its exact-match semantics. 32/32 dnd suite + typecheck/lint/guard clean.
- [x] **T-42** ✔ RAC — **`useCalendarYearPicker` maxValue off-by-one** (+8): `Math.ceil(v/2)-1`, `visibleYears-1`, loop `<= 0` so the year window **includes** the `maxValue` year. Direct T-25 follow-up (our `createCalendarYearPicker`). **Done:** the off-by-one math was **already faithful** in the T-25 port (`1fb52f6e` blame confirms `Math.ceil(visibleYears / 2) - 1`, both `visibleYears - 1` clamps, and the inclusive `<= maxDate` loop were correct from the start — we ported against the post-fix upstream). The reconciliation surfaced one remaining divergence in the same hook: our `?? 20` default vs upstream's `props.visibleYears || 20`, which differ only for a falsy `visibleYears` (`0`/`NaN`) — aligned to `|| 20` (changeset `calendar-year-picker-visible-years-default.md`, `@proyecto-viviana/solidaria` patch). Locked the full upstream oracle (`Calendar.test.js` "supports minValue and maxValue") into `CalendarPicker.test.tsx`: maxValue clamp (already present), **minValue clamp**, **both-bounds → 7 inclusive years** (the headline off-by-one guard), **`visibleYears: 1` → only the focused year**, and **`visibleYears: 0` → default 20**. 10/10 picker + 119 calendar tests green; lint/typecheck clean.
- [x] **T-43** ✔ RAC — **detached-node / getter-only-focus crash fixes** (release-noted). **`useFocusVisible.ts` focus-patch (+21) — ported.** Our `createInteractionModality` `setupGlobalFocusEvents`/`tearDownWindowFocusTracking` patched `HTMLElement.prototype.focus` with **plain assignment wrapped in `try`/`catch`** plus a `canOverride` flag; when `focus` is a getter-only accessor (e.g. a test library's `setup()` instrumentation) the assignment throws → our `catch` swallowed it and **silently skipped the patch** (`canOverride=false`), degrading programmatic-focus modality tracking. Now uses `Reflect.defineProperty({configurable, writable, value})` for both setup and teardown (mirroring upstream), which installs the wrapper without throwing and returns `false` (not throws) in the pathological non-configurable case — so the `canOverride` bookkeeping is gone (changeset `interaction-modality-focus-define-property.md`). Test (`createInteractionModality.test.ts`, isolated fake window so the real prototype/listeners are untouched): a getter-only `focus` is patched as a data property and the wrapper delegates to the original; teardown restores it (confirmed discriminating — plain assignment fails it). **`isElementVisible.ts` (+2) + `isFocusable.ts` isInert (+3) — N/A here, folded into T-57.** The +2/+3 are detached-node `getOwnerWindow` refinements on top of upstream's full `isElementVisible`/`isInert`; our `utils/dom.ts` `isFocusable` is a simplified tagName/tabindex check with **no** `isElementVisible` and **no** `isInert`, so there is nothing to refine. The larger gap (porting the full visibility/inert focusable utils, which is where these refinements belong) is filed as **T-57**.
- [x] **T-44** ✔ RAC — **small behavioral fixes bundle** (4 sub-fixes, each reconciled against our equivalent). **#1 `createEventHandler.ts` (+5) — ported.** Upstream makes `continuePropagation()` also forward to a parent-wrapped event; our `createKeyboard` `createEventHandler` mutates the event via `Object.assign` (rather than upstream's object spread), so the inner wrapper was overwriting any parent `continuePropagation`. Fix captures the prior `continuePropagation` before overwriting and `.call(e)`s it from the new one (changeset `keyboard-continue-propagation-forwarding.md`). Tests in `createKeyboard.test.tsx`: forwards to a parent-wrapped event when the inner handler continues; leaves the parent untouched when it doesn't (both confirmed discriminating — fail without the fix). **#2 `useFormValidation.ts` (+7) — ported.** Native-mode effect now guards `'setCustomValidity' in input` so a ref pointed at a non-form element (e.g. a custom element) skips rather than throws (changeset `form-validation-non-element-ref-guard.md`). Test in `createFormValidation.test.tsx`: a `<div>` ref no longer throws (confirmed discriminating). **#3 `TabsKeyboardDelegate.ts` (+4) — N/A by construction.** Upstream adds `key !== startKey` to its `do...while` loops to break the all-disabled infinite loop; our `createTabs` `getNextKey`/`getPreviousKey` use **bounded** `for (let i = 1; i <= keys.length; i++)` loops over a materialized key array and return `null` after exhaustion — structurally immune, and the caller already guards `if (nextKey !== null)`. **#4 `DragManager.ts` (+5) — N/A, not ported.** Upstream's `onBlur` null-guards `activateButton`; our `createDrag` has no keyboard `DragManager` (it's the simplified pointer-only path), so there is nothing to guard. Two src files patched (both `@proyecto-viviana/solidaria`); 34/34 across the two test suites green; lint/typecheck clean.
- [x] **T-45** ✅ S2 — **CenterBaseline public export + JSDoc — surfaced.** Upstream promotes `CenterBaseline` to a documented public export: `@react-spectrum/s2`'s `exports/index.ts` re-exports `{CenterBaseline}` _and_ a dedicated subpath `exports/CenterBaseline.ts` (`@react-spectrum/s2/CenterBaseline`, the path named in the component's JSDoc example). We already had it internally (`solid-spectrum/src/icon/center-baseline.tsx`, consumed by radio/calendar/color/searchfield/picker, given an optional `id` in T-32) but **un-exported** — no barrel re-export anywhere (grep-empty). Mirrored the full public surface the way our tree mirrors every other S2 component: new `src/CenterBaseline.ts` subpath entry (re-exports `CenterBaseline` + `CenterBaselineProps`), added to the `vite.config.ts` entry list and the `package.json` `exports` map (`./CenterBaseline` → `dist/CenterBaseline.{js,jsx,d.ts}`), plus a root-barrel `export { CenterBaseline }` / `export type { CenterBaselineProps }`. The internal `centerBaseline` factory + `centerBaselineBefore` helper stay private, matching upstream's public surface (it exports only the component). Build confirmed it emits `dist/CenterBaseline.{js,jsx,d.ts}`; typecheck/lint clean (changeset `center-baseline-public-export.md`, `@proyecto-viviana/solid-spectrum` **minor** — additive public API).
- [x] **T-46** ◑ S2 — **TableView focus-ring + divider re-sync** (the big +112) — **divider-transparency delta ported; row-`borderBottom` N/A; focus-ring offset split to T-59.** Three parts, reconciled against our real-`<table>` DOM (upstream's is a virtualizer over `div`s):
  - **Divider transparent under `isSelected` — ported (the behavioral delta).** Upstream's row divider `borderColor` now goes `transparent` on **both** `isSelected` _and_ `isNextSelected` (`TableView.tsx` ~2049); our box-shadow divider only suppressed on `isNextSelected`, so a **lone/bottom-of-block selected row** still painted a stray gray `inset 0 -1px` line _under_ its rounded blue highlight-block border. Added the `isSelected` suppression branch to the `tableRow` `boxShadow` (`packages/solid-spectrum/src/table/index.tsx`) so the divider collapses whenever the row itself or the row below is selected (the block border paints that edge). **Discrimination probe** (isolated-selected row, `git stash` the source): exactly **two box-shadow atomic tokens** flip (the static `_n…13` class + its `--borderColorGray` dynamic var) and nothing else, confirming the new branch is reachable and surgical. 27/27 table+editablecell suites green; fmt/lint/typecheck clean (changeset `table-divider-transparent-on-selected.md`, `@proyecto-viviana/solid-spectrum` patch).
  - **Row dividers `box-shadow` → `borderBottom` — N/A by construction.** Upstream moves the divider to a row `borderBottom`, but our table renders a real `<table>` with `border-collapse: separate`, under which CSS **ignores borders set on `<tr>`** — so upstream's row `borderBottom` has **no** real-DOM equivalent. Our box-shadow-on-`<tr>` (highlight) + cell-`<td>`-`borderBottom` (checkbox) is the faithful realization of the same visual; the `table-highlight-selection-block-border.md` approach stays, now extended with the `isSelected` transparency above. (Not a "re-sync to the borderBottom model" — that model is unavailable to us.)
  - **`CellFocusRing` `top: var(--topFocusRing, 0)` — split to T-59 (deferred).** The 1px top-extension that overlaps the divider above can't be expressed by our element-`outline` focus ring (`focusRing()` paints a uniform outline; an asymmetric top offset needs an absolutely-positioned ring div). Re-architecting the table cell focus ring to a custom-prop-driven inset div is its own pass with visual-regression risk → **T-59**. T-31 follow-up.
- [x] **T-47** ✔ S2 — **ButtonGroup overflow.** `maxWidth: 'full'` + overflow detection (collapse to vertical when the group exceeds its container). **The overflow machinery was already ported** — `packages/solid-spectrum/src/buttongroup/index.tsx` mirrors upstream faithfully: the two-pass force-horizontal-then-measure (our `setHasOverflow(false)` + `requestAnimationFrame(measureOverflow)` realizes upstream's `useValueEffect` generator `yield false; yield computeHasOverflow()`), the exact overflow predicate (`child.offsetLeft < 0 || child.offsetLeft + child.offsetWidth > offsetWidth + 1`, the `+1` rounding slack), the `ResizeObserver` on `parentElement`, the internal remeasure on orientation/size/children, the `effectiveOrientation` switch (`orientation === 'vertical' || hasOverflow ? 'vertical' : 'horizontal'`), the `isHidden → null`, and the `{styles: flexShrink:0, size, isDisabled}` button context. **The one real gap:** our `s2ButtonGroup` style was **missing `maxWidth: 'full'`** (upstream `ButtonGroup.tsx:68`) — without the width cap an `inline-flex` group grows to fit its buttons, so no child ever extends past `maxX` and the overflow→vertical switch was dead code. Added `maxWidth: 'full'` in the exact upstream position (after `position: 'relative'`) in `packages/solid-spectrum/src/button/s2-action-button-styles.ts`. **Discrimination probe** (plain ButtonGroup, `git stash` the style): exactly **one new style atomic** (`La13` = `max-width: 100%`) + its dynamic-hash token flip, nothing else. **Behavioral overflow can't be exercised in our jsdom harness** — upstream's own test is a `.browser.test.tsx` (`getComputedStyle().flexDirection` / real `offsetWidth`); jsdom returns 0 for every box metric and loads no macro CSS, so the row→column switch is real-layout-only territory (we have no browser-mode vitest project). Relied on the faithful port + probe + 123/123 across the 7 ButtonGroup-rendering suites; fmt/lint/typecheck clean (changeset `button-group-max-width-overflow.md`, `@proyecto-viviana/solid-spectrum` patch).
- [x] **T-48** ✔ S2 — **Disclosure styling** (+171, largely cosmetic): styling refactor + `StylesPropWithFont` `styles?` on `DisclosureTitle`. **The style objects were already at full parity** (`headingStyles`/`buttonStyles`/`chevronStyles` mirror upstream values); the one real behavioral delta was **where `DisclosureTitle.styles` is applied**. Upstream routes it to the trigger `Button` as a `style()` override gated by `getAllowedOverrides({font: true})` (`Disclosure.tsx:325` — `buttonStyles({...renderProps, size, density, isQuiet}, styles)`), with the prop typed `StylesPropWithFont`. Our port merged it onto the **outer `<Heading>` wrapper** via `mergeStyles(headingStyles, local.styles)` — which (a) can't affect the title's own font (the font lives on the inner button) so a font override silently no-op'd, and (b) is unfiltered, so any non-font override leaked through. **Fix** (`packages/solid-spectrum/src/disclosure/index.tsx`): pass `local.styles` as the runtime override to `buttonStyles({...}, local.styles)`, define `buttonStyles` with `getAllowedOverrides({font: true})`, drop `local.styles` from the heading (now just `headingStyles`), and narrow `DisclosureTitleProps.styles` to `StylesPropWithFont` + the upstream JSDoc. Added the supporting `font` option to `getAllowedOverrides` plus `fontProperties`/`StylesPropWithFont` exports in `s2-internal/style-utils.ts`. **Discrimination probe** (override = `style({fontWeight:'black', paddingTop:8})`, with/without via `git stash`): `fontWeight:black` (`xf13`) and `paddingTop:8` (`Tt13`) — **old** heading carried both unfiltered + button carried neither; **new** heading carries neither, button carries `xf13` (overriding the base `xd13` fontWeight:bold) and **drops** the disallowed `Tt13` (base `pt13` paddingTop retained). Proves both the heading→button routing and the font allowlist are live and surgical. Disclosure 5/5; fmt/lint/typecheck/guard clean (changeset `disclosure-title-styles-font-override.md`, `@proyecto-viviana/solid-spectrum` patch).
- [x] **T-49** ➖ S2 — **Menu un-omit `dependencies`** (+9) — **N/A by construction, confirmed.** Upstream re-exposes the `dependencies` array prop (RAC's collection-invalidation deps array). In the pinned upstream, `@react-spectrum/s2` `MenuProps` `Omit`s only `'children' | 'style' | 'className' | 'render' | 'renderEmptyState' | keyof GlobalDOMAttributes` from `AriaMenuProps` — `dependencies` is already _not_ omitted. We never had the omit because we never had the prop: our collections invalidate **reactively via Solid signals** (any signal read inside an item render is auto-tracked), so there is no manual `dependencies` array to surface — `dependencies` is grep-empty across our `menu`/`solidaria`. Nothing else in the un-omit applies.
- [x] **T-50** ➖ S2 — **Tabs stable `contentId`** (+11) — **N/A in Solid's no-rerender model, confirmed.** Upstream's S2 `Tab` (`@react-spectrum/s2/src/Tabs.tsx`) derives `contentId = \`${baseId ?? fallbackId}-content-${props.id ?? fallbackId}\``from a shared`baseId`(on`InternalTabsContext`), used as the text `id`and in`aria-labelledby`for the`labelBehavior === 'hide'`case; the comment states it "Prevents infinite loop when rendering across multiple CollectionBuilder portals" — a React reconciliation artifact where`useId()`re-derives a changing id across the portal double-render. Our`Tab` (`solid-spectrum/src/tabs/index.tsx`) uses `const contentId = createUniqueId()`**once** in the component body (Solid runs the body once → stable for the instance's lifetime), and both the text`id={contentId}`and`aria-labelledby={…contentId…}`read the **same closure variable**, so they are internally consistent. With no re-render and no portal double-render producing a changing id, neither the infinite loop nor an aria mismatch can occur; the upstream`baseId`-derivation exists purely to be deterministic across React's double render, which we don't have. (The aria-hook id path is separately faithful: our `getTabListId`WeakMap +`${baseId}-tab/tabpanel-${key}`mirrors upstream`generateId`.)

## Cross-train behavioral tickets (source-level sweep)

These come from reading upstream source hook-by-hook during the **source-level
behavioral sweep** (not from a single release diff), so they aren't tied to a train.
They were previously the "open items (deferred)" checklist in
[upstream-sync.md](./upstream-sync.md); that doc now keeps the per-behavior
**evidence** while this is the single actionable list. (The sweep's typeahead
capture-phase item folded into **T-35** — the 1.19 `useTypeSelect` refactor is that
work — so it isn't repeated here.)

- [ ] **T-51** ⛔ RAC — **`replace`-mode action model (architectural epic).** Wire upstream's single-click-selects / double-click-acts split so the row action becomes the _secondary_ action under `selectionBehavior: 'replace'` with selection enabled. Route `gridlist/createGridListItem.ts`, `tree/createTreeItem.ts`, and `table/createTableRow.ts` activation through a **press-based path** (not raw pointer events), thread `hasPrimaryAction` / `hasSecondaryAction` + `pointerType`, and add the `onDoubleClick` secondary action and the touch **long-press → `setSelectionBehavior('toggle')`** switch. Evidence: the `selectionBehavior: 'replace'` section in upstream-sync.md ("Not yet ported"). **High risk / cross-hook — scope and validate on its own before starting.** ⇄ shares the item-hook press-path migration with **T-34** (`keyboardNavigationBehavior`); sequence the two together, and bundle **T-52** (shared plumbing). **Scoped 2026-06-20 in `press-path-epic.md`** (Phases 0–2 there: T-52 foundation → build `createSelectableItem` → migrate the three item hooks; T-56 falls out in Phase 2).
- [x] **T-52** ✔ RAC — **`select()` multiple-mode fidelity.** Resolved 2026-06-20 by restoring upstream's two-layer split (`press-path-epic.md` Phase 0). `createSelectionState.select()` is thinned to the `SelectionManager.select` shape — it now consults `pointerType` (touch/virtual ⇒ `toggleSelection`) + `selectionBehavior` only, with a new `SelectionPressEvent` type; the platform-aware modifier decision moved up to solidaria's aria-layer `selectItem` (`isCtrlKeyPressed` = Command on macOS / Control elsewhere), which `createMenu`'s keyboard activation now routes through. Both gaps (a) platform-aware ctrl and (b) `pointerType` handling are closed.
- [x] **T-53** ✔ RAC — **`escapeKeyBehavior` prop — ported.** Added `escapeKeyBehavior: 'clearSelection' | 'none'` (default `'clearSelection'`) to `createListBox`, mirroring `useSelectableCollection` (the prop default lives in the hook; the Escape case now gates on `(escapeKeyBehavior ?? 'clearSelection') === 'clearSelection'` on top of the existing `!disallowEmptySelection() && !isEmpty()` guard). `'none'` leaves Escape entirely untouched — no clear, no `preventDefault`/`stopPropagation` — so an enclosing overlay can handle it. The headless `ListBox` component forwards it via its props rest-spread (`escapeKeyBehavior` isn't in the `local`/`stateProps` `splitProps` groups, so it lands in `ariaProps` → `createListBox`), and `filterDOMProps` keeps it off the DOM; **upstream S2 doesn't surface it at the styled layer, so `solid-spectrum` doesn't either** (mirror, don't invent). Changeset `listbox-escape-key-behavior.md`. Two discriminating tests in `createListBox.test.tsx` ('none' neither clears nor swallows with a live selection; explicit 'clearSelection' clears + swallows) — both fail under the old always-default path. 65/65 createListBox suite + lint/typecheck clean. Evidence also in upstream-sync.md "Escape key (ListBox)" section.
- [x] **T-54** ◑ RAC — **PageUp/PageDown navigation — Menu gated (Part b); ListBox page-nav deferred (Part a, structural).** Upstream `useSelectableCollection` delegates Page keys to `delegate.getKeyPage{Above,Below}` (computed from item heights + the scroll container's `clientHeight`) and only `preventDefault`s when `focusedKey != null` **and** the delegate returns a target. **Part b — `createMenu` gated (ported).** Our menu already pages via its own `clientHeight` traversal but `preventDefault`d **unconditionally**, so with nothing focused it swallowed the Page key while doing nothing instead of letting it bubble (e.g. to scroll an enclosing region) — the same gap its arrow/Home keys were already fixed for. The PageDown/PageUp cases now read `state.focusedKey()` first and `break` before `preventDefault` when it's `null` (changeset `menu-page-key-focus-gate.md`, `@proyecto-viviana/solidaria` patch). **The "found-target" half of upstream's gate is N/A by construction:** upstream's `getKeyPage{Above,Below}` returns `?? getLastKey()/getFirstKey()`, so for a non-empty collection it is **always** non-null whenever `focusedKey != null` — i.e. the focused-key gate _is_ the substantive condition (the only `nextKey == null` case is an empty collection, which can't have a focused key), so the existing focused-key paging is left unchanged. 3 tests in `createMenu.test.tsx` "page navigation" block: PageDown with a focused key still `preventDefault`s + moves; PageDown/PageUp with **nothing** focused neither move nor `preventDefault` — the **2 no-focus tests confirmed discriminating** (fail against the old unconditional `preventDefault`). 47/47 menu + 131 menu-component/ActionMenu regression green; lint/typecheck clean. **Part a — `createListBox`/`createGridList` page navigation deferred (structural).** They handle **neither** Page key, and a faithful port needs the per-item geometry upstream's `DOMLayoutDelegate.getItemRect`/`getVisibleRect` reads — which the headless ListBox hook can't measure: it receives **no root ref** (`createListBox(props, state)`; the `_ref` param is unused, same finding as T-37), and `getBoundingClientRect` is a jsdom no-op. Adding a Menu-style `clientHeight` pager would be inventing a divergent implementation rather than porting upstream's delegate model, so this waits on the shared real `getKeyPage{Above,Below}`/layout-delegate work (same virtualizer-class family as T-37 Part a). Tracked open here.
- [x] **T-55** ✔ S2 — **modal-Popover nested-overlay focus containment — ported.** A _modal_ outer `Popover`'s `FocusScope contain` collapsed a nested menu overlay: the menu is portaled **out of** the outer scope's DOM subtree, so the outer scope's `focusin` handler saw the menu gain focus, judged it "escaped," and yanked focus back — tearing the overlay down before it rendered (`role="menu"` count stayed 0); the menu only opened cleanly inside a **non-modal** outer Popover. **Root cause:** our `FocusScope` containment tested only `isElementInScope` (DOM `contains`), which can't see a portaled descendant scope. **Fix (faithful to `@react-aria/focus`'s `focusScopeTree`):** added a module-level parent/child scope tree keyed by each scope's (stable) scope-elements accessor, with the parent resolved via `FocusScopeContext` — **Solid propagates context across `<Portal>`**, so the inner menu's `FocusScope` registers as a child of the outer popover's even though their DOM lives apart. Each scope `addTreeNode`s on mount / `removeTreeNode`s on cleanup (re-parenting orphaned children). The containment `onFocusIn` now consults a ported `isElementInChildScope` (subtree traverse + `data-react-aria-top-layer` escape hatch) and, when focus lands in a **descendant** scope, tracks it without pulling focus back. True escapes (focus to an unrelated element) restore exactly as before; the Tab handler already bailed for portaled children (DOM-`contains` false) so it needed no change. Changeset `focus-scope-descendant-containment.md` (`@proyecto-viviana/solidaria` patch). New discriminating test `packages/solid-spectrum/test/Menu.test.tsx` "keeps a nested menu open inside a modal Popover" (modal outer `role="dialog"` → open menu → `role="menu"` present) — **confirmed discriminating** (fails against the pre-fix `FocusScope`: menu never renders); the pre-existing non-modal "renders in its own popover" test's now-stale follow-up note was repointed to it. 86 FocusScope + 296 overlay-family (Dialog/Modal/Select/ComboBox/Tooltip across all 3 layers) green; lint/typecheck clean. **Deferred (not needed for this case, noted in the changeset):** upstream's `activeScope`/`shouldContainFocus` arbitration between multiple nested _contained_ scopes and the tree-aware focus-restoration (`useRestoreFocus` clone/`nodeToRestore`) — both larger, regression-sensitive ports beyond T-55's gap.
- [ ] **T-56** 🔍 RAC — **`disabledBehavior: 'selection'` onAction on the _pointer_ path (minor, verified).** A `disabledBehavior: 'selection'` item is selection-blocked but still actionable, so upstream fires `onAction` on **both** click and keyboard (`useSelectableItem.ts`: `isDisabled = isDisabled || manager.isDisabled(key)` is all-gated → `allowsActions` true → the item's own `usePress`/`performAction` runs for pointer _and_ keyboard). Our `disabled-selection-onaction` fix only patched the **keyboard/container** path (`createMenu` `onKeyDown` is navigation-disabled-gated), but `createMenuItem`'s `createPress` is gated on the **raw** disabled check, so it stays off for selection-disabled items — meaning a **pointer click** on such an item fires `onAction` **0 times** (empirically confirmed) vs upstream's once. Faithful fix: gate the item-hook _action_ (press) on the all-resolved check while keeping _selection_ on the raw check (`state.select` already self-guards), which also retires the now-redundant container compensation path. **Same raw-pointer → press-based item-hook migration as T-34/T-51** — audit the sibling collections (`createGridListItem`/`createTreeItem`/`createListBoxOption`, all touched by `disabled-selection-onaction`) for the identical click gap and **sequence with that cluster**. Surfaced while answering the "two call sites" question against `createMenuItem` vs `useSelectableItem`.
- [x] **T-58** ✔ RAC — **`useAutocomplete` deferred `autoFocusOnMount` + Arrow inline-navigation (T-40 Parts C+D) — ported (hook level).** Reconciled against the **vendored** `react-aria/src/autocomplete/useAutocomplete.ts` (the autocomplete rc.8 paired with the RAC 1.19.0 pin; **not** the stale `apps/comparison/node_modules` rc.5, which lacks both behaviors). **C — deferred first-item focus:** added a `autoFocusOnMount` signal; when `focusFirstItem` runs but `collectionRef()` is null it sets the flag and returns (instead of dispatching an `autocomplete:focus` CustomEvent into a not-yet-rendered collection), and `collectionProps.autoFocus` is a getter reading `autoFocusOnMount() ? 'first' : false`; `clearVirtualFocus` resets the flag, so editing/deleting clears the deferred request. **D — Arrow inline-navigation:** ArrowLeft/ArrowRight with `focusedNodeId == null` now `stopPropagation()` + return (text cursor moves, no clear, no dispatch); with virtual focus it falls through to the shared forward-dispatch block, which **dispatches the key to the focused item first** (capturing `shouldPerformDefaultAction = item.dispatchEvent(...)`) and only then calls `clearVirtualFocus()` — and **only if the item didn't cancel the event** (an item that consumes the arrow, e.g. an expandable row collapsing, retains virtual focus). Previously Left/Right cleared virtual focus unconditionally and returned before any dispatch. Changeset `autocomplete-autofocus-arrow-nav.md`. `createAutocomplete.test.tsx` +6 tests (22 total): Part C — deferred `autoFocus==='first'` when unmounted, reset on edit/delete, and the **mounted** path still fires `autocomplete:focus` with `autoFocus===false`; Part D — no-focus arrow doesn't clear and isn't forwarded, focused arrow dispatches to the item **before** the clear (`['item:ArrowRight','clear']` order), and an item that `preventDefault`s retains focus — all discriminating against the old behavior. Full solidaria suite green (3513 passed / 8 skipped); lint/typecheck clean. **Unwired-bridge caveat:** this is a hook-level port validated by the emitted `collectionProps.autoFocus` and the focus/clear CustomEvents. The autocomplete↔collection bridge is **unwired repo-wide** — no ListBox/Menu consumes `AutocompleteCollectionContext`, honors `collectionProps.autoFocus` on mount, or listens for `autocomplete:focus`/`autocomplete:clearfocus`. Making a collection consumer actually act on these is the **autocomplete consumer-wiring port already tracked** in `upstream-sync.md` (Bucket D, the "autocomplete" entry: "our SearchField/Menu don't consume those contexts yet… closing it is a consumer-wiring port") — a separate, larger architectural effort, distinct from T-51's `replace`-mode action model.
- [x] **T-57** ✔ RAC — **full `isFocusable`/`isElementVisible`/`isTabbable` port (visibility + inert + detached-node window) — ported.** Our `utils/dom.ts` `isFocusable` was a simplified tagName/tabindex/contenteditable check with **no** visibility filtering and **no** `inert` filtering, and we had **no** `isElementVisible`/`isTabbable` at all. Now mirrors `@react-aria/utils@3.34.1` (the version paired with the RAC 1.19.0 pin — confirmed against the **vendored** monorepo, not the stale `apps/comparison/node_modules` 3.33.0): `isFocusable(element, {skipVisibilityCheck?})` = `element.matches(FOCUSABLE_ELEMENT_SELECTOR)` && `!isInert(element)` && `(skipVisibilityCheck || isElementVisible(element))` (the `skipVisibilityCheck` bypass is what the press-path `preventFocus` ancestor walk uses); `isTabbable` = the same against `TABBABLE_ELEMENT_SELECTOR` (excludes `tabindex="-1"`); `isElementVisible` uses `checkVisibility({visibilityProperty})` when supported, else the `isStyleVisible`/`isAttributeVisible` walk (honoring `hidden`, `data-react-aria-prevent-focus`, `<details>`/`<summary>`); `isInert` walks ancestors for `node.inert`. All owner-window lookups go through `getOwnerWindow` (global-window fallback) so the helpers stay robust on detached documents (`defaultView === null`). The selector strings are reconstructed by the same `join(...)` so the focusable-vs-tabbable split is byte-identical. **T-43 #1/#2 landed here:** the `getOwnerWindow(node).HTMLElement`/`SVGElement` `instanceof` and `getOwnerWindow` `getComputedStyle` refinements are baked into the ported `isStyleVisible`/`isInert`. The duplicated local `isTabbable` copies in `FocusScope` and `createToolbar` now collapse onto the shared util, and the barrel re-exports `isTabbable`/`isElementVisible`. **Solid deviation (documented):** `isStyleVisible`/`isInert`'s realm guard also accepts the **global** realm's `HTMLElement`/`SVGElement`, because Solid's `Portal` clones templates with the global document — a node adopted into an iframe keeps its main-realm prototype even though `getOwnerWindow` resolves to the iframe window, so upstream's single owner-window `instanceof` would reject real cross-document elements (identical to upstream in the same-realm case). Changeset `focusable-utils-visibility-inert.md`. New `isFocusable.test.ts` (18 tests: focusable selector, display:none/visibility:hidden/`hidden`/ancestor/`data-react-aria-prevent-focus` rejection, `inert` subtree, `skipVisibilityCheck` bypass, detached-document robustness, focusable-vs-tabbable for `tabindex="-1"`, the cross-realm iframe case) — the visibility/inert cases are discriminating against the old check. Full solidaria + solidaria-components suites green (3507 tests, incl. FocusScope/FocusScopeOwnerDocument/createToolbar/FocusManagement); lint/typecheck clean.
- [ ] **T-59** 🔍 S2 — **TableView cell focus-ring `--topFocusRing` offset (focus-ring re-architecture).** Upstream's `CellFocusRing` is an absolutely-positioned ring `div` whose `top: var(--topFocusRing, 0)` extends to `-1px` (overlapping the divider above) for all but the first row, so a focused cell's ring sits flush against the row boundary. Our table cell focus ring is an element `outline` via `focusRing()` (`packages/solid-spectrum/src/table/index.tsx` `tableCell`, `outlineOffset: -2`), which paints a **uniform** outline — an asymmetric top extension can't be expressed by `outline`. Porting needs replacing the cell outline with a custom-prop-driven (`--topFocusRing`/`--bottomPosition`) absolutely-positioned ring presentation div (mirroring upstream's `cellFocus` + `focusIndicator` raw CSS), which changes the focus-ring rendering model table-wide → visual-regression risk; scope and validate on its own. **Split from T-46** (T-46 landed the divider-transparency delta; this is the focus-ring half).

---

## Confirmed gaps (the actionable shortlist)

From the first pass, these are **confirmed absent** and are the clearest port
candidates (roughly highest-value first):

1. ~~**T-29 SliderFill**~~ — ✔ **done** (changeset `slider-fill-component.md`); first port landed.
2. ~~**T-25 CalendarMonthPicker / CalendarYearPicker**~~ — ✔ **done** (changeset `calendar-month-year-picker.md`); ~~**T-23 Calendar multi-select**~~ — ✔ **done** (changeset `calendar-multiple-selection.md`); ~~**T-26 RangeCalendar firstAvailableDate**~~ — ✔ **done** (changeset `rangecalendar-available-range.md`). **Calendar 1.18 cluster complete.**
3. ~~**T-16 Table expandable rows**~~ (`treeColumn`) — ✔ **done** (changeset `table-expandable-rows.md`); the `UNSTABLE_` tree-grid stack ported headless-first through solid-stately (`createTreeGridState` + `TableCollection` tree mode) → solidaria (`createTableRow` tree ARIA + press-based chevron + arrow expand/collapse) → solidaria-components (`Table` chevron slot + `hasChildItems`/`isTreeColumn` render props). S2-styled chevron/indentation **now done** (changeset `table-tree-column-chevron.md`): styled `TableCell` auto-injects the headless chevron Button (flex wrapper inside the `<td>`, `rotate` shorthand on expand) + tree-column depth indentation (`--treeColumnPadding` 36/16 + `--indent` 16).
4. ~~**T-17/T-21 Virtualizer window scrolling**~~ — ✔ **done** (changeset `virtualizer-window-scrolling.md`); `window ∩ element` viewport + a document-level capturing scroll listener, default-on via the new `allowsWindowScrolling` prop (opt-out to element-only). `isScrolling` pointer-events **now done** (changeset `virtualizer-isscrolling-pointer-events.md`): `pointer-events: none` on the content during scroll via a new inner content wrapper; the DOM-reorder-deferral and relayout scroll write-back halves are N/A to our slice-render + browser-owned-scroll model (no `scrollToItem`/`scrollToRect` to port).
5. ~~**T-04 DateField constrain-on-blur**~~ — ✔ **done** (changeset `datefield-constrain-on-blur.md`); ported the `IncompleteDate` display model so edits constrain on blur, and removed the non-upstream min/max snap (out-of-range now stays as-typed + flags validation).

**Pass-1 shortlist (1–5) fully cleared.** New confirmed gaps now come from the
depth-verify of the 🔍/✅ tickets (oldest-first), highest-value first:

6. **T-02 inline TableView cell editing** (`EditableCell` + `renderEditing`) — **✔ done** (changesets `table-editable-cell.md`, `table-row-modal-editor-support.md`): S2-styled `EditableCell` with the desktop-popover / touch-dialog split via a new `createMediaQuery` primitive, plus the `TableRow` subtree-recreation fix and `Popover` `AriaLabelingProps` widening that make an in-cell modal editor viable.
7. **S2 Toast animations** (not a numbered release-notes ticket — found while verifying T-01) — **✔ done** (changeset `toast-view-transitions.md`): ported the View Transitions API enter/exit/restack animation. A generic `wrapUpdate(fn, action)` hook on `solid-stately`'s `ToastQueue` (mirroring `@react-stately/toast`) wraps only the subscriber fan-out; `solid-spectrum`'s `ToastContainer` installs a wrapper that runs each queue mutation / expand / collapse inside `document.startViewTransition()` (mutation applied via Solid `batch`), tags `<html>` with `toast-<action>`, names each toast (`toast-<key>`, prefixed for valid CSS idents; per-index suffix on background toasts under reduced motion), and injects the upstream `Toast.module.css` keyframes / `::view-transition-group()` / `html.toast-*` rules at runtime (the same guarded-`<style>` idiom as `createPress`). `prefers-reduced-motion` is tracked with a `PRIVATE_forceReducedMotion` test hook; the API-absent path (SSR/jsdom) runs synchronously. 4 stately + 5 spectrum tests.
8. **T-32 custom field `prefix`** — **✔ done** (changeset `field-prefix.md`): added `prefix?: JSX.Element` to ColorField / ComboBox / NumberField / TextField via a shared `field/prefix.tsx` helper (`FieldPrefix` + `PrefixInputProvider`) that appends a `prefixId` to the input's `aria-labelledby` through a context proxy — no shared `FieldGroup` needed, no-prefix path unchanged. 4 tests.
9. **T-18 horizontal virtualization** — **✔ done** (changeset `virtualizer-horizontal-orientation.md`): gave `VirtualizerLayouts`/`ListLayout` an `orientation` axis (offset along `x`/width when horizontal), taught the `Virtualizer` to window along `scrollLeft` + measured width and expose its resolved `orientation`, and gave `GridList` an `orientation` prop + `data-orientation` (with GridList/ListBox spacers switching to `width`). **Keyboard-navigation half now done too** (changeset `gridlist-horizontal-keyboard-axis.md`): `createGridList`'s arrow handling is orientation- + direction-aware — vertical stack leaves Left/Right inert (Up/Down navigate), horizontal stack promotes Left/Right to the primary axis (Right=next, Left=prev in LTR, RTL-flipped) with Up/Down still working, mirroring upstream `ListKeyboardDelegate`.

10. **T-31 TableView `selectionStyle="highlight"`** — **✔ done** (`table-selection-style-highlight.md`): added `selectionStyle?: 'checkbox' | 'highlight'` to our S2 `TableView`, derived `selectionBehavior` (highlight → `replace`), gated both the select-all column and per-row checkboxes on `selectionStyle === 'checkbox'`, and gave selected rows the blue-tinted highlight background (HCM `Highlight`/`HighlightText`), scoped to the highlight path. Mirrors the Tree (T-20) port. Virtualizer-only border polish **now done too** (`table-highlight-selection-block-border.md`): rounded selection-block border via a `::before` overlay (injected stylesheet) + `isNextSelected`/`isPrevSelected` inner-edge/corner suppression + highlight-mode box-shadow row dividers; the only upstream bit dropped is the overlay `z-index: 3` (sticky-cell layering, N/A for our real-DOM `<table>`).
11. **T-10 scroll-into-view util family** — **✔ done** (`scroll-into-view.md`): ported `@react-aria/utils` `scrollIntoView` / `scrollIntoViewport` (+ `getScrollParents`) and wired `scrollIntoViewport` into the three real-DOM-focus hooks — `createDateSegment`, `createCalendarCell`, and `createTable`'s selectable-collection focus effect — so keyboard focus reveals off-screen items (incl. the 1.16 `scrollMargin` / alignment / scrollbar-width / RTL refinements). **Follow-up now done** (`scroll-into-view-activedescendant.md`): the activedescendant collections (ListBox / Menu) + virtual-focus ComboBox reveal the focused option through a shared `createScrollIntoViewOnFocus` hook (Select already reveals via real `focusSafely` focus — left unwired); only grid / gridlist walker-nav stays deferred (no programmatic row focus / `useGridCell` walker to attach to).
12. **T-22 workflow-icon regen** — **✔ done** (`workflow-icons-regen.md`): copied the 8 missing upstream SVGs into `assets/s2wf-icons` and reran the icon codegen (+ `vp fmt`), bringing the set to 410 with a `s2wfIcons` export for each.

**Minor follow-ups (not full tickets):** **T-06** — **✔ done** (changeset `tree-disabled-forcedcolors.md`): ported upstream's `treeCellGrid` / `expandButton` colors onto our merged `treeViewItem` + description + chevron — disabled content now carries `forcedColors: 'GrayText'`, the row gains `forcedColors: 'ButtonText'` + the highlight `'HighlightText'`, and the chevron defaults to `inherit` (also fixing disabled labels that kept the enabled `neutral-subdued` color). **T-05** — **✔ done** (test-only): a menu-in-popover render test now locks the architecture-immune popover-vs-inline decision (non-modal outer Popover). Writing it surfaced a **new minor follow-up** — a _modal_ outer Popover's `FocusScope contain` collapses a nested _modal_ menu overlay so the menu never renders; making that focus containment descendant-overlay aware (so a menu opened from inside a modal Popover stays open, as upstream allows) is its own item.

**Trains 1–5 complete.** Pass 1, the source-based depth-verify, and web pass 2 have
each run; for **T-01…T-33, no 🔍 tickets remain** — every one is ✔ done,
✅ verified-present, or ➖ n/a, with **no ⛔ gaps**. The standalone S2 Toast-animations
follow-up (item 7; not a numbered release-notes ticket) is **now done** (changeset
`toast-view-transitions.md`), and the **T-06** HCM styling follow-up is **now
done** (changeset `tree-disabled-forcedcolors.md`). The minor **T-05**
menu-in-popover render test is **now done** (test-only, non-modal outer Popover);
writing it surfaced one **new** minor follow-up — making a _modal_ Popover's focus
containment descendant-overlay aware so a nested _modal_ menu can open — which
joins the deferred sub-items called out per ticket.

**T-34…T-55 is the active backlog** — one list now: **Train 6 (T-34…T-50)**, the
RAC 1.18→1.19 / S2 1.4→1.5 release absorb, plus the **cross-train sweep tickets
(T-51…T-55)** consolidated here from upstream-sync.md's old deferred checklist.
**T-38** (`onAction(key, value)`) has landed; the rest pending. Work it like the
earlier trains — the two real port tickets still open are the high-risk epics
**T-34** (`keyboardNavigationBehavior`) and **T-51** (`replace`-mode action model).
The minors have all landed or dispositioned: **T-39** overlay `getTargetRect` and
**T-53** `escapeKeyBehavior` ported; **T-37** column-reverse delegate ➖ N/A (no
`ListKeyboardDelegate` class / no reversed-layout consumer); **T-36**
`UNSTABLE_focusOnEntry` ➖ N/A (experimental private API, no `createSelectableCollection`;
its deps-change half folds into the deferred Bucket D autocomplete bridge); and **T-54**
PageUp/PageDown is now ◑ — `createMenu`'s Page keys gated on a focused key (changeset
`menu-page-key-focus-gate.md`), with `createListBox`/`createGridList` page-nav deferred
structural (needs the per-item layout-delegate geometry the headless hook can't measure,
same family as T-37 Part a). **T-55** (modal-Popover nested-overlay focus containment) is now ✔ — `FocusScope`
containment made descendant-scope aware via a ported `focusScopeTree`
(changeset `focus-scope-descendant-containment.md`), so a menu opens and stays
open inside a modal popover. **T-52** (`select()` multiple-mode fidelity) is now ✔ —
the press-path epic's Phase 0 restored upstream's two-layer onSelect split
(stately `select()` thinned to pointerType + behavior; platform-aware modifier
decision moved to solidaria's aria-layer `selectItem`; changeset
`selection-onselect-layer-split.md`). The remaining 🔍 minor — **T-56**
(pointer-path `onAction`, sequenced with the T-34/T-51 press-path cluster; it
falls out in the epic's Phase 2) — still needs a source-vs-our-code reconcile
before it resolves to ✔/✅/➖; **T-45** (CenterBaseline) is ✅ now surfaced as a public export, and **T-49**/**T-50** resolved ➖ N/A (reactive collections / Solid no-rerender). **Sequencing:**
**T-34** and **T-51** are the two high-risk cross-hook epics and they share one
prerequisite — migrating the gridlist/tree/table item hooks from raw-pointer to a
press-based activation path — so scope and validate them together, on their own,
before the smaller tickets. The low-risk reconciles are the natural next execution
targets — **T-38** (`onAction` value) is done (changeset `menu-onaction-value.md`),
**T-42** (CalendarYearPicker) is done (already-faithful off-by-one + the
`visibleYears` default aligned, changeset `calendar-year-picker-visible-years-default.md`),
and **T-44** (small-fixes bundle) is done (2 of 4 ported — `createKeyboard`
`continuePropagation` forwarding + `createFormValidation` non-element-ref guard;
the other 2 N/A: our bounded Tabs loops + the un-ported keyboard `DragManager`),
and **T-43** (detached-node / getter-only-focus crash fixes) is done (the
`createInteractionModality` focus-patch now uses `Reflect.defineProperty`,
changeset `interaction-modality-focus-define-property.md`; the two focusable-util
refinements are N/A on our simplified `isFocusable` and folded into the new
**T-57**), and **T-41** (DnD type/modality refinements) is done (Part A — the
`DragTypes.has` `*/*`/`type/*`/array wildcards — ported with the `DragType`
contract widening, changeset `drag-types-wildcard-array-matching.md`; Part B —
`mapModality`'s coarse-pointer switch — N/A, we have no drag-modality mapping),
and **T-40** (autocomplete CJK/IME) is done for its forward-typing core (Parts
A+B — the `beforeinput` listener switch + `insertCompositionText`/
`insertFromComposition` → `focusFirstItem`, changeset
`autocomplete-ime-forward-typing.md`; the deferred-`autoFocusOnMount` and Arrow
inline-navigation parts need collection-consumer plumbing / keydown-dispatch
reordering and are split into the new **T-58**).
and **T-35** (`useTypeSelect` 1.19 refactor) is done — the capture/bubble split,
the `altKey` bail (reverting `dfd4d37b`), `preventDefault`/`stopPropagation` on a
char match, reset-on-no-match, and the unmount timeout cleanup are all ported
(changeset `typeselect-keydown-faithful-parity.md`; the capture handler stays
Solid-inert-by-necessity, the live bubble path covers mid-search Space, and the
consumer shims are N/A because our consumers only spread `typeSelectProps`).
**T-46** (TableView re-sync) is now ◑: the divider-transparency delta landed,
the row-`borderBottom` move is N/A under our real-`<table>` `border-collapse:
separate` DOM, and the `CellFocusRing` top-offset split out as **T-59** (a
focus-ring re-architecture). **T-47** (ButtonGroup overflow) is done — the
overflow machinery was already a faithful port; only the `maxWidth: 'full'` cap
that makes it fire was missing (added, probe-confirmed). **T-48** (Disclosure) is
done — the style objects were already at parity; the real delta was routing
`DisclosureTitle.styles` to the trigger button (font-restricted) instead of the
heading wrapper, probe-confirmed. **T-57** (focusable-utils port — visibility +
inert + `isElementVisible`/`isTabbable`, with the Solid cross-realm guard) and
**T-58** (autocomplete `autoFocusOnMount` deferral + Arrow inline-nav, at the hook
level — the collection-consumer wiring stays a separate unwired-bridge ticket) are
done; the remaining **T-59** table focus-ring wants its own pass; **T-34**/**T-51**
(and the related **T-56** pointer-path `onAction`) stay gated behind the shared
press-path migration.
